# DATA_FLOW_AUDIT（Phase 1：読み取り調査 → Phase 2：v1.50.0 で高＋M1＋M6 を対応）

> 対応状況は各項目の見出し末尾と「6. 対応状況」を参照。回帰テストは `tests/data-reflection.test.js`（`ONLY=H1,H2,H3,M1,M6` で個別実行可）。

対象: index.html（v1.49.0）。行番号は調査時点。「確認済」=コードを読んで確定、「要再現」=Phase 2 で失敗テスト化して確定させる。

## 1. 保存データと共通関数

| キー | 内容 | 児童の紐付け |
|---|---|---|
| `spa_tests` | 課題（subject/testType/category/maxScore/date/term/inputMode/peUnit…） | — |
| `spa_scores` | 記録 `{testId, studentIndex, score(数値 or 'A','B+',…), absent, lateSubmit, answers, stage, score10…}` | **配列インデックス** |
| `spa_submissions_assignments` / `_data` | 提出物課題 / `{assignmentId, studentIndex, status, correctionDone, lateOnDue, absent}` | **配列インデックス** |
| `spa_grades_external` | 専科成績（A/B/C＋評定、学期なし） | インデックス |
| `spa_grade_weights` | 項目重み `[教科][観点][k_/t_/a_+testId]` | — |
| `spa_grade_thresholds` | A/B しきい値・評定境界 | — |

共通関数（すべて MODULE:GRADES 内）: `grdItemTerm`(15973・学期の唯一の判定) / `abcTo10`(15330・定義は1箇所) / `scoreTo10`(15346) / `peRecordToScore10`(15277) / `grdSubmissionCredit`(15259) / `getItemInputMode`(7672) / `grdCalculate`(16059)。

## 2. 対応表（書き込み元 → 読み取り先 → 換算・学期・欠席）

| 読み取り画面 | 元データ | 換算 | 学期フィルタ | 欠席 | 判定 |
|---|---|---|---|---|---|
| 成績処理統合 `grdCalculate` | tests/scores/提出/巡視 | scoreTo10・abcTo10・PE換算・重み | 成績画面の `grdCurrentTermFilter` | 除外(知/思/主) | 基準 |
| 統計タブ（課題別）`recRenderStats`/偏差値 | scores | 生点・偏差値（数値のみ） | 課題選択のみ | 除外 | OK |
| 提出物 統計/入力 | submissions | `grdSubmissionCredit` | `subPassesTermFilter` | 分母に含む(0点) | OK(M6) |
| 個人カルテ画面 概要/偏差値 | tests/scores/提出 | 生点・偏差値／提出率B式 | `kartePassesTermFilter` | 除外 | OK |
| カルテ出力PDF（児童用/教員用） | grdCalculate＋提出＋生活 | 成績は基準 | 提出/生活=`kvOutputTermFilter`、**成績=成績画面の学期** | — | **H1** |
| 面談用テキスト `kvBuildConferenceText` | 同上 | 同上 | **なし（通年）＋成績=成績画面の学期** | 除外 | **M3** |
| データ出力 テスト成績CSV | scores | 生値のみ | なし | 「欠席」表記 | M9 |
| データ出力 提出物CSV | submissions | status文字列のみ | なし | absent/遅れ/お直しが落ちる | M9 |
| データ出力 成績処理CSV | grdCalculate | 基準 | 成績画面の学期（出力に学期表記なし） | — | **H1** |
| レーダーPDF（教科カード） | grdCalculate＋tests直読 | カード内は scoreTo10 独自 | ABC/評定=成績画面の学期、テスト一覧/平均=**通年** | 除外 | **M4** |
| 算出根拠xlsx | grdCalculate結果 | 基準 | 成績画面の学期 | — | OK（`1→4点`はM5） |
| 同僚用テンプレートxlsx | tests直読 | 独自分類 | **なし（通年）** | — | **M5** |
| ダッシュボード（児童テスト平均/提出率、クラス提出率） | tests/scores/submissions | 独自 | なし／「今月」 | **除外しない** | **M1/M2** |

## 3. 不具合候補

### 高（成績値が変わる／出力が実態と食い違う）

**H1. カルテ出力・レーダーPDF・成績CSV の学期が、出力側の学期選択とずれる** ｜ ✅対応済(v1.50.0)
- 箇所: 11550, 11729（PDF）, 11930（面談テキスト）, 15659（レーダー）, 18682（CSV）。いずれも `grdCalculate(subj)`＝term省略 → `grdCurrentTermFilter`（成績処理画面の選択）で計算。
- 出力側は `kvOutputTermFilter` を「画面とは独立」と明記して持つ（12260）が、成績だけ参照していない。紙面見出しは「N学期のきろく」(11528, 11879)。
- 再現: 成績処理で「2学期」を表示 → カルテ出力で「1学期」を選び PDF 生成 → 見出し1学期・評定/知思主は2学期の値。CSV/レーダーは出力に学期の記載自体がない。
- 修正方針: `grdCalculate(subj, term)` に出力側の学期を渡す小ヘルパー1つに集約（`'all'`は通年）。CSVは列またはファイル名に学期を付与。保護者向けPDFには内部警告は出さない（教員向け画面のみ）。
- **対応内容**: カルテ出力PDF(児童用・教員用)・面談用テキストの成績を `kvOutputTermFilter` で計算。成績CSVはデータ出力画面に学期セレクト(`expGradeTerm`、既定=現在学期)を追加し、ファイル名と「学期」列に記載。レーダーPDFは成績処理画面の機能のため画面の学期に従う仕様として維持（学期の紙面表記は未対応、M4と併せて検討）。

**H2. 実技記録（検定/ルーブリック以外）を「思考・判断・表現」「主体性」に設定すると成績が化ける** ｜ ✅対応済(v1.50.0・失敗テストで再現確認済)
- 実技記録は `maxScore=9999` で保存（10291付近）。カテゴリは検定/ルーブリック preset のみ保存時に「知識・技能」へ強制、汎用単位（秒/回/点/m…）はロックなし。
- 知識ブロックのみ `peRecordToScore10` 経由（16090）。思考ブロック(16113)は `scoreTo10(score, 9999)`＝約0点として平均へ混入。主体性ブロック(16143)は `abc5` 扱いで数値を `abcTo10` へ渡し、3→10 / 2→7 / 1→3 に化け、それ以外は null。
- 修正方針: 観点ブロックごとの分岐をやめ、1課題1記録→score10 を返す共通関数 `grdItemScore10(test, sr)` に寄せる（PE分岐・inputMode・欠席・遅れ係数を1箇所化）。UI側で実技記録のカテゴリを知識・技能固定にする案もある（教員の運用確認が必要）。
- **対応内容**: 決定により(b)採用。`grdItemScore10(test, sr, persp)` を新設し、3ブロックが共通で使う（実技記録は観点によらず `peRecordToScore10` を経由）。実技ルーブリック/検定の換算そのもの(`peRecordToScore10`内)は無変更。観点ごとの既存差異（知識=満点未設定は100点扱い・思考=満点0は未評価・主体性=遅れ係数なし）は挙動を維持。

**H3. 課題の観点/教科を変更すると項目重みが既定値(1)に戻る** ｜ ✅対応済(v1.50.0)
- 重みキーは `[教科][観点]['k_'|'t_'|'a_' + testId]`（15219）。recAddTest の編集は category/subject 変更時に重みを移送せず、旧キーが孤立（課題削除時も未掃除）。
- 影響: 重みを 0/2 などにしていた課題は観点変更で重みが変わり、成績値が変わる。
- 修正方針: 編集保存時に旧→新キーへ移送。削除時にキー掃除。
- **対応内容**: `grdRelocateTestWeight(oldTest, newTest)`（成績モジュール）を編集保存・削除から呼ぶ。削除は取り消し(undo)対象に重みキーを含め、書き込みは即時(`setImmediate`)。まとめテスト(複合)は対象外。

**H5. 成績側キャッシュが画面切替で破棄されず、出力が古い値になる（Phase 2 の調査中に新規発見）** ｜ ✅対応済(v1.50.0)
- 成績モジュールのキャッシュ(`_grdCacheTests/Scores/Assign/Subs/Weights`)は `showView('grades')` と取り消し時にしか破棄されなかった。成績処理を一度開いた後に記録・提出物を入力し、カルテ出力/成績CSV/ダッシュボードへ進むと、古いデータで成績を計算していた。
- 対応: `showView` フックで全画面切替時に `grdInvalidate()`。

**H4. 児童IDが存在せず全記録が名簿の並び順に紐づく（設計リスク）** ｜ 🔧進行中（案B 段階1=ID付与・移行は実装済み(v1.51.0)、段階2以降はiPad確認後）
- scores/submissions/attendance/karte すべて `studentIndex`（配列添字）。`saveMasterRoster`(5426〜)は途中削除・並べ替えを警告するのみで再マップしない。確認ダイアログを通すと全データがずれる。
- 修正方針: 大規模（studentId導入＋移行）につき別計画。短期は「警告が出る」ことを回帰テストで固定するのみ。
- **追記(調査結果)**: 並べ替え・途中削除・途中挿入で15種類のデータすべてが実際にずれることを再現確認（7章）。あわせて、名簿を保存するだけで児童の視力・身長が消える **H6** も見つかった。

**H6. 名簿を保存すると児童の視力・身長(visionL/visionR/height)が消える（調査中に新規発見）** ｜ ✅対応済(v1.50.1)
- `saveMasterRoster`(5432〜)は `master.students` を `{name, gender}` だけで作り直す(5486)。身体タブ(座席の視力配慮・身体計測)で入力した属性が、名簿を1文字も変えずに保存しても消える。警告が勧める「安全な直し方」(転出の改名・末尾への転入)でも消える。
- 再現: 視力・身長入りの名簿を、内容を変えずに保存 → `[{name,gender,visionL,visionR,height}]` が `[{name,gender}]` になる。
- 修正方針: 保存時に既存児童の属性を引き継ぐ（H4のID導入と同時に、IDで突き合わせて引き継ぐのが確実。単独で直すなら位置＋氏名一致で引き継ぎ）。
- **対応内容**: `saveMasterRoster` が既存児童オブジェクトの全項目を保持したまま name・gender だけ更新（添字位置のまま。並べ替え対応はH4で）。`tests/roster-preserve.test.js`(11件、修正前に7件失敗を確認)。

**H7. 課題の観点を編集すると入力形式と満点が矛盾して保存される（isabc系テストの切り分け中に新規発見。v1.49.0由来）** ｜ ✅対応済(v1.50.1)
- 明示の `inputMode` を持つ課題(v1.49.0以降の新規作成分)で観点を 知識・技能→主体性 に編集すると、観点セレクトの onchange でタブは5段階に自動で切り替わるのに、保存は元のフラグ(score)を引き継ぎ `{category:'主体性', maxScore:0, inputMode:'score'}` になる。入力画面が満点0の数値欄になる。得点入力済みの課題では数値データがあるのに maxScore=0 になる。
- 対応: フォームの入力形式が元データと変わったら明示フラグを書く／得点入力済み(ロック中)は onchange で自動切替しない。`tests/category-edit-inputmode.test.js`(9件、修正前に4件失敗を確認)。

### 中（表示不一致・方針確認）

- **M1** ｜ ✅対応済(v1.50.0) ダッシュボード「テスト平均」(5695): 欠席レコード（v1.15.3以降 score を保持）を除外しない／学期絞りなし／遅れ係数なし／主体性の得点方式項目を category で除外／5段階項目は対象外。成績処理と別計算。
  - 対応: `grdStudentTestAverage(idx, term)`（成績モジュール）を新設。`grdCalculate` の項目を集めて `calcWeightedScore` で平均（提出物・机間巡視は除く、既定=現在学期）。ダッシュボードはこれを使用し、見出しに学期を表示。
- **M2** ｜ ⏸未対応（ただし分母の期限日欠席除外はM6で全系統に適用済み）提出率の定義が3系統: A=`grdSubmissionCredit`（お直し済1.0/前0.8、遅れ上限0.8：成績・提出物統計・レーダー）、B=(提出+再提出)/課題数（カルテ・PDF・ダッシュボード児童・面談）、C=ダッシュボードのクラス提出率(5580: `re*0.8`、分母=課題×全児童)。630faf2 は児童側(5517)のみB化しCは未統一。表示のみで成績には影響しない。
- **M3** ｜ ⏸未対応（成績のみH1で出力学期に統一） 面談用テキスト: 提出物・テスト個票は通年、成績は成績画面の学期。混在。
- **M4** ｜ ⏸未対応 レーダーPDFの教科カード: テスト一覧/平均が学期未フィルタ・遅れ係数(0.8)なし・`category !== '主体性'` 直参照で v1.49 の得点方式主体性を除外（`getItemInputMode` 非経由）。同カード内の ABC/評定は成績画面の学期で不一致。
- **M5** ｜ ⏸未対応 同僚用テンプレート(17130〜): 学期フィルタなし、`persp==='attitude'` を無条件でABC扱い（v1.49 得点方式が誤分類）。算出根拠の `raw===1 → '1→4点'`(17021) は `abcTo10(1)=3` と不一致（旧データ用ラベル）。
- **M6** ｜ ✅対応済(v1.50.0・決定: 期限日に欠席かつ未提出は分母から除外／後日提出は「提出」・遅延提出にしない) 欠席の扱い: テスト=分母から除外、提出物=分母に含めcredit0（成績・統計・カルテ・PDF・入力画面の%）。提出物の重み既定は0のため、重みを設定した場合のみ成績に影響。方針確認が必要。
  - 対応: 判定を成績モジュールの `grdSubmissionJudge(idx, assign, sub, att)` に集約（期限日欠席=提出記録の欠席フラグ または 出席簿が ×／忌・停。後日提出は遅れ印があっても0.8上限・遅れ扱いにしない）。適用経路: 成績処理・提出物画面(統計・入力進捗・個人入力の遅れタグ)・個人カルテ・PDF個票(`calcSubmissionStats`)・面談テキスト・ダッシュボード(児童/クラス)・提出物CSV(欠席セルは「欠席」)。
- **M7** ｜ ⏸未対応 専科成績: 学期の概念がなく全学期に通年値を表示（`grdExtCalculate` は term 無視）。同教科名の tests/提出物は早期 return で読まれない（混入・二重計上はなし）。
- **M8** ｜ ⏸未対応 主体性の得点方式(16143〜)・まとめテストには `lateSubmit ×0.8` が適用されない（知識/思考は適用）。
- **M9** ｜ 一部対応（提出物CSVの欠席セルのみM6で対応。他は未対応） テスト成績/提出物CSVは生データ出力で、換算・観点・学期列なし。提出物CSVは absent/遅れ/お直しが落ち、「欠席×」と「未提出×」が区別できない。

### 低（将来リスク）　※L1〜L7 はすべて ⏸未対応

- L1 `KEYS.grades`(`spa_grades`) は定義のみで読み書きなし。旧キー `gradeItemWeights`/`gradeIntegrationThresholds` は移行フォールバックのみ。
- L2 授業態度 ○/－/× → A/B/C 変換は `grdCalculate` 主体性ブロック(16136)の1箇所のみ。現UIは記号を書かない（レガシー用）が、他観点なら `abcTo10('○')=7`・`'－'/'×'=null` となり変換後(○→A=10)と不一致。
- L3 `grdExtSuggestHyoutei` は固定しきい値(8/5)で `cfg.final` を参照しない。
- L4 課題編集の保存で `createdAt` を毎回上書き。
- L5 `subRenderStats`(14315): 名簿縮小後の範囲外 studentIndex の提出記録が課題別集計に混入。
- L6 テスト一覧の「入力済 x/N」は欠席・空レコードも数える。
- L7 主体性の得点方式で score が文字列だった場合 `scoreTo10` が NaN を返し平均を汚染（入力形式ロックにより通常は到達しない）。

## 4. 問題なしと確認できた観点

- 学期判定: 全画面（記録入力/一覧・提出・カルテ・出力・成績）が `grdItemTerm` に一本化。
- ABC換算: `abcTo10` の定義は1つ（A=10/B+=8.5/B=7/B-=5/C=3、UI凡例3493と一致）。PE旧ロジックも経由済み。
- 観点振り分け: `grdCalculate` は `test.category` 直参照。記述問題・作文・振り返りで任意観点にしても、文字列→`abcTo10`で一貫。まとめテストは設問ごとの観点で分割。
- 児童キーの型: `Number()`/`String()` 正規化が全読み取り経路にある。
- 欠席（テスト）: 成績・偏差値・統計・CSV・カルテは除外/明示（例外はM1のみ。まとめテストは欠席入力UIなし）。
- 課題削除: scores・matomeQ・提出記録が連動削除（undo付き）。名称変更は id 紐付けで影響なし。
- 専科: `grdCalculate` は専科教科で早期 return するため二重計上なし。
- tests/ に index.html・sw.js のコピーなし。

## 5. Phase 2 案（tests/data-reflection.test.js）

1. 学期別（1/2/3/通年）× 入力形式別（得点/5段階/授業態度/記述/作文/実技）で、成績処理・カルテ出力・CSVの値を同一期待値と照合。
2. 失敗テストを先に作る: H1（出力学期≠成績画面学期）、H2（汎用実技記録×思考/主体性）、H3（観点変更で重み消失）、M1（欠席の混入）。
3. 修正の順序案: H1 → H2（共通関数化）→ H3 → M1/M4/M5 の共通関数寄せ → M2/M6 は方針確認後。

## 6. 対応状況（v1.50.0）

| 項目 | 状況 | テスト(data-reflection.test.js) |
|---|---|---|
| H1 出力の学期 | ✅ | 児童用/教員用PDF・面談テキスト・成績CSV 9件 |
| H5 キャッシュの古さ | ✅（新規） | 記録更新→PDF評定 2件 |
| H2 汎用実技記録 | ✅ | 7件（ルーブリック不変を含む） |
| H3 観点/教科変更・削除の重み | ✅ | 9件 |
| M1 ダッシュボード平均 | ✅ | 2件 |
| M6 期限日欠席 | ✅ | 15件（9経路） |
| H6 名簿保存で視力・身長が消える | ✅(v1.50.1) | roster-preserve.test.js 11件 |
| H7 観点編集で入力形式が矛盾(新規) | ✅(v1.50.1) | category-edit-inputmode.test.js 9件 |
| H7の既存矛盾データの検出 | ✅(v1.51.0) 診断ツールに一覧表示(検出のみ・自動修正なし) | audit-inputmode-conflict.test.js 12件 |
| H4 案B 段階1(児童ID付与・移行) | ✅(v1.51.0) | student-id-migration.test.js 37件 |
| H4 案B 段階2〜4・M2〜M5・M7〜M9(一部)・L1〜L7 | ⏸未対応（段階2以降は段階1のiPad確認後） | — |

### 既存データの値が変わる児童数（テストの合成データでの変更前→後）

| 修正 | 合成データ | 変わった児童 | 内容 |
|---|---|---|---|
| H1 | 2名(成績画面=2学期のまま出力=1学期) | 2/2 | 評定 1→3（甲）、3→1（乙）。出力学期を選んだ学期の値に |
| H2 | 3名（実技記録を思考/主体性に設定） | 2/3 | 甲: 思考平均 0→8・主体性 null→6、乙: 0→5・null→4。丙(欠席)は不変 |
| H3 | 1課題(重み2) | 観点変更した課題の重み 1→2（保持） | 重みを保持することで、変更前に起きていた 2→1 の値変化を防止 |
| M1 | 1名 | 1/1 | ダッシュボード表示 7.5→8.0 |
| M6 | 4名 | 2/4 | 成績の提出物10点: 乙 5→10、丁 8→9（甲10・丙0は不変）。表示系の提出率: 乙 50%→100%、クラス 57%→80% など |

実データでの影響は、実技記録を「知識・技能」以外にしている課題（H2）、および出席簿が欠席の日を期限日にした未提出課題（M6）がある児童に限られる。

## 7. H4 詳細調査（児童ID）— 調査のみ・コード変更なし

### 7.1 データ種別ごとの紐付けキー（すべて名簿の配列位置 = 0始まりの添字。児童ID・氏名での紐付けは無い）

| データ | ストレージキー | 紐付けの形 |
|---|---|---|
| 記録（点数・ABC・授業態度・記述・作文・まとめ・**実技記録/検定/ルーブリック**を含む） | `spa_scores` | 各レコードの `studentIndex`（数値。読み込み時に文字列→数値へ正規化） |
| 提出記録 | `spa_submissions_data` | 各レコードの `studentIndex` |
| 机間巡視 | `spa_patrol` | 各レコードの `studentIndex` |
| カルテ 児童記録 | `spa_karte_recs` | 各レコードの `studentIndex` |
| カルテ 生活／保護者対応／身体配慮／学習配慮 | `spa_karte_life/parent/health/learning` | オブジェクトのキー `String(添字)` |
| 出席簿／出欠届 | `spa_attendance` / `spa_attendance_notice` | `[日付][String(添字)]` |
| 朝の準備 | `spa_morning_prep` | `[日付][String(添字)]` |
| 忘れ物（日ごとの記録） | `spa_forgotten_daily` | `[日付][添字]`（配列） |
| 忘れ物（日ごとの集計・派生） | `spa_forgotten_history` | `[日付] = {total, students:{添字:件数}}`。**再現(7.2)では未検証だった**（段階2の設計時に発見） |
| 専科成績の児童別PDFコメント | `spa_grades_ext_pdf_comments` | `[String(添字)]`。**再現(7.2)では未検証だった**（同上） |
| 提出物 日記帳 | `spa_submissions_diary` | `[日付][添字]` |
| 漢字チェック | `spa_kanji` | `checks[String(添字)][String(文字番号)]` |
| 座席 | `spa_seating` | 座席キー → 添字（数値） |
| 専科成績 | `spa_grades_external` | `data[教科][String(添字)]` |
| 削除の取り消しバッファ(メモリのみ) | `karteDeletedBuffer` | `studentIndex`（画面を閉じると消える） |
| 新体力テスト(pf.html) | `pf_records_年度` / `pf_roster` | pf側名簿の `id`。`spa_master` に seqNo/id が無いため **位置+1** で採番（氏名が空の行を除外するとさらにずれる）。SPA側への取り込み(`grdImportFitnessTest`)は **氏名一致**（同姓同名は取り込み中止、改名で不一致） |
| 児童の属性(視力・身長) | `spa_master.students[i]` | 児童オブジェクトの中（並べ替えでは付いて動くが、名簿保存で消える=H6） |

名簿を変更できる経路は `saveMasterRoster`(テキスト欄→保存ボタン)とバックアップ復元(`importBackup`)の2つだけ。

### 7.2 再現結果（実際の保存ボタンで名簿を操作。4名・15種類のデータ＋座席に持ち主の目印を付け、※後日の点検で対象は計18ストアと判明(10.1)。未検証の2種類は同じ添字キーで、同じずれが起きる構造。操作後に「その添字の児童のものか」を照合。`ずれ`=別の児童のものとして表示される件数/4名）

| 操作 | 警告ダイアログ | ずれ | 児童属性(視力・身長) |
|---|---|---|---|
| R0 同じ名簿で保存 | なし | 0 | **消失(H6)** |
| R1 並べ替え(甲乙を入替) | あり | **全15種類 2/4** ＋座席 2/4 | 消失 |
| R2 途中削除(乙を削除) | あり | **全15種類 3/4** ＋座席 3/4 | 消失 |
| R3 途中挿入(転入生を3番目) | あり | **全15種類 2/4** ＋座席 2/4 | 消失 |
| R4 末尾追加(転入生を最後) | なし | 0 | 消失 |
| R5 転出を「（転出）乙」に改名 | なし | 0 | 消失 |

- 警告は R1〜R3 で正しく出る（設計どおり）。ただし確認して進めると、記録・提出・カルテ・出欠・座席など全データが同時にずれ、ずれたことに気づく仕組みも元に戻す手段も無い（復元はバックアップ頼み）。
- R2 では削除した児童の記録が名簿外の添字に取り残される（孤立データ）。R3 では転入生に他の児童の記録が付く。
- 再現スクリプトは調査用（リポジトリ外）。回帰テスト化する場合は `tests/roster-shift.test.js` として、方式案の実装後に「ずれ0」を期待する形で追加する。

### 7.3 方式案

**案B（推奨）: 不変ID＋名簿保存時に全データを一括付け替え（保存形式は添字キーのまま）**
- 各児童に不変ID（`stu_` + 乱数8桁。フィールド名は **`studentId`**。`pf.html` が `s.id` を新体力テストの児童キーに使うため `students[].id` は増やさない）を付与。既存データは添字と整合しているので、**起動時に位置順でIDを振るだけ**（データ変更なし・冪等・`migration_studentId_v1` フラグで記録）。
- 名簿保存時に IDで「旧添字→新添字」の対応表を作り、下記15種類＋座席を1つの関数 `remapStudentIndexes(map)` で**まとめて書き換える**。読み取り側（`studentIndex` 参照約160箇所）は無変更。削除された児童のデータは捨てず `spa_student_archive`（ID別）へ退避し、後から戻せる。
- 名簿編集UI: テキスト欄は行と児童の対応が取れない（改名＋並べ替えを区別できない）ため、(1)行末にID札（例 `山田 太郎,男,#a1b2`）を持たせる方式か、(2)1行1児童のリスト編集(並べ替え・転出・追加ボタン)へ変更。iPadでは(2)を推奨。IDの無い行=転入、IDが消えた児童=転出(確認ダイアログで退避を明示)。
- 利点: 変更が名簿保存周辺に閉じる／既存データ・既存テスト・旧版との往復に影響が少ない。欠点: 保存形式が添字のままなので、一部キーだけを復元した場合のずれは防げない。

**案C（将来案）: 保存キーそのものをIDへ全面移行**
- 15種類のキーを `studentId` に置換。読み取り側約160箇所の改修が必要で、バックアップ形式も版分岐が必要。名簿操作は再付け替え不要になり、部分復元にも強いが、影響範囲が大きく1コミット1段階でも工数・リスクが高い。案B完了後に必要性を再評価する。

### 7.4 マイグレーション・バックアップ・ロールバック（案B）

1. **既存データの移行**: 起動時に `students[i].id` が無い児童へ位置順でIDを付与するだけ（添字キーのデータは触らない）。実行前に `spa_master` の生JSONを `spa_migration_backup_studentId`（1世代）へ退避。フラグ `migration_studentId_v1` で二重実行防止。失敗時はフラグを立てず退避から `spa_master` を書き戻し、従来動作（IDなし）のまま起動する。
2. **名簿保存の一括付け替え（トランザクション）**: (a)対象の全キーの生JSONを1つのスナップショットへ保存(直近3回分) → (b)新しい値をメモリ上で全部作る → (c)検証(件数が退避分を除き一致・添字が名簿長未満・`(studentIndex,testId)` 重複なし) → (d)`setImmediate` で全書き込み → 再読込して再検証。**どこかで失敗したらスナップショットを書き戻して名簿変更ごと中止**。成功後も既存の `uiUndoable` に全キーを渡して「元に戻す」を提供（デバウンス書き込みが取り消しを上書きしないよう即時書き込みを徹底 — v1.50.0 のH3と同じ注意点）。
3. **バックアップ互換**: 形式を `version: 11`（`studentIdSchema: 1`）へ。旧版(v10)のバックアップを新版へ復元 → 起動時移行が位置順でIDを付与（バックアップ内は自己整合しているので安全）。新版のバックアップを旧版へ復元 → 保存形式が添字のままなので読める（`id`は無視される。旧版で名簿を保存すると `id` が消えるが、次回起動時に再付与され、旧版の警告が効く範囲では整合する）。あわせて現状は無い**復元前の自動スナップショット**を追加し、`importAll` が「バックアップに無いキーを消さない」ことによる新旧の混在を検出・警告する。
4. **pf.html / 新体力テスト取り込み**: pf側の `id` を `student.id` に、`grdImportFitnessTest` の突き合わせを氏名から `id` に変更（同姓同名の取り込み中止が不要になる）。既存の `pf_records_*` は移行時点の位置+1 → 新IDへ1回だけ変換（移行時に `pf_roster` と `spa_master` が一致しているか氏名で検証し、不一致なら変換せず警告）。
5. **ロールバック**: 案Bは「IDの付与」と「名簿保存時の付け替え」だけで、保存形式を変えない。問題が出たら sw.js のバージョンを戻せば、IDは無視されるだけで従来どおり動く（付け替え済みの名簿変更はスナップショット/取り消しで戻せる）。

### 7.5 実施順序案（1段階1コミット・各段階で回帰テストとスナップショット検証、承認後にpush）
1. ID付与＋起動時移行＋退避（挙動変更なし）＋テスト
2. 対象ストアの登録表＋`remapStudentIndexes`＋スナップショット/検証/ロールバック＋テスト（7.2の再現を「ずれ0」で固定、新ストア追加時に登録漏れを検出するテスト付き）
3. 名簿編集UI（リスト編集）を `remapStudentIndexes` に接続、警告ダイアログを置換、H6（属性の引き継ぎ）を同時に解消
4. pf.html・体力テスト取り込みをIDへ、バックアップ v11・復元前スナップショット
   - **【段階4の予定・追記】`pf-residual`（復元時に pf 名簿が不一致だったため退避に残した pf の記録・集計）を復元できるようにする**: pf を `studentId` ベースへ切り替えた（pf の名簿・記録のキーを `studentId` にした）あとは、pf 名簿の「一致／不一致」の判定が不要になり、位置に依存せず児童のIDで pf の記録を戻せる。退避の `pf-residual` エントリ（`residual:true`、`data` は `pf_*` のみ、`studentId` あり）を、名簿にその `studentId` の児童がいれば、その児童の pf 記録として復元する（いない場合は、先に児童を復元するよう案内）。復元も既存のトランザクション（スナップショット・ジャーナル・ロールバック）に含め、`residual-not-restorable` の拒否を解除する。テストは、pf 名簿が不一致の状態で復元→`pf-residual` が残る→段階4の移行後に復元→pf の記録が児童に戻る（保存則・ずれ0）を追加する。
（案Cは4の後に必要性を再評価）

## 8. 既存テスト失敗3件の切り分け（`isabc-consolidation` 2件・`isabc-editform-fix` 1件）

**結論: 3件とも実装の不具合ではなく、テスト側の古さ（v1.49.0で課題フォームの前提が変わったため）。→ v1.50.1 でテストを修正し全件PASS。**
- 修正の過程で、これまで途中で止まって見えなかった検査が実行され、実装の不具合H7(v1.49.0由来)が見つかったため、あわせて修正した。
- 原因: v1.49.0(5ac43ca)で `recAddTest` が入力形式の判定を「観点・種別から都度計算」から「フォームの得点/5段階タブの状態(`_recFormInputMode`)」に変えた。このタブ状態は種別・観点セレクトの `onchange` で初期化されるが、旧テストは `select.value` を直接代入して `onchange` を発火せず、既定の「得点」のまま満点空欄で保存 → 「満点を入力してください」で保存されず `null` になる。
- 実UI相当（`onchange` 発火後に保存）で確認すると、観点=主体性の小テストは `maxScore=0`・`inputMode='abc5'`、種別=ルーブリックも `maxScore=0`・`inputMode='abc5'` で保存され、期待どおり。実装は正しい。
- 個別: (1) `isabc-consolidation` パターン1(主体性) と (2) パターン2(ルーブリック)、(3) `isabc-editform-fix` の前提(主体性+小テスト)は、いずれも同一原因。
- 対応(v1.50.1): `fillTestForm` で種別・観点の設定後に `recOnTestTypeChange()`/`recOnCategoryChange()` を呼び、`findTestByName` を部分一致に変更（自動プレフィックス対応）。編集フローの観点変更も onchange を発火。

## 9. H4 案B 段階1 実施記録（v1.51.0）

- **付与**: 起動時に `studentId` を持たない児童にだけ `stu_`+8桁（`crypto.getRandomValues`）を付与。全員付与済みなら何も書かない（退避も移行フラグも作らない）ので、再起動のたびに振り直さない。名簿保存時の転入生にも付与（既存のIDは保持）。
- **退避とロールバック**: 付与前に `spa_master` の生JSONを `spa_migration_backup_studentId`（1世代）へ退避（読み戻して一致確認）→ 永続化済みJSONの複製へ付与 → 書き込み → 読み戻して検証（人数・IDの有無と一意性・ID以外の全項目が付与前と同一）。退避・書き込みの例外／検証不一致のときは退避から書き戻し、ID なしの従来動作のまま起動（次回起動で再試行）。名簿が空・読み込み失敗（破損）のときは何もしない。
- **既存データは1件も変わらない**: 全キーのスナップショット比較で、増えたキーは退避コピーと移行フラグ(`migration_studentId_v1`)だけ、`spa_master` は `studentId` が増えただけ、他の全キーは1バイトも変わらない（削除されたキーも無い）ことをテストで確認。
- **IDが失われない**: 名簿保存（変更なし・改名・末尾追加・末尾削除）・バックアップ復元（IDあり／旧形式=IDなしは復元後の起動で付与）・H6の属性保持のいずれでも付与済みIDは保持されることをテストで確認。
- **設計上の判断**: (1) フィールド名は `id` でなく `studentId`（`pf.html` の児童キー `s.id` を壊さないため）。(2) 退避キーは氏名を含むため `KEYS` に登録（端末データ消去・バックアップの対象）。(3) 診断ツール(`collectMigrationFlags`)がキー名に `migration` を含む値を全文表示するため、退避コピーは内容を出さず文字数のみ表示に変更（診断出力はコピーして共有されるため。`audit-range-check.test.js` が検出）。
- **段階1の制約**: 名簿保存でのIDは添字位置に付いて残る（H6の属性保持と同じ）。並べ替え・途中削除でIDが人に付いて動くのは段階3から。段階1ではIDは何にも使われず、記録の紐付け・画面の動作は変わらない。

## 10. H4 案B 段階2 設計案（付け替え関数）— 設計のみ・コード変更なし

前提: 段階3で名簿編集UIが「新しい名簿（各行が `studentId` を持つ。転入は ID なし）」を渡してくる。段階2はそれを受けて全データを安全に付け替える関数 `applyRosterChange(newStudents)` を作る（UI接続は段階3）。

### 10.1 付け替え対象（18ストア＋pf。点検の結果、7章の再現で見落としていた2ストアを含む）

`M`=`Number/String`の型ゆれに注意（`studentIndex` は数値・文字列の両方が過去に混在。読み込み時は `Number()` で正規化して照合し、書き戻しは数値）。

| # | キー | 紐付け方式 | 付け替え方法（旧添字→新添字 `map`） | 削除された児童の分 |
|---|---|---|---|---|
| 1 | `spa_scores`（実技記録・検定・ルーブリック・まとめ含む） | 配列レコードの `studentIndex` | 各レコードの `studentIndex` を `map[旧]` に書換。順序は保持 | 該当レコードを抜いて退避 |
| 2 | `spa_submissions_data` | 配列レコードの `studentIndex` | 同上 | 同上 |
| 3 | `spa_patrol` | 配列レコードの `studentIndex` | 同上 | 同上 |
| 4 | `spa_karte_recs` | 配列レコードの `studentIndex` | 同上 | 同上 |
| 5〜8 | `spa_karte_life` / `_parent` / `_health` / `_learning` | オブジェクトのキー `String(添字)` | キーを `String(map[旧])` に付け替え（全部を一度に新オブジェクトへ。上書き衝突を避ける） | 該当キーの値を退避 |
| 9〜11 | `spa_attendance` / `_attendance_notice` / `spa_morning_prep` | `[日付][String(添字)]` | 各日付内でキーを付け替え。空になった日付は残す（元の状態を保つ） | 各日付の値を退避 |
| 12 | `spa_forgotten_daily` | `[日付][添字]`（配列） | 各日付内でキー付け替え | 各日付の配列を退避 |
| 13 | `spa_forgotten_history`（派生） | `[日付].students[添字]`＋`total` | `students` のキーを付け替え、`total` は「旧total − 退避した児童の件数」に更新（残る児童が居なければその日付ごと削除。アプリの既存動作と同じ）。**他ストアに依存せず単独で完結**（当初案の「dailyから再計算」は、dailyと履歴が元々食い違っている場合に値を解釈し直してしまうため取りやめ） | 件数を日付別に退避 |
| 14 | `spa_submissions_diary` | `[日付][添字]`（件数） | キー付け替え | 各日付の件数を退避 |
| 15 | `spa_kanji` | `checks[String(添字)][String(文字番号)]` | `checks` のキーを付け替え（`chars` は不変） | 該当キーを退避 |
| 16 | `spa_seating` | 座席キー → 添字（値） | **値**を `map[旧]` に書換。削除された児童の席は**キーごと削除**（空席になる） | 席キーを退避 |
| 17 | `spa_grades_external` | `data[教科][String(添字)]` | 各教科内でキー付け替え | 各教科の値を退避 |
| 18 | `spa_grades_ext_pdf_comments` | `[String(添字)]` | キー付け替え | 該当キーを退避 |
| — | `spa_seating_groups` | 座席キーのみ（児童に紐づかない） | 不要（無変更） | — |
| — | `spa_karte_archive` | 定義のみで未使用 | 不要 | — |
| — | 課題・重み・しきい値・教科リスト・課題名リスト等 | 児童に紐づかない | 不要 | — |
| — | `scoreDataBackup_*` / `spa_submissions_removed_v1863` / `spa_kanji_backup_*`（旧バックアップ） | 旧データの写し（添字入り） | **付け替えない**（写しなので。診断ツールの「旧バックアップ」として削除可能な扱いのまま） | — |
| pf | `pf_roster` / `pf_records_年度` / `pf_fitness_年度` | pf側の名簿 `id`（＝spa名簿の位置+1）で紐づく | 10.5 | 10.5 |

- **登録表**: 上表を `STUDENT_KEYED_STORES = [{key, kind, remap(raw, map), extract(raw, ids), restore(raw, idx, piece)}]` として1か所に定義（`kind`: `array-field` / `object-key` / `date-object-key` / `nested-checks` / `seat-value` / `derived-history` / `subject-object-key`）。各ハンドラは**純関数**（生JSON→新しい生JSON。書き込みはしない）。
- **登録漏れの防止**: テストで `KEYS` の全キーと `pf_*` を走査し、「登録表にある」か「児童に紐づかない許可リスト（理由つき）にある」かのどちらかであることを検証する。新しいストアを足して登録を忘れるとテストが落ちる。
- **範囲外の添字（孤立データ）**: 旧名簿の人数以上の添字を持つデータ（過去の削除で取り残されたもの）は、名簿が広がると別の児童に付いてしまうため、付け替え時に `spa_student_archive` の `__orphan__:添字` という枠へ退避する（消さない。診断ツールの「studentIndex 範囲超過」で従来から検出されるもの）。添字が数値でない・欠けているレコードは「児童に紐づかない」ものとして無変更。
- **同一名簿（恒等）**: 並べ替え・削除・転入が無ければ `noop`（書き込みゼロ）。孤立データがあっても触らない。
- **対応表の作り方（純関数 `computeRosterRemap(oldStudents, newStudents)`）**: `studentId` で突き合わせ、`map`（旧添字→新添字）・`deleted`（旧にあって新に無いID）・`added`（新添字。IDなし=転入）を返す。新名簿のIDの重複、旧名簿に無いIDが混ざる（改ざん・別端末の名簿）、旧名簿にIDが無い児童がいる場合はエラー（何も書かずに中止）。

### 10.2 削除された児童の退避（`spa_student_archive`）と復元

```
spa_student_archive = { version: 1, entries: [
  { archiveId: 'arc_…', studentId: 'stu_…', archivedAt: ISO, reason: 'roster-delete', sourceIndex: 旧添字,
    student: { name, gender, visionL, …（児童オブジェクト全項目） },
    data: { spa_scores: [レコード…(studentIndexは除く)], spa_karte_life: [...], spa_attendance: {日付: '×', …},
            spa_seating: ['seat3'], spa_kanji: {文字番号: 'o'}, spa_grades_external: {教科: {…}}, … } }
] }
```
- 氏名・記録を含むため `KEYS` に登録（バックアップ対象・**端末データ消去(退勤モード)の対象**。段階1の退避キーと同じ扱い）。
- **復元** `restoreArchivedStudent(archiveId)`: 名簿の**末尾**に同じ `studentId` で追加し（新添字=人数）、`data` を各ストアの `restore` ハンドラで新添字へ戻す（席は空席の場合のみ復元、衝突する場合は席だけ諦めて通知）。復元後、その `entries` から削除。`studentId` が名簿に既にいる場合は復元を拒否（重複防止）。復元も付け替えと同じトランザクション（10.4）で行い、失敗時は何も変えない。
- UIは段階3（設定の名簿欄の下に「転出した児童のデータ」一覧＝氏名・退避日・復元/完全に削除）。「転出」は名簿から消すのではなく改名で残す従来の案内も引き続き有効。
- 完全に削除する操作だけが、記録を失う唯一の操作になる（確認ダイアログあり）。

### 10.3 付け替え前の全キースナップショット

- `spa_roster_snapshot` に、**変更対象のキー全部（10.1の18ストア＋`spa_master`＋`spa_student_archive`＋pf）の生JSON**と、その時点の `spa_master` を1つのJSONとして保存（1世代。新しい名簿変更が成功したら置き換える。容量のため段階1の案の「3世代」は取りやめ）。存在しなかったキーは `null` で記録（復元時に削除する）。
- サイズ: 記録の総量の約2倍になる（localStorage と IndexedDB の両方に書かれる）。実行前に `navigator.storage.estimate()` の残量と比較し、足りなければ**開始前に**中止して理由を表示する。

### 10.4 トランザクションと「失敗時に名簿変更ごと中止」する流れ

`StorageManager` の性質（コード確認の結果）— 設計上の前提:
- `setImmediate` は失敗(容量超過など)を**例外にしない**（トーストを出すだけ）。しかも**先にメモリキャッシュへ書く**ため、`getRaw` で読み戻しても失敗を検出できない。IndexedDB への書き込みも投げっぱなし。複数キーをまとめて確実に書く仕組みも無い。
- → 付け替えは専用の書き込み関数 `storageVerifiedWrite(key, str)`（`localStorage.setItem` → `localStorage.getItem(key) === str` を**直接**確認し、そのうえで `StorageManager` のキャッシュ/IDBにも反映）を使い、失敗は例外にする。
- **段階1についての注意（正直な報告）**: 段階1の移行の検証は `getRaw`（キャッシュ経由）で読み戻しており、localStorage への書き込み失敗（容量超過）は検出できない。書くのは名簿（数KB）と退避コピーのみで実害の可能性は低いが、段階2で `storageVerifiedWrite` を作る際に段階1の移行もこれに置き換える。

流れ:
1. **事前確認**: 保留中の書き込みを確定（`uiFlushPending()`＋`flushSaveQueue()`）、未完了の `spa_roster_txn` が無いこと、名簿が読み込み失敗でないこと、容量の残量。→ 満たさなければ何も書かずに中止。
2. **計画をメモリ上で作成（書き込みゼロ）**: 全ストアをパース→`computeRemap`→各ハンドラで新しい生JSONと退避分を作成→**不変条件**を検証: (I1) 各ストアのレコード数＝付け替え後＋退避分、(I2) 全添字が新名簿の範囲内、(I3) `(studentIndex, testId)` 等の重複なし、(I4) 児童に紐づかないキーは1バイトも変わらない、(I5) `studentId` の一意、(I6) 転入生の添字にデータが付いていない。1つでも崩れたら**何も書かずに中止**（名簿欄はそのまま）。
3. **スナップショット保存**（10.3）＋読み戻し確認 → 失敗なら中止（この時点で変更済みなのはスナップショットキーだけ。削除して終了）。
4. **ジャーナル**: `spa_roster_txn = {id, state:'applying', at, keys:[…]}` を書いて確認。
5. **適用**: 退避 `spa_student_archive` → 各ストア → `spa_master`（最後）の順に `storageVerifiedWrite`。**1件でも失敗したら**、スナップショットから全キーを書き戻し（同じ関数で確認）、名簿変更ごと中止して理由を表示。
6. **最終検証**: 永続層から読み直して不変条件を再確認。崩れたら手順5の失敗と同じくロールバック。
7. **確定**: `spa_roster_txn.state='committed'`（変更後の各キーのハッシュを記録）→ メモリ状態（選択中の児童・キャッシュ・取り消しバッファ）を破棄するため**ページを再読み込み**（バックアップ復元と同じ。画面上の児童選択が古い添字を指し続ける事故を防ぐ）。
- **途中で強制終了(クラッシュ・iPadのアプリ切替)した場合**: 起動時、`loadMaster` の**前**に `spa_roster_txn.state==='applying'` を検出したらスナップショットから自動ロールバックし、「名簿変更が途中で中断したため元に戻しました」と表示（再試行はユーザー操作）。ロールバックの書き戻しにも失敗した場合は、起動を止めずに赤い警告バー（既存のリカバリーバー）で「バックアップから復元してください」を表示し、スナップショットは残す。
- **取り消し**: 確定後、設定に「直前の名簿変更を取り消す」を出す。スナップショットへ戻す。ただし確定後に対象キーが変わっていた（新しい入力があった）場合は、ハッシュ不一致として「そのあとの入力が失われます」を明示して確認（既定は中止）。
- **同一名簿での保存**（`map` が恒等・追加も削除も無し）: 書き込みゼロで終了（付け替え・スナップショット・再読み込みをしない）。

### 10.5 pf.html（新体力テスト）との連携で影響が出る点

- pf.html は **`localStorage` を直接**読み書きし、`StorageManager` を通らない。名簿は `pf_roster`（`spa_master` の読み込み時に `id = 位置+1` で採番、氏名が空の行は除外）、記録は `pf_records_年度[id]`。**`spa_master` の並べ替え・削除は pf 側に伝わらない**。現状でも、SPAの名簿を並べ替えたあとに pf の「読み込み」を押すと、`id` が新しい位置で振り直されるのに `pf_records` のキーは古い `id` のままなので、記録が別の児童に付け替わる（pf内で同種のずれ。**7.2の再現には含まれていない既存の問題**）。
- SPA側の取り込み `grdImportFitnessTest` は**氏名一致**（`pf_roster` の氏名→`spa_master` の氏名）。テスト `fitness_年度` の点数は取り込みのたびに全置換されるので、付け替えの影響は次の取り込みまで一時的なだけ。ただし氏名の改名・同姓同名で取り込めない既存の制約は残る。
- **段階2での扱い**: `pf_roster` が付け替え前の `spa_master` と**氏名・順序ともに一致**している場合に限り、`pf_records_*`（と `pf_fitness_*`）のキー（旧 `id`=旧位置+1）を新位置+1へ付け替え、`pf_roster` も新名簿から作り直して同時に保存する（スナップショット・検証の対象に含める）。一致しない場合（pfで手入力した名簿・古い名簿）は**pf側には触れず**、付け替え後に「新体力テストの名簿がSPAと一致しないため未更新です。pfで名簿を読み込み直してください」と通知する（勝手に付け替えて別の児童に紐づける事故を避ける）。削除された児童の pf 記録は `spa_student_archive` に含めて退避。
- **段階4（pf対応）で解消**: `pf_roster` に `studentId` を持たせ、`pf_records` のキーを `studentId` に変更（旧キーは移行時に位置+1→`studentId` へ変換。`pf_roster` と `spa_master` が一致しない場合は変換せず警告）。`grdImportFitnessTest` も `studentId` で突き合わせ（同姓同名の取り込み中止が不要になる）。`pf.html` は Service Worker のキャッシュ対象なので、変更時は `CACHE_VERSION` を上げる。
- pf.html を変更する段階（2のpf連携・4）は、pf.html側のテスト（`pf_*` の直接書き込みを検証）を追加してから行う。

### 10.6 テスト計画（`tests/roster-shift.test.js`）

期待値は**「ずれ0」**（7.2の再現スクリプトを基に、期待値を反転）。ただし対象は「6操作×13データ」ではなく、点検の結果 **18ストア（座席を含む）＋pf**。
1. **操作**: R0 同一名簿・R1 並べ替え・R2 途中削除・R3 途中挿入・R4 末尾追加・R5 転出の改名（＋R6 削除→復元）。名簿は `applyRosterChange(newStudents)`（段階2）を直接呼ぶ。段階3以降はUI経由でも同じ期待値で回す。
2. **ずれ0**: 各ストアの持ち主の目印（7.2と同じ）が、操作後も `studentId` が同じ児童に付いていること。R2では削除児童の目印が**どのストアにも残らず**、`spa_student_archive` にだけ残ること（孤立データ0）。R3の転入生には他の児童のデータが付かないこと。
3. **不変条件I1〜I6**を各操作後に検証（レコード数の保存則を含む）。児童に紐づかないキーは全キー比較で1バイトも変わらない。
4. **失敗注入**: (a) N番目のキー書き込みで例外、(b) 書き込み内容が壊れる（読み戻し不一致）、(c) 容量超過（`localStorage.setItem` が例外）でスナップショット/適用の各段階、(d) 適用の途中で強制終了（テスト用フックで例外を投げてロールバックを飛ばし、再読み込みして起動時の自動ロールバックを確認）→ いずれも**全キーが操作前と1バイトも変わらない**こと。ロールバック自体が失敗する場合（書き戻しも例外）の警告バー表示も確認。
5. **同一名簿**は書き込みゼロ（`setItem` 呼び出し回数0）。
6. **復元**: 削除→`restoreArchivedStudent`→末尾に同じ `studentId`・全データが戻る（席は空席のときのみ）。IDの重複は拒否。
7. **ランダム操作（乱数固定）**: 児童数4〜30の名簿に対し、並べ替え・削除・挿入をランダムに50ケース → ずれ0・保存則を検証（1操作ごとにスナップショットと比較）。
8. **登録漏れガード**: `KEYS`＋`pf_*` の全キーが「登録表」または「許可リスト（理由つき）」にあること。
9. **バックアップ互換**: 付け替え後にバックアップ→復元して、データが一致すること。旧形式（`studentId` なし）の復元後に付け替えが動くこと。
10. **pf**: `pf_roster` が一致する場合は `pf_records` が追従、一致しない場合は無変更＋通知。
11. 既存の `roster-preserve`（属性保持）・`student-id-migration`・全テストが引き続きPASS。

### 10.7 実施の区切り（各1コミット・iPad確認を挟む）と未決事項

- **2a**: 純関数のみ（`computeRemap`・18ストアのハンドラ・登録表・不変条件検証）と、その単体テスト（書き込みなし）。挙動変更なし。
- **2b**: `storageVerifiedWrite`（段階1の移行も置き換え）・スナップショット・ジャーナル・起動時ロールバック・`applyRosterChange`・退避/復元・失敗注入テスト。UIには未接続（挙動変更なし）。
- **2c**: pf連携（pf_roster一致時の付け替え）とpf側テスト。
- **確定事項（レビューで決定）**:
  1. **退避データ**は無期限で保持する。設定画面に「退避中の児童 N名」を表示し、児童ごとに完全削除できる（UIは段階3）。
  2. **スナップショット**は1世代。
  3. **取り消し**: 名簿変更のあとに（対象キーへの）入力がある場合は取り消さず、理由をトーストで表示する（確認ダイアログは使わない）。10.4の「ハッシュ不一致→確認」を「ハッシュ不一致→中止＋トースト」に読み替える。
  4. **pf名簿が一致しない場合**は通知のみ（pf側には触れない）。
  5. **自動再読み込み**は行う。再読み込みの前に `flushSaveQueue()` で保存待ちの入力をすべて書き込み、再読み込み後は名簿設定の画面を開く。
- **進捗**: 2a（純関数・不変条件・ストア登録表）は実装済み(11章)。2b・2cは未着手。

## 11. H4 案B 段階2a 実施記録（v1.51.1）— 純関数・不変条件・ストア登録表（UI未接続・書き込みなし）

- **実装**（`index.html` MASTER モジュール。`window.StudentRemap` で公開。**どこからも呼ばれておらず、既存の挙動は変わらない**）:
  - `STUDENT_KEYED_STORES`: 18ストアの登録表（種別 `array-field`×4／`object-key`×5／`date-object-key`×5／`derived-history`／`nested-checks`／`seat-value`／`subject-object-key`）。見落としていた `spa_forgotten_history`・`spa_grades_ext_pdf_comments` を含む。
  - `NOT_STUDENT_KEYED`: 児童に紐づかない KEYS の全キーを理由つきで列挙。
  - `computeRosterRemap(old, new)`: `studentId` で旧→新の対応表（`map`/`deleted`/`added`/`identity`）。ID なし・重複・不明ID・不正IDはエラー。
  - `planRosterChange(rawMap, old, new)`: 全ストアの新しい生JSON（`newRaw`）と退避分（`archive`/`archiveEntries`）を作る**純関数**。恒等は `noop`。ストアが壊れている・形が違うときは `ok:false` で何も返さない。意味が変わらないストアは `newRaw` に入れない。
  - `verifyRemapInvariants`: I1（件数の保存則）・I2（添字の範囲）・I3（重複の増加なし）・I4（登録表以外への書き込みなし）・I5（対応表とIDの整合）・I6（転入生の添字にデータなし）・I7（退避IDは削除児童か孤立データのみ）。
  - `collectStudentKeyedRaws`: 登録表の生JSONを読む（読み取り専用）。
- **設計の確定・補足**（10.1に反映）: (1) 忘れ物集計は単独ストアで完結（旧total−退避分、残る児童が居ない日は削除）。(2) 孤立データ（旧人数以上の添字）は `__orphan__:添字` に退避（消さない）。(3) 恒等は孤立データがあっても noop。(4) 添字として読めないキー・先頭ゼロのキー・添字の無いレコードは「児童に紐づかない」として無変更。(5) 動かないレコードは1バイトも変えない（文字列の添字は移動時のみ数値に正規化）。
- **テスト**（すべてPASS。全テストFAIL 0）:
  - `tests/student-remap.test.js`（53件）: 対応表とエラー・各種別のエッジケース・不変条件の違反検出（I1〜I7を意図的に壊して確認）・純粋性（`setItem`/`removeItem`/`StorageManager.set*` 呼び出し0回・入力不変）・**登録漏れ検出**（KEYS の全キー、`index.html` 内の `'spa_*'` リテラル全件を走査して、登録表か既知の非対象キー（理由つき）に無ければ失敗）・乱数固定のランダム60ケース（4〜30人）と連続8操作でずれ0・保存則。
  - `tests/roster-shift.test.js`（126件）: **A（有効）** 付け替え計画レベルで **6操作 × 18ストア = 108セルすべてずれ0**（件数も期待値どおり）、削除児童が全ストアから消えて退避に18ストア分入ること、転入生の添字にデータが付かないこと、付け替えなしなら全18ストアでずれが検出される検算（照合が機能している証拠）。**B（SKIP）** 実適用(`applyRosterChange`・名簿UI経由)の 6操作 × 18ストアは、未実装の間はSKIP（FAIL 0を維持）。`ROSTER_SHIFT_FORCE=1` で実行すると期待どおり失敗する（`applyRosterChange is not a function`）ことを確認済み。段階2b/3で有効化する。
  - **変異テスト**: 実装を一時的に壊して、テストが失敗することを確認した（付け替え先を変えない→roster-shift 57件・student-remap 2件失敗／削除児童の席が残る→3件・4件失敗／忘れ物集計の合計を更新しない→student-remapで1件失敗）。確認後に元へ戻した。
- **次（2b）**: `storageVerifiedWrite`（段階1の移行の検証も置き換え）・スナップショット・ジャーナル・起動時ロールバック・`applyRosterChange`・退避(`spa_student_archive`)と復元・失敗注入テスト。2c: pf連携。

## 12. H8 IndexedDB の鏡に削除したデータ（氏名）が残る（段階2bの調査中に新規発見）｜ ✅対応済(v1.51.2)

**背景（コード確認）**: `StorageManager` は localStorage を真実とし、IndexedDB を鏡にしている。起動のたびに `migrateFromLS` が localStorage の全キーを IndexedDB へ**上書きコピー**し（値は自己修復する）、`loadCache` が IndexedDB の全キーをメモリキャッシュへ読む。**キーの削除は鏡に反映されない**（`migrateFromLS` は削除をしない）。

**再現テストの結果（`tests/idb-residue.test.js`、修正前は19件中10件失敗）**
| 操作 | 結果 |
|---|---|
| 課題の削除・記録の削除 | **残らない**（配列を書き直すので IndexedDB の値も更新され、localStorage と全キー一致）。回帰確認として保持 |
| 退勤モードの端末データ消去（`wipeLocalData`） | `KEYS` の全キーは localStorage・IndexedDB とも消える。**しかし `KEYS` に無い個人データが残る**: 新体力テスト `pf_roster`（**氏名**）・`pf_records_*`（記録）・`pf_setting`、移行時の旧バックアップ（`scoreDataBackup_*`・`spa_kanji_backup_*`・`spa_submissions_removed_*`）。消去後に書き出したバックアップにも氏名が入る（トーストは「安全に退勤できます」） |
| pf の名簿リセット・記録データ削除（`index.html` 内蔵版と `pf.html`） | `localStorage.removeItem` だけ。IndexedDB の鏡に残り、**次回起動で `loadCache` によりキャッシュに復活**する |

**復活する経路（実測）**: 鏡に残ったキーは(1)再読み込み後のキャッシュに現れ、(2) `exportAll`（キャッシュのキーを使う）でバックアップに入り、(3) そのバックアップを復元すると localStorage に戻る。`pf_*` は読み取りが localStorage 直接のため画面には出ないが、バックアップ経由で削除した名簿・記録が持ち出される。

**重大度**: 中（データの破損・喪失ではなく、プライバシー。「消したはずの氏名が端末・バックアップに残る」）。修正は小さく安全なため、段階2bとは別のコミットで対応した。

**修正（v1.51.2）**
1. `wipeLocalData`: `KEYS` に加え、`pf_*`・`scoreDataBackup_*`・`spa_kanji_backup_*`・`spa_submissions_removed_*` も `StorageManager.remove`（localStorage・鏡・キャッシュ）で削除。起動後の `getAllKeys` はキャッシュ由来のため、鏡にだけ残るキーも対象になる。同じオリジンの別アプリのキー（`doc-index-v1`・task系）は消さない。
2. `pf` 内蔵版の名簿リセット・記録データ削除は `StorageManager.remove` 経由に変更。
3. `pf.html` の同2操作は `pfRemoveKey`（localStorage＋同じオリジンの鏡）に変更。**鏡が未作成の場合は作らない**（空の IndexedDB を作ると index.html の初期化（objectStore 作成）が走らず Phase C にならない。`onupgradeneeded` で中止。ガードを外す変異で検出できることを確認）。
4. 起動時（`loadCache`）: **localStorage に無い `pf_*` の鏡は、過去の pf 削除の残りとして、キャッシュに入れず鏡からも削除**（実機に既にある残りの掃除）。**`pf_*` 以外は消さない**: 容量超過で localStorage へ書けず IndexedDB にだけある新しい値の可能性があるため（`safeSetItem` は localStorage 失敗時も IndexedDB へ書く）。
- 未対応・注意: `migrateFromLS` 自体は削除を反映しない設計のまま（一般のキー削除は `StorageManager.remove` 経由なら鏡からも消える。localStorage を直接消すコードを新たに書かないこと）。段階2bの `storageVerifiedRemove` は鏡（IndexedDB・キャッシュ）も消し、IndexedDB 準備前は保留リストで準備後に消す。

## 13. H4 案B 段階2b 実施記録（v1.51.2）— 検証付き書き込み・トランザクション・退避/復元・取り消し（UI未接続）

- **実装**（`index.html` MASTER モジュール。`window.applyRosterChange` などで公開。**名簿画面(`saveMasterRoster`)には未接続で、既存の名簿保存の挙動は変わらない**）:
  - `storageVerifiedWrite/Remove`: localStorage へ書いて**直接読み戻し**、失敗（容量超過・内容の破損）は例外にする。キャッシュ・IndexedDB は検証後に反映。`storageIdbFlush`: 再読み込み前に IndexedDB への書き込み・削除の完了を待つ。IndexedDB 準備前に消したキーは保留リストに積み、準備後に鏡から消す。**段階1のID付与の検証もこれに置き換え済み**（容量超過でキャッシュだけ先行しないことをテストで確認）。
  - `applyRosterChange(newStudents)`: 計画(書き込みゼロ・不変条件)→容量の事前確認→スナップショット(1世代)→ジャーナル(applying)→退避→各ストア→名簿(最後)→永続層から読み直して検証→確定(committed＋全対象キーのハッシュ)。失敗したらスナップショットへ全キーを書き戻して名簿変更ごと中止。同一名簿は書き込みゼロ、改名・属性だけの変更は名簿1キーのみ（失敗したら元へ）。成功後は `flushSaveQueue` → IndexedDB の完了待ち → 再読み込み（`sessionStorage` のフラグで再読み込み後に名簿設定の画面を開く）。
  - 容量の事前確認: スナップショット＋各キーの増分＋余裕(32KB)が localStorage の空き（既定5MiB相当の保守的な上限）に入らなければ、**書き込みゼロ**で `insufficient-storage`（見積もりを返す）。実際の QuotaExceeded は localStorage を満杯にして再現（Chrome では約520万文字で満杯）し、スナップショットの書き込みで失敗→変更前と同一に戻ることを確認。IndexedDB の空き(`navigator.storage.estimate`)は非同期のため事前確認には含めない（IndexedDB は鏡で、書き込みは検証対象の localStorage）。
  - 起動時 `recoverRosterTxnOnStartup`（**`DOMContentLoaded` の最初、`loadMaster` や他の移行より前**）: `applying`/`rollback-failed` のジャーナルとスナップショットを検出したら自動ロールバックして「元に戻しました」を通知。`committed` は取り消し用に残す。`rolled-back` は後始末のみ。復旧できない（スナップショットなし・破損・書き戻し失敗）場合は赤いバー（既存のリカバリーバー）で「バックアップから復元」を案内し、`masterLoadFailed` を立てて保存を停止（壊れた状態の上に書かない）。ジャーナルの無いスナップショット（スナップショット直後・ジャーナル前の中断の残り）は全データの写しを端末に残さないよう削除。
  - 退避 `spa_student_archive`（`KEYS` に登録＝端末データ消去・バックアップの対象）: `studentArchiveSummary`（設定の「退避中の児童 N名」用）、`restoreArchivedStudent`（末尾に同じ `studentId` で戻す。ID重複は `duplicate-id` で拒否、孤立データは `orphan-not-restorable`。席が埋まっていれば席だけ戻さず `skipped` で通知）、`purgeArchivedStudent`（完全削除）。復元も付け替えと同じトランザクション。
  - 取り消し `undoRosterChange`: 確定後に対象キー(18ストア＋名簿＋退避)が1つでも変わっていれば**取り消さず**、トースト「名簿を変更したあとに入力があったため、取り消せません」を表示（確認ダイアログは使わない）。1世代。
  - バックアップ: スナップショットとジャーナルは含めない（容量が倍にならない・復元先で古い取り消し点として誤動作しない）。退避は含める。
- **テスト**（`tests/roster-transaction.test.js` 65件、すべてPASS）: 検証付き書き込み／容量の事前確認と実QuotaExceeded／正常系（書き込み順・ハッシュ・保存待ちのflush・ずれ0・非対象キーの不変）／**書き込みの合間の中断を全23通り再現**（各書き込みの直前で強制終了→再起動で自動復旧→全キーが変更前と1バイトも変わらず、ジャーナル・スナップショット・退避がキャッシュ・鏡にも復活しない）／**容量超過・内容破損を各書き込み(23通り)に注入**→ロールバック／ロールバック自体の失敗・起動時の復旧失敗／起動時ジャーナル検出の全状態／退避・復元・取り消し／バックアップ・端末データ消去・鏡の整合。`tests/roster-shift.test.js` の **B（実適用）のSKIPを外し、6操作×18ストアで実際の `applyRosterChange` 経由でもずれ0**（合計240件PASS）。
- **変異テスト**（実装を一時的に壊して、テストが失敗することを確認。確認後に元へ戻した）: ロールバックが何も書き戻さない→14件失敗／起動時の自動ロールバックを無効化→10件／検証付き書き込みが読み戻しを検証しない→2件／取り消しが入力を検出しない→2件／容量の事前確認を無効化→2件。**適用後の永続層からの再検証(post-verify)だけは、書き込み時の検証が先に働く冗長な二重チェックのため、除去してもテストでは区別できない**（多重防御として残す）。
- **設計上の発見・判断**: (1) 起動時は localStorage が真実（`migrateFromLS` が全キーを IndexedDB へ上書きコピー）。(2) `StorageManager.setImmediate` は容量超過を握りつぶしキャッシュを先に更新する（12章のH8と同じ「鏡」の性質）。(3) 強制終了は「以降の書き込み・削除がすべて失敗する」障害注入で再現（永続状態が同じ）。
- **次**: 2c（pf連携: `pf_roster` が付け替え前の名簿と一致する場合のみ `pf_records_*` を追従）。段階3（名簿編集UIを `applyRosterChange` に接続、`saveMasterRoster` の警告ダイアログを置換、退避の一覧・復元・完全削除UI）。

### 12.1 H8 追補（v1.51.3）: 実機に既に残っている鏡の一回限りの掃除（所有キー全般）

v1.51.2 の起動時掃除は `pf_*` だけだったため、KEYS のキー等が退勤モード消去（IndexedDB 準備前・削除の中断など）で鏡に残っている実機は掃除されなかった。レビューの指示で、**利用者の操作（PWAの削除・再設置など）なしに**、起動時に一回だけ掃除する処理を追加した。
- **対象**: localStorage に無く IndexedDB にだけある、**アプリが所有するキー**（`KEYS` の全キー・`pf_*`・旧バックアップ類。判定は `isClassroomOwnedKey`）。同じオリジンの別アプリのキー（`doc-index-v1`・task系）や未知のキーは**決して触らない**（テストで確認）。削除は鏡（IndexedDB）とメモリキャッシュから。localStorage は1バイトも書き換えない（完了の印 `migration_idbGhostPurge_v1` を除く）。
- **一回限り**: 完了の印（`{at, removed}`）が localStorage にあれば実行しない。以後の残存は、削除処理の修正（v1.51.2）と `storageVerifiedRemove` で防ぐ。
- **安全策（実行条件 `idbGhostPurgeDue`）**: 未実行 かつ（localStorage に所有キーが1つ以上ある＝localStorage が生きている、または直前が退勤モード消去〔`spa_wiped` の印〕）。**localStorage が空で退勤モードの印も無い場合は掃除しない**（localStorage だけが消えて、鏡が唯一の写しになっている可能性を守る。印が付かないので、localStorage にデータが入った次回以降に実行される）。localStorage が読めない場合も実行しない。
- **方針の変更（記録）**: v1.51.2 では「pf以外のキーは、容量超過で localStorage へ書けなかった新しい値の可能性があるため触らない」としていたが、指示により所有キーは削除対象とした。リスクは、容量超過で localStorage に書けず鏡にだけ入った新しい値を失うこと。ただし起動時の `migrateFromLS` が localStorage の値で鏡を毎回上書きしており、localStorage が真実という設計は既に同じ性質（既存キーの新しい値は既に失われる）のため、許容とした。
- **テスト**（`idb-residue.test.js` 27件）: 所有キー（KEYS・pf・旧バックアップ）が鏡・キャッシュから消える／所有しないキーは残る／localStorage の既存キーは不変／完了の印／一回限り（完了後は繰り返さない）／localStorage が空＋印なしは掃除しない／退勤モード直後は空でも掃除して氏名が残らない。

### 13.1 追補（v1.51.3）: 容量の事前確認を「端末の実際の空き」で判定する方式へ変更

- **変更前**: localStorage の使用量を数え、**固定の上限（5MiB相当）** との比較で判定していた。上限はブラウザごとに違う（Chrome では約520万文字で満杯になることを実測。iPad の Safari は別）ため、端末によっては判定が甘い／厳しい。
- **変更後**: 必要量（スナップショット＋各キーの増分＋8KB＋余裕32KB）と**同じ大きさの一時キー**（`spa_capacity_probe`）を実際に書いてみて、QuotaExceeded 等で失敗したら「足りない」と判断し、**名簿・記録・スナップショット等へは一切書かずに**中止（`insufficient-storage`、見積もりと失敗理由を返す）。書けたらすぐ消す。固定値と、テスト用の上書きフック（`ROSTER_LS_LIMIT_CHARS_OVERRIDE`）は削除。一時キーは `StorageManager` を通さない（鏡に入れない）。強制終了などで残った場合は、次の起動（`recoverRosterTxnOnStartup` の最初）で削除する。
- **役割分担は変えない**: 確認を通っても、実際の書き込みの失敗は従来どおり検出してロールバックする（確認は「早めに諦めるため」、安全性はトランザクション側）。
- **テスト**（`roster-transaction.test.js` 70件）: 確認の書き込みが QuotaExceeded→書き込みゼロで中止・全キー不変・一時キーが残らない／疑似的に小さい容量の端末（空き20KB）で中止・十分な容量（空き600KB）で成功／確認を通っても書き込みの途中で容量超過→ロールバック／localStorage を実際に満杯にして、確認が実際の空きで失敗／確認をすり抜けても実際のスナップショット書き込みで `snapshot-failed`／残った一時キーの起動時削除。確認を無効化する変異で4件失敗することを確認済み。

## 14. H4 案B 段階2c 実施記録（v1.51.3）— 新体力テスト(pf)連携（UI未接続）

- **方針（レビューの指示・10.5の確定）**: `pf_roster` が付け替え前のSPA名簿と**氏名・順序・id(位置+1)とも一致**する場合だけ、pf の記録・集計・名簿を追従させる。一致しなければ **pf には一切触れず**、トーストで通知だけする（核となる18ストアの付け替えは通常どおり行う）。pf の書き込みは `applyRosterChange` と**同じトランザクション**（スナップショット・ジャーナル・ロールバック・起動時の自動復旧・取り消し）に含める。
- **一致判定 `srPfMatch`**: pf は `pf_loadFromSpa` で SPA名簿の位置+1 を id に採番し、氏名が空の行を除外する。よって `pf_roster` が「付け替え前の名簿から氏名が空の行を除いたもの」と件数・氏名・順序が同じで、各エントリの id が元の位置+1 に等しいときだけ一致。`pf_roster` が無い・壊れている・氏名や順序が違う・id が位置+1 でない（手入力名簿など）・記録が壊れている場合は不一致（`mismatch`）。pf のデータが全く無ければ `none`（pf のキーを作らず、通知もしない）。
- **追従 `planPfChange`（純関数）**: `pf_records_年度`・`pf_fitness_年度`（全年度）を id=位置+1 で付け替え（削除児童の分は退避、旧人数を超える id は孤立データとして `__orphan__` に退避）。`pf_roster` は新しいSPA名簿から作り直し（氏名・順序・id=位置+1。生き残る児童の age・note など pf 側の項目は引き継ぎ、転入生は age=10、氏名が空の行は除外）。不変条件: P1 件数の保存則／P2 id が新名簿の範囲内／P3 `pf_roster` が新名簿と一致／P4 id=位置+1／P6 転入生の id に記録が無い。
- **トランザクションへの組み込み**: 書き込む pf のキーはスナップショットの対象に加わり、書き込み順は「…各ストア → **pf のキー** → 名簿 → 確定」。ロールバック・起動時の復旧・取り消し（確定後に pf へ入力があれば取り消さない）も pf を含む。改名のみ（同一名簿）でも pf が一致していれば、名簿と `pf_roster` を同じトランザクションで更新する。
- **退避・復元**: 削除児童の pf の記録・集計・`pf_roster` の項目を退避（`spa_student_archive` の `data` に `pf_*` として）に含める。復元時、現在の pf 名簿が SPA名簿と一致していれば末尾（id=位置+1）へ戻す。**一致しなければ pf には触れず、pf の分だけを退避に `pf-residual` として残し**（消さない・退避中の児童の数には数えない）通知する。`pf-residual` は復元できず（`residual-not-restorable`）、完全削除はできる。将来 pf 名簿が一致してからの復元は未実装（制約）。
- **通知**: 不一致のとき「新体力テストの名簿がSPAの名簿と一致しないため、新体力テストのデータは更新していません。新体力テストの画面で名簿を読み込み直してください」をトーストで表示。再読み込みで消えないよう `sessionStorage` にも積み、再読み込み後の起動時にも表示する。
- **テスト**（`tests/roster-pf.test.js` 71件、すべてPASS）: 一致する場合の6操作（追従・pf の持ち主ずれ0・`pf_roster` の作り直しと age/note の引き継ぎ・退避・スナップショットに pf のキー・pf_setting と非対象キー不変・核の18ストアもずれ0）／改名のみ／不一致6種（順序違い・余分な児童・id が位置+1 でない・氏名違い・`pf_roster` なし・壊れた JSON）で pf 不変＋通知＋核データは付け替え／pf データなし／再読み込み後の通知／**pf を含む中断27通り・容量超過27通り・内容破損27通りで全キー（pf 4キーを含む）が変更前と同一**／取り消し（pf も戻る・pf 入力後は拒否）／復元（一致→末尾に戻り保存則が成立・不一致→`pf-residual`）／鏡（IndexedDB）との整合。
- **変異テスト**（実装を一時的に壊して確認、確認後に戻した）: 一致判定を常に真→12件失敗／pf をスナップショットに含めない→7件／通知を出さない→8件／不一致時に pf の分を退避に残さない→2件。
- **テストが見つけた実装の不具合**: 不一致（pf に触れない）の場合にも、適用後の検証が「pf_roster が新名簿と一致するか」を確認してしまい、ロールバックしていた（追従しない場合は pf の検証を行わないよう修正）。
- **テストの不具合（修正済み）**: 通知の確認に `document.body.textContent` を使っていたが、インライン `<script>` のソース文字列も含むため文言が**常に見つかり**、通知を出さなくてもテストが通っていた（変異テストで発覚）。`roster-transaction.test.js`（「元に戻しました」「取り消せません」等）も同様だったため、両方とも**表示中のトースト要素（`#toast.show` の `#toastMsg`）を待って確認**する方式に直し、実際の表示を確認できるようにした（修正後も全件PASS）。
- **次**: 段階3（名簿編集UIを `applyRosterChange` に接続、`saveMasterRoster` の警告ダイアログを置換、退避の一覧・復元・完全削除UI）。 段階4で `studentId` ベースへ切り替えたあと、`pf-residual` を復元できるようにする（7.5の段階4に追記）。


## 15. H4 案B 段階3 実施記録（v1.52.0）— 名簿編集UIの接続（実機の挙動が変わる最初の段階）

- **置き換えたもの**: 設定の「名簿」は、従来の「氏名を1行1名で並べたテキスト欄 → 保存時に警告ダイアログ」から**リスト編集UI**に変わった。従来の `saveMasterRoster` のテキスト解析・警告付き保存・`updateRosterPreview` は**廃止**。名簿を変える経路は `saveMasterRoster` → `applyRosterChange`（段階2b/2cのトランザクション）の**1本だけ**（`master.students` を直接書き換える経路は残していない）。確認ダイアログ（`confirm`）は一度も使わない。
- **画面**: 1行1児童（番号・氏名欄・性別・↑・↓・＋(この下に転入)・削除）。すべてタップ領域 `--tap-min`(44px)以上。先頭に「＋ 先頭に追加」、下に一括追加欄（貼り付けた氏名を**末尾に新規行として**追加。初期登録用。既存児童の順序・属性は動かさない）。並べ替え・転入(任意の位置)・削除・改名は**下書き**（`rosterDraft`）として画面上で行い、「保存」ボタンで**1回のトランザクション**として適用する（1回の保存＝取り消し1回）。削除した行は「削除予定」と表示され「↩ 戻す」で保存前なら元に戻せる。氏名が空の行があると保存せず、その行を赤枠にしてトースト（データ不変・再読み込みなし）。
- **下書きの持ち方**: 下書きは氏名と性別の**編集値だけ**を持ち、視力・身長などの属性は保存時に**現在の名簿から**取り込む（`rosterDraftStudents`）。下書きを開いたままあとで身体データを保存しても、名簿の保存で古い値に戻らない（変異テストで確認）。クラス情報（年度・学年・組・担任）は同じ画面から保存でき、名簿が同じでクラス情報だけ変わった場合は再読み込みせず保存、名簿も変わった場合は `applyRosterChange(…, {classInfo})` で**同じトランザクション**（取り消しでクラス情報も戻る）。
- **保存後**: `applyRosterChange` 成功後は自動で再読み込みされ、再読み込み後に設定の名簿画面を開いて「✅ 名簿を更新しました［取り消す］」のトースト（取り消せる場合のみ）を表示。名簿カードにも「↩ 直前の名簿変更を取り消す」ボタン（`srUndoStatus` が取り消し可能のときだけ表示。確定後に対象キーへ入力があれば表示せず、押した場合も段階2bの拒否トーストが出る）。
- **手動バックアップの促し**: 下書きの**最初の編集時**と**保存の直前**に、「💾 名簿を変える前に、バックアップを取っておくと安心です」のトーストを出す（操作は止めない。直近10分以内にバックアップ済みなら出さない）。
- **退避中の児童**: 設定に「退避中の児童 N名」のカード。一覧（氏名・退避した日・種別）、「復元」（末尾に戻す。段階2bの `restoreArchivedStudent`。**未保存の下書きがある間は復元・取り消しを拒否**してトーストで知らせる）、「完全削除」。**完全削除は確認ダイアログの代わりに「2回タップ」**（1回目で「もう一度タップで完全削除」と表示して4秒間だけ有効、2回目で実行）。`pf-residual`（新体力テスト分だけの退避）・孤立データは、一覧に表示され**完全削除のみ可能**（復元ボタンは出さない。7.5の段階4で `studentId` ベースにしてから復元可能にする予定は変更なし）。
- **エラー表示**: `applyRosterChange` の失敗理由（容量不足・書き込み失敗・検証失敗など）は、内部コードではなく教員向けの文言にしてトースト表示（保護者向けの出力には一切出ない画面のみ）。
- **テスト**:
  - 新規 `tests/roster-ui.test.js`（66件）: 画面の表示／**6操作(並べ替え・途中削除・転入(先頭/途中/末尾)・改名・性別・組み合わせ)を実際のタップ・入力で行い、18ストア＋pf でずれ0**／保存後の再読み込みと画面（設定名簿画面・取り消しトースト・取り消しボタン）／下書き・戻す・破棄・バックアップの促し・氏名空・保存失敗（スナップショット書き込みの容量超過を注入して全データ不変）／取り消し（ボタン・トースト・入力後は拒否）／退避（復元・2回タップの完全削除・pf-residual・孤立データ・下書き中の復元拒否）／クラス情報のみ・併用／一括追加／古い下書きが身体データを上書きしない／空の名簿／タップ領域44px以上／確認ダイアログ0回。
  - 既存 `roster-preserve.test.js`（10件）と `student-id-migration.test.js` 7章（合計37件）を、`textarea` へ値を代入する方式から**実際のUI操作**（`tests/helpers/roster-ui.js`）に書き換え（同じ性質を、UI経由でも満たすことの確認）。
  - 全体: 1226件PASS・0FAIL、`attendance-snapshot.js` 一致。
- **変異テスト**（実装を一時的に壊してテストの失敗を確認。確認後に戻した）: 保存が `applyRosterChange` を迂回して名簿を直接書き換える→21件失敗／下書きが現在の名簿を取り込まず古いコピーで上書き→1件／完全削除が1タップで実行される→1件（画面の状態確認で例外）／氏名空の検証をしない→1件。
- **テスト作成上の注意（記録）**: (1) 日本語の入力欄の置き換えは、トリプルクリックでは全選択にならず文字が連結される（`element.select()` を使う）。(2) トーストの確認は `#toast.show #toastMsg` を見る（`document.body.textContent` はスクリプト文字列を含み常に真になる。14章と同じ）。(3) テストの初期データに児童IDを固定しないと、起動時のID採番で毎回別のIDになり、ID不変の確認ができない。
- **制約・未対応**: (1) `pf-residual` の復元は段階4（`studentId` ベース切替）まで不可。(2) 下書きは画面を離れる／再読み込みすると失われる（保存前の内容は端末に残さない。保存されていない変更は元の名簿のまま）。(3) 一括追加は末尾追加のみ（並べ替えは下書き上で行う）。(4) 実機(iPad)での操作確認は未実施（次項の手順で先生に確認してもらう）。
- **決定事項（先生の承認・2026-09-19）**: (1) 名簿編集は**「下書き → 保存ボタン」方式で確定**（1回の保存＝1トランザクション＝取り消し1回。1行ごとの即時確定にはしない）。(2) 退避した児童の完全削除は**「2回タップ」方式で確定**（確認ダイアログは使わない）。push は先生の承認後に実施。iPad の実機確認は、GitHub Pages の都合で push 後に行う（先生はバックアップ済み）。
- **次**: push 後に実機(iPad)での確認 → 結果を受けて段階4（`studentId` を読み書きの基準にし、位置(index)参照を廃止。`pf-residual` の復元）。

## 16. H4 案B 段階4 設計案 — 新体力テスト(pf)の studentId 化・pf-residual の復元・バックアップ v11（設計のみ・コード変更なし）

### 16.1 調査で分かった現状（コード読み取りのみ）

1. **pf の実装が2つある**。(a) `index.html` 内蔵版（`pf_*` 関数群、約22394行〜。アプリの「新体力テスト」タブ）、(b) 単体ページ `pf.html`（1266行、`sw.js` のキャッシュ対象）。**両方が同じキー(`pf_roster`・`pf_records_年度`)を localStorage へ直接読み書きする**。`pf.html` へのリンクはアプリ内のどこにも無く（`index.html`・`manifest.json` に参照なし）、URL を直接開く（ブックマーク等）場合のみ到達できる。
2. 児童キーは **`pf_roster[].id`（=SPA名簿の位置+1、氏名が空の行は除外）**。`pf_records_年度[id]` と `pf_fitness_年度[id]` がこの id をキーにする。`pf_fitness_年度` を書くのは **`pf.html` の `transferToSpa` だけ**（内蔵版には書く処理が無い）。→ 端末に `pf_fitness_*` があれば、過去に pf.html を単体で使った証拠になる。
3. id は画面のHTMLに**数値のまま埋め込まれている**（`setRecord(${s.id},…)`、`id="total-${s.id}"`、`<select>` の `value`、`parseInt(sel.value)`、`roster.find(r=>r.id===id)`）。内蔵版で id を参照する行は約24、`pf.html` は約28。文字列の ID に変える場合は、これらすべてが対象。
4. 手動名簿（`pf_applyManualRoster`／`pf.html` の手動入力）は任意の id・氏名を登録でき、**SPA の児童と結び付かない**児童を作れる。
5. `pf_loadFromSpa`（名簿を読み込む）は id を位置+1 で振り直すだけで `pf_records` のキーを直さないため、SPA名簿を並べ替えた後に押すと記録が別の児童に付く（10.5 の既存の不具合。段階2c は「一致するときだけ追従」で悪化を防いでいるが、根本は残っている）。
6. `grdImportFitnessTest` は `pf_roster` の**氏名**でSPA児童を探し（同姓同名は取り込み中止、改名で不一致）、点数を `spa_scores` へ全置換で取り込む。
7. バックアップ `exportBackup` は `version: 10`。`importBackup` は **version を見ておらず**、`rawLocalStorage` と `data` を `StorageManager.importAll`（`setImmediate` の単純な上書き。読み戻し確認なし、バックアップに無いキーは消さない）で書くだけ。復元前の自動退避も無い。`pf_*` は `exportBackup` が直接 `rawLocalStorage` に追加している。

### 16.2 データ形式（v2）

- `pf_roster[i] = { id, studentId, name, gender, age, … }`。**`studentId` が正、`id` は表示用の通し番号（位置+1）に格下げ**（画面の「#」列・CSV用。記録のキーには使わない）。
- `pf_records_年度` と `pf_fitness_年度` は **`studentId` をキー**にする（`{ "stu_ab12cd34": {握力:…}, … }`）。
- 形式の判別は**キーの形**で行う（`stu_…` か数字か）。グローバルなフラグだけに頼らない — バックアップの部分復元やキー単位の混在（16.6）でも、キーごとに正しく扱うため。移行の完了は `migration_pfStudentId_v1`（実行の記録）で残す。
- 内蔵版の HTML 埋め込みは `setRecord('${s.studentId}',…)` のように**引用符付きの文字列**にする（`stu_[a-z0-9]{8}` は引用符・HTML属性で安全）。要素ID・`<select>` の値・`find` も文字列比較へ。旧 `parseInt(sel.value)` は廃止。
- **手動名簿**の児童（SPAに居ない）: `studentId` を持たない代わりに、pf 側で `pfm_`＋乱数8桁の ID を振る（`pfm_` の児童は SPA の名簿変更・取り込み・退避の対象外＝pf内だけで完結する）。SPA名簿を読み込み直すときは、`pfm_` の児童を**残す**（消さない）。→ 手動名簿を使っていなければ影響なし。

### 16.3 既存データの移行（初回起動時に一度だけ）

実行場所: `recoverRosterTxnOnStartup` と `loadMaster` の**後**、pf を読む前。段階2bの `srRunTransaction`（スナップショット・ジャーナル・ロールバック・起動時復旧）に載せる。対象キー: `pf_roster`・`pf_records_*`・`pf_fitness_*`。

1. **一致する場合（自動）**: `srPfMatch(pfRaws, master.students)` が真（氏名・順序・id=位置+1 が SPA名簿と一致）→ 位置 idx の児童の `studentId` で、`pf_roster` に `studentId` を付け、`pf_records_*`・`pf_fitness_*` のキー `String(idx+1)` を `studentId` に付け替える。**記録件数の保存則**（付け替え前後で件数が同じ）と、`pf_roster` の各児童の `studentId` が SPA の児童と氏名で一致することを検証（`verifyPfMigration`、不変条件 M1〜M5）。失敗したらロールバック（pf は移行前のまま）。
2. **一致しない場合（自動では変換しない）**: 一致しない原因は、(a) 名簿を並べ替えた後に pf を読み込み直していない、(b) 手動名簿、(c) 過去に上記5の不具合で記録が既にずれている、のどれか。**位置による変換は誤った児童に記録を付ける危険があるため行わない**。代わりに **pf の「設定」タブに、連携状態と「名簿の氏名で引き継ぐ」ボタン**（プレビュー付き）を出す。
   - 「氏名で引き継ぐ」: pf 名簿の各児童を、SPA名簿の氏名が**一意に一致**する児童へ紐付け（同姓同名・一致なしは紐付けない）。プレビューに「氏名 → 引き継ぎ先」を表示してから、ボタンで確定（1回の操作＝1トランザクション＝取り消し可）。これは pf 画面で先生が実際に見ている「id→氏名」の対応（pf_roster 内で自己整合）をそのまま引き継ぐ方式で、上記(c)で既にずれた記録は救えない（救う手段は無い）ことを明記する。
   - 紐付けられなかった児童の記録は**削除せず**、`spa_student_archive` に `reason:'pf-unlinked'`（氏名つき・完全削除のみ可能、退避中の児童の数には数えない）として残す。
3. **pf のデータが無い**: 何もしない（キーを作らない）。
4. **診断表示（読み取りのみ）**: 移行の前に、設定に「新体力テストの連携状態: 一致／不一致（理由）／データなし／移行済み」の1行を出す（データを書かない）。実機の状態を、DevTools なしで確認するため。

### 16.4 名簿変更(`applyRosterChange`)・復元の簡素化

- **v2 の pf**: `planPfChange` は「`studentId` キーの記録を、名簿に居ない児童ぶんだけ退避へ移す」だけになる。並べ替え・転入・改名では **記録キーは一切動かさない**（位置に依存しないため）。`pf_roster` は新しいSPA名簿から作り直す（`id`=位置+1 を振り直し、`studentId`・age など pf 側の項目は引き継ぎ、`pfm_` の児童は末尾に残す）。**`srPfMatch` の「一致しなければ触らない」分岐は v2 では不要**（移行前の v1 形式の端末だけで使う）。
- **pf-residual の復元**: 復元は `studentId` で行う。
  - 児童の復元（`restoreArchivedStudent`）: pf が v2 なら、退避した pf の記録・集計を **`studentId` キーで**戻す（一致判定なし）。pf が未移行（v1）で不一致のときは従来どおり `pf-residual` を残す。
  - **`pf-residual` 単体の復元**（新規、`restoreArchivedResidual`）: 名簿に同じ `studentId` の児童が**居る**ときだけ、退避の pf の記録を、その児童の記録として戻す。居なければ「先に児童を復元してください」と通知して何もしない。すでにその児童の**同じ年度・同じ種目に値が入力されている**場合は上書きしない（入力済みの値を優先し、戻せなかった分は退避に残して通知）。トランザクション・取り消しは既存と同じ。`residual-not-restorable` の拒否は、v2 の端末で解除する。
  - 設定の退避カード: `pf-residual` に「復元」ボタン（名簿に児童が居るときのみ有効）を出す。
- **`pf-unlinked`**（16.3 の2）: 完全削除のみ。

### 16.5 取り込み(`grdImportFitnessTest`)

`pf_roster[].studentId` で SPA の児童を**直接**探す（氏名は使わない）。同姓同名の取り込み中止は不要になる。`studentId` を持たない児童（v1 形式・手動名簿）は、v1 形式の端末に限り従来どおり氏名で照合し、**v2 では取り込まずに「引き継ぎが必要」と通知**する。

### 16.6 バックアップ v11・復元

1. **形式**: `version: 11`、`studentIdSchema: 1`、`pfSchema: 2` を追加。`rawLocalStorage`/`data` の中身は変えない（`spa_master` の `studentId`、`pf_*` の v2 形式がそのまま入る）。`spa_roster_snapshot`・`spa_roster_txn` を含めない現行方針は維持。
2. **`importBackup` に版の判別を追加**: `version` と `pfSchema` を読み、(a) v11（v2）→ そのまま復元、(b) v10 以前（v1 形式の pf・`studentId` なしの名簿）→ 復元後の起動時移行（`studentId` の付与、16.3 の pf 移行）に任せる。復元後の再読み込み時に**整合性検査**（`spa_master` の `studentId` 重複・欠落、pf の v1／v2 混在、退避の孤立）を行い、問題があれば設定に警告を出す（保護者向け出力には出さない）。
3. **混在の対策**: `importAll` は「バックアップに無いキーを消さない」ため、v11 の端末へ v10 のバックアップを復元すると、`pf_records_2025`（バックアップに無い年度）が v2 のまま残り、バックアップの `pf_roster` が v1 になる、といった**キー単位の混在**が起きる。16.2 のキーの形による判別で、各キーを正しく扱い、移行は混在を許す（v1 のキーだけを変換、v2 のキーは触らない）。
4. **復元前の自動退避（新規）**: 復元の直前に、復元が上書きするキーの現在の値を `spa_restore_snapshot`（1世代）へ書く。**容量の事前確認**は名簿変更と同じ方式（実際に書いて確認）。容量が足りないときは復元を止めず、「復元前のデータを残せませんでした。先に現在のバックアップを取ることをお勧めします」とトーストで知らせて復元する（災害復旧を止めないため）。復元後、設定に「復元を取り消す」を出し、復元後に対象キーへ入力があれば取り消さない（名簿の取り消しと同じ規則）。`spa_restore_snapshot` はバックアップに含めず、端末データ消去の対象に加える（氏名を含むため）。
5. **書き込みの検証**: 復元も `storageVerifiedWrite` を使い、書き込み失敗（容量超過）を握りつぶさない（現状の `setImmediate` は握りつぶしてキャッシュだけ更新する — 12章のH8と同じ性質）。

### 16.7 `pf.html`（単体ページ）の扱い — **先生の判断が必要**

`pf.html` は内蔵版と同じ処理の**複製**で、同じキーを書く。v2 に対応しないまま残すと、旧ブックマーク等から開いて「名簿を読み込む」を押すだけで `pf_roster` が v1 形式に戻り、v2 の記録が見えなくなる。選択肢:
- **案P1（推奨）: `pf.html` を「アプリのタブへ移動する」だけの案内ページにする**（データに触れない。`index.html#pf` へ誘導。`sw.js` のキャッシュ対象は維持）。重複の解消でテストも1系統になり、v2 を破壊する経路が無くなる。JSONバックアップ機能（`pf.html` の `exportJSON`）は、SPA本体のバックアップ（`pf_*` を含む）に一本化する。
- 案P2: `pf.html` も v2 に書き換える（約28箇所＋JSONバックアップの v2 化＋テスト）。工数・回帰リスクが約2倍。
- 案P3: 触らない（非推奨。上記の破壊経路が残る）。

`pf.html` を単体で使っておらず、`pf_fitness_*` が端末に無い（16.3 の診断表示で確認できる）なら P1 でよい。

### 16.8 実施順序（各段階1コミット以上、承認後に push。実機の挙動が変わるのは 4b 以降）

| 段階 | 内容 | 実機の挙動 | push |
|---|---|---|---|
| 4a | 純関数（`planPfMigration`・氏名での紐付け・不変条件 M1〜M5・v2 の `planPfChange`）と診断表示（読み取りのみ）＋テスト。書き込みなし | 設定に連携状態の1行が増えるだけ | 単独で可（先に push し、iPad の状態を確認してから 4b の方針を確定できる） |
| 4b | 移行の実行（16.3）＋内蔵版 pf の studentId 化（16.2）＋取り込み（16.5）＋v2 の名簿変更・復元（16.4）。**4b の途中で切り離すと pf が読めなくなるため、この4点は同じリリース** | pf 画面の操作は同じ。裏で記録のキーが変わる | 4bが終わってから。`CACHE_VERSION` を上げる |
| 4c | `pf-residual` 単体の復元・`pf-unlinked`・退避カードのUI | 退避の「復元」ボタン | 4b とまとめて可 |
| 4d | バックアップ v11・`importBackup` の判別・整合性検査・復元前の自動退避・書き込みの検証 | 復元の挙動が変わる | 別リリース |
| 4e | `pf.html` の案内ページ化（案P1）または v2 化（案P2） | 単体ページの挙動 | 4b の**後**（先だと v2 を壊す経路が残る） |

### 16.9 テスト計画

1. **移行**: 一致する pf（1〜30名、氏名が空の行あり／手動名簿あり）→ 記録・集計の保存則、`pf_roster` の `studentId`、非対象キー不変。不一致6種（順序違い・氏名違い・id が位置+1でない・`pf_roster` なし・壊れた JSON・余分な児童）で**位置による変換をしない**こと。氏名での引き継ぎ（一意／同姓同名／一致なし）と `pf-unlinked`。二重実行しても変わらない（冪等）。中断・容量超過・内容破損の注入（段階2cの27通り方式）で全キーが移行前と同一。
2. **v2 の名簿変更**: 6操作×（18ストア＋pf）で、pf の記録が `studentId` に付いたままずれ0（並べ替え・転入・改名では記録キー不変、削除は退避のみ）。`pf_roster` の再構築（`pfm_` を残す）。取り消し。
3. **pf-residual の復元**: 一致しない状態で退避 → 移行後に単体復元 → 記録が戻る（保存則）／児童が名簿に居ないと拒否／入力済みの種目は上書きしない。
4. **取り込み**: `studentId` で照合（同姓同名でも取り込める）、v2 で `studentId` 無しの児童は取り込まず通知。
5. **バックアップ**: v11 の往復（書き出し→復元→全キー一致）、v10 の復元→起動時移行、**キー単位の混在**（v11 の端末へ v10 を復元）、復元前の自動退避と取り消し、容量不足時の警告、書き込み失敗の検出。
6. **UI（実際の操作）**: pf の記録入力・保存・並べ替え後の表示・退避カードの復元。**pf.html** は P1 なら「移動案内のみでデータに触れない」ことを確認。
7. **変異テスト**: 一致判定を常に真／`studentId` でなく位置で付け替え／pf-residual が上書きする／検証を外す、などで失敗することを確認。
8. 既存 `roster-pf.test.js`（71件）は v1 形式の前提なので、v1 端末用（移行前）と v2 用に**分割して維持**する。`idb-residue.test.js` の `pf.html` 削除テストは案 P1/P2 に合わせて更新。

### 16.10 リスクと制約

- 位置による自動変換は**一致する場合のみ**。一致しない端末では、既にずれた記録は救えない（引き継げるのは pf 名簿の氏名に付いている記録まで）。
- 旧版アプリ（v1.52.0 以前）に戻すと、v2 の pf は旧版から見て「id が位置+1・記録キーが文字列」となり、名簿変更で pf に追従しない（データは消えない）。ロールバックは非推奨。
- 端末の容量: 移行のスナップショット・復元前の自動退避で、一時的に pf のキーと復元対象キーの写しが増える（容量の事前確認で判定）。
- `pf.html` の扱い（16.7）が決まらないと 4e に着手できない。

### 16.11 先生に決めていただくこと

1. `pf.html` を単体で開いて使っていますか？（アプリ内の「新体力テスト」タブだけなら案P1。単体ページも使うなら案P2）
2. 手動名簿（SPA に居ない児童を pf に手で登録する機能）を使っていますか？（使っていなければ `pfm_` の扱いは最小限で済む）
3. 一致しない端末の引き継ぎは「氏名で引き継ぐ」（プレビュー→ボタン）で足りるか。
4. 復元前の自動退避で、容量不足のときは「警告して復元する」（提案）でよいか、それとも復元を止めるか。
5. 4a（診断表示のみ）を先に push して、先生の iPad の状態を確認してから 4b に進む進め方でよいか。

### 16.12 決定事項（先生の承認・2026-09-19）と実施順の変更

- **16章の設計案を承認**。16.11 は次のとおり確定。
  - **a・b（`pf.html` の単体使用・手動名簿の有無）**: 4a の診断表示の結果を見て決める。4a の表示に次を含める: (1) `pf_fitness_*` の有無（単体の `pf.html` を使った痕跡）、(2) `pf_roster` にあって SPA 名簿にない児童の有無と人数（手動登録の痕跡。**氏名は表示しない**）、(3) `pf_roster` と SPA 名簿の一致・不一致。
  - **c**: 名簿が一致しない端末の引き継ぎは**「氏名で引き継ぐ」（プレビューとボタンで確定）方式で確定**。
  - **d**: 復元前の自動退避で容量が不足した場合は、**警告を出して復元を続行する方式で確定**（復元を止めない）。
  - **e**: **4a を先行**させる（診断表示のみを先に push し、実機の状態を確認してから本体に進む）。
- **実施順の変更（16.8 を置き換え）**: 4a（診断表示・読み取り専用）→ **4d-1（`importBackup` の安全化: 復元前の自動退避・`storageVerifiedWrite` による読み戻し検証・バックアップに含まれない所有キーの扱いの明確化とテスト）** → 4b（移行・pf の studentId 化・取り込み・v2 の名簿変更）→ 4c（`pf-residual` 単体の復元・`pf-unlinked`）→ 4d-2（v11 の印・版の判別・整合性検査）→ 4e（`pf.html`）。復元を先に安全にするのは、4b で pf のキーの形式を変える前に、バックアップからの復元（唯一の逃げ道）を「書き込みの失敗を握りつぶさず、復元前に戻せる」状態にしておくため。
- **今回の範囲**: 4a（push可）と 4d-1（commit まで・push しない）。


### 16.13 4a の iPad 表示結果と 16.11 の a・b の確定（2026-09-19）

- **iPad の表示結果**（v1.52.1 の設定画面「🔎 新体力テストの連携状態」）: 名簿の一致＝**OK（一致しています）**／人数＝SPA **27名**・新体力テスト **27名**／単体ページの使用＝**なし**／SPA の名簿にいない児童＝**なし**／記録＝**2026年度 27名**。（「名簿を変更せずに保存すると『変更はありません』」は先の確認で OK 済み。今回の回答欄は選択肢のまま未記入。）
- **a（単体ページ `pf.html`）**: 使用の痕跡が「なし」のため、**`pf.html` は案内ページにする**（16.7 の案P1）。4e の工数は縮小（v2 化・JSONバックアップの v2 化は不要）。
- **b（手動登録）**: SPA の名簿にいない児童が「なし」のため、**pf の手動登録は移行の対象外**とする（4b では、手入力の児童に `pfm_` の ID を振り、名簿変更・名簿の読み込みで消さない最小限の扱いのみ）。
- **c の実施位置**: 「氏名で引き継ぐ」（プレビュー→ボタン）は、引き継げなかった記録の行き先（`pf-unlinked`）を必要とするため **4c に置く**（4c は `pf-residual` の単体復元・`pf-unlinked` を含む）。先生の端末は名簿が**一致している**ので、4b の自動移行の対象になる。

## 17. H4 案B 段階4a 実施記録（v1.52.1）— 新体力テストの連携状態の診断表示（読み取り専用）

- **目的**: 16.12 の決定（a・b は診断の結果を見て決める）のため、実機（iPad。DevTools なし）の pf の状態を、設定画面で確認できるようにする。**データは一切変えない**。
- **場所**: 設定画面（歯車から開く画面）の「👥 統一名簿マスター」「📦 退避中の児童」の下、「📏 身体データ」の上の「🔎 新体力テストの連携状態（確認用）」カード。設定を開くたびに最新の状態を表示し、「🔄 もう一度確認する」ボタン（44px以上）で再確認する。
- **表示する内容**（氏名は一切表示しない）:
  1. **名簿の一致**: 「一致しています」／「一致していません：理由」（人数が違います／氏名または並び順が違います／番号が名簿の位置と合っていません／新体力テストの名簿がありません／名簿のデータが読み取れません）／「新体力テストのデータはありません」。判定は段階2cと同じ `srPfMatch`。
  2. **人数**: SPAの名簿 N名／新体力テストの名簿 M名。
  3. **単体ページの使用**: `pf_fitness_年度` の有無（あり=年度を表示）。`pf_fitness_*` を書くのは単体ページ `pf.html` の成績転送だけなので、「あり」なら単体ページを使った痕跡（16.1）。
  4. **SPAの名簿にいない児童**: `pf_roster` にあってSPA名簿に氏名が無い児童の人数（人数のみ。手入力で登録した痕跡）。名簿が読めないときは「確認できません」。
  5. **記録**: 年度ごとの記録がある児童数。
- **実装**: `pfDiagnose()`（localStorage を直接読むだけ。書き込み・キャッシュの更新なし。結果は件数・年度・理由コードだけ）と `renderPfDiagCard()`（`updateSettingsUI` から呼ぶ）。保護者向けの出力（PDF・帳票）からは呼ばれない（設定画面の中のカードだけ）。
- **テスト**（`tests/pf-diagnosis.test.js` 37件、すべてPASS）: データなし／一致（人数・年度別の記録数・pf_fitness の痕跡）／pf_fitness なし／pf 名簿にだけいる児童（人数のみ・不一致）／不一致の各理由（順序違い・氏名違い・idが位置+1でない・名簿なし・壊れたJSON）／氏名が空の行がある名簿は「一致」／**読み取り専用**（診断・表示・再確認の間、`localStorage` と `IndexedDB` への書き込み・削除の呼び出しが0回、全キーとメモリキャッシュが1バイトも変わらない）／**氏名を表示しない**（pf 名簿にだけいる児童の氏名・SPA名簿の氏名が、画面にも診断結果にも無い。「児童」など画面の語と重ならない固有の氏名で確認）／画面（設定の中にカードがある・再確認ボタンのタップ領域・実際にタップして表示が更新される・自動では再計算しない）／確認ダイアログ0回。
- **変異テスト**（実装を一時的に壊して失敗を確認。確認後に戻した）: 診断が localStorage に書く→2件失敗／画面に pf 名簿の氏名を出す→2件／SPAにいない児童を常に0→2件／一致判定を常に一致→10件／pf_fitness を数えない→2件／再確認ボタンが何もしない→1件。
- **設計上の判断**: 段階4aは診断表示のみに絞り、移行の純関数（`planPfMigration` 等。16.8の旧4a）は 4b で実装する（診断の結果で 4b の方針を確定するため）。
- **次**: 先生が iPad の設定画面で表示を確認 → 結果で 16.11 の a・b を決める。並行して 4d-1（`importBackup` の安全化）。

## 18. H4 案B 段階4d-1 実施記録（v1.52.2）— バックアップの復元（`importBackup`）の安全化

- **なぜ 4b の前にやるか**: 4b で pf のキーの形式を変える前に、バックアップからの復元（唯一の逃げ道）を「書き込みの失敗を握りつぶさず、復元前に戻せる」状態にしておくため（16.12）。
- **従来の復元の問題**（実装の読み取りとテストで確認）: (a) `StorageManager.importAll`（`setImmediate`）は容量超過を握りつぶしてキャッシュだけ更新するため、書けていないのに「復元しました」と出る。(b) 復元前の状態を残さないため、途中で失敗・中断すると新旧が混ざったまま戻せない。(c) バックアップに無いキーは消さないため、名簿が違うバックアップを復元すると、端末に残った児童別データ（添字キー）が復元後の名簿の**別の児童のもの**として表示される（既存の設計 7.4 の3で予告していた「新旧の混在」）。(d) 同じオリジンの別アプリのキー（`doc-index-v1` 等）まで、バックアップの古い値で上書きする。(e) 形式が違うファイル（`{}` など）でも何も復元せず「復元しました」と出る。(f) 復元の前に保存待ち（デバウンス中）の値を確定せず、復元後に古い値で書き戻される恐れがある。
- **実装**（`brPlanRestore` / `brRestoreFromBackup` / `buildBackupObject`。書き出しの中身は変えず、テストのため `exportBackup` から切り出しただけ）:
  1. **計画（書き込みなし）**: 形式の検証（不正なら**何も書かず**「データは変更されていません」で中止: バックアップがオブジェクトでない／データ層が無い／値が文字列でない／名簿が読めない・`students` が無い）。書くキー（端末と値が違うもののみ）・消すキー・触らないキーを決める。
  2. **復元前の自動退避**: 名簿変更と同じスナップショット `spa_roster_snapshot`・ジャーナル `spa_roster_txn`（`kind:'backup-restore'`）に、**復元が変えるキーの現在値**（復元前に無いキーは null）を保存する。新しいキーは作らない。すべて**検証付き書き込み**（localStorage を直接読み戻す）。書き込み → 消去 → 検証（書いたキーの一致・消したキーの不在・**変えないアプリのキーの不変**・名簿が読めること）→ 確定（ハッシュ）。失敗したらスナップショットへ書き戻し、**復元前の取り消し点（あれば）も元に戻す**。途中で強制終了しても、次の起動（`recoverRosterTxnOnStartup`）で自動的に復元前へ戻る。
  3. **容量が足りず退避できない場合（決定 d）**: 容量の事前確認（名簿変更と同じ実測方式）またはスナップショットの書き込みが失敗したら、**警告を出して復元を続行**する（`mode:'no-snapshot'`）。この場合は**消す操作は行わず**（戻す手段が無いため）、検証付き書き込みだけを行う。書き込みが壊れたキーは1回だけ再試行し、それでも書けないキーがあれば `partial-restore` として件数を報告する（一部だけ復元された状態であることを明示）。以前の取り消し点は壊さない。
  4. **保存待ちの扱い**: 復元の前に保存待ちを確定し、復元で書いたキーの保存待ち（古い値）は破棄する。
  5. **復元の取り消し**: 名簿変更の取り消し（`undoRosterChange`）を共用。復元後に対象キーへ入力があれば取り消さない（専用の文言）。名簿カードのボタンは「↩ 直前のバックアップの復元を取り消す」と表示。再読み込み後は、警告（あれば）のあとに「✅ バックアップを復元しました［取り消す］」のトースト。**再読み込み後の最初の起動で、起動時の移行（児童IDの付与など）が復元したキーを書き換えていても取り消せるよう、取り消し用ハッシュを一度だけ取り直す**（古い形式のバックアップで、入力があったと誤判定しないため）。
- **バックアップに無いキーの扱い（明確化）**:

| キーの種類 | バックアップに無いとき | バックアップにあるとき |
|---|---|---|
| アプリのキー（`KEYS`・`pf_*`・`spa_`/`migration_` 等）で児童別データ以外 | **触らない**（従来どおり） | 値が違うときだけ書く |
| 児童別データ（登録表の18ストア・`pf_roster`）| **名簿（氏名の並び）がバックアップと端末で違うときだけ消す**（残すと別の児童のデータとして表示されるため。退避に入っているので取り消しで戻る）。名簿が同じなら触らない。バックアップに名簿が無ければ触らない | 値が違うときだけ書く |
| pf の記録・集計（`pf_records_*`・`pf_fitness_*`）| 上記に加え、**pf の名簿がバックアップと違うときも消す**（pf の id は pf 名簿の位置のため） | 値が違うときだけ書く |
| 別アプリのキー（`doc-index-v1`・task 系など） | 触らない | **端末に無いときだけ書く**（既にあれば触らない。バックアップの古い値で別アプリの新しいデータを壊さない） |
| 復元しないキー | — | 名簿変更のスナップショット・ジャーナル、容量確認の一時キー、退勤モードの印（古い取り消し点・中断の印が復元先で誤動作するため） |

- **既存の挙動の変更（先生に影響する点）**: (1) 形式が違うファイルは、従来は「復元しました」と出て何も復元されなかったが、今後は「復元できません（データは変更されていません）」と出る。(2) 名簿が違うバックアップを復元すると、バックアップに無かった児童別データは消える（取り消しで戻る）。(3) 別アプリのキーは、端末に既にあれば上書きされない。(4) 復元は取り消せる（1世代。次の名簿変更・復元で置き換わる）。復元前の状態を残す空き容量が無いときは、警告が出て取り消せない。
- **テスト**（`tests/backup-restore.test.js` 71件、すべてPASS）: 不正な形式8種で書き込みゼロ／計画は書き込まない／レイヤーの優先順／復元しないキー（悪意のある `applying` ジャーナルを復元しても次の起動で名簿が消えない）／正常系（全キーがバックアップの値・変わらないキー・取り消しで復元前と1バイトも変わらない・入力後は取り消さない）／バックアップに無い児童別キー（名簿が違う→消して取り消しで戻る／同じ→消さない／pf 名簿が違う／バックアップに名簿が無い）／別アプリのキー（既にあれば不変・無ければ書く）／**失敗注入**: 容量超過15通り・内容破損15通り（各書き込み位置）・強制終了18通り（書き込みと消去の各位置。再起動で自動復旧し全キー同一）／退避できない（容量確認の失敗・スナップショットの失敗）で警告して続行・消さない・以前の取り消し点を壊さない／退避なしで1キーが常に書けない→`partial-restore`／退避なしで書き込みが1回壊れる→再試行で回復／保存待ちの古い値／古い形式（`appType` なし・児童IDなし）のバックアップ・再読み込み後の取り消し（ボタン・トースト・表記）／名簿が壊れて読み込みに失敗している端末への復元／**実際の画面**（バックアップ画面のファイル選択→復元ボタン→再読み込み後の通知・取り消し、不正なファイル3種、容量が足りない端末での警告）／書き出しの内容（version 10・スナップショットを含めない）。確認ダイアログ0回。
- **変異テスト**（実装を一時的に壊して失敗を確認。確認後に戻した。13件すべて検出）: 検証付き書き込みをやめ読み戻し検証も外す→2件失敗／退避を取らない→15件／児童別キーを消さない→6件／退避なしでも消す→2件／復元しないキーの除外をやめる→1件／保存待ちの確定・破棄をやめる→1件／別アプリのキーを常に上書き→3件／失敗してもロールバックしない→4件／起動時の自動復旧をやめる→2件／取り消しハッシュの取り直しをやめる→4件／値の型の検証をやめる→1件／名簿が同じでも消す→3件／取り消しの種別判定をやめる→2件。**検出できない変更**: 「変わらないキーを書き込まない」最適化（書いても結果が同じ）。
- **制約・未対応**: (1) 取り消し点は1つだけで、名簿変更と復元で共用（復元すると直前の名簿変更の取り消し点は置き換わる）。(2) 復元前の退避は、復元が変えるキーの写しのため、容量が足りないと取れない（その場合は警告して続行）。(3) 別アプリのキーの書き込みは検証なし（失敗しても復元は止めない）。(4) バックアップの `version`（10のまま）・`pfSchema` の印と版の判別は 4d-2。(5) 暗号化ファイルのパスワード入力は従来のまま。
- **登録漏れガード**: 新しい sessionStorage キー `spa_restore_notice_after_reload`（復元・取り消しの再読み込み後の通知）を `student-remap.test.js` の「既知の非対象キー」に理由つきで追加した（`spa_*` リテラルの登録漏れ検出が、新しい名前を検出して知らせた）。
- **設計上の発見**: 復元後の最初の起動で `migrateStudentIdsV1` が復元した `spa_master` を書き換えるため、復元直後のハッシュのままだと「復元後に入力があった」と誤判定して取り消せなくなる（古い形式のバックアップで再現）。取り消し用ハッシュを再読み込み後の最初の起動で取り直して解決。
- **次**: 4b（移行・pf の studentId 化・取り込み・v2 の名簿変更）。4a の診断表示の結果（a・b）を先に確認する。

## 19. H4 案B 段階4b 実施記録（v1.53.0）— 新体力テスト(pf)の児童ID化（移行・名簿変更・復元・取り込み・pf画面）

- **範囲**: (1) 起動時の移行（旧方式→新方式）、(2) 新方式での名簿変更・復元、(3) 取り込み `grdImportFitnessTest`、(4) 内蔵版 pf 画面、(5) 4a の診断カードの拡張。単体ページ `pf.html` は 4e（案内ページ化）。`pf-residual` の単体復元・`pf-unlinked`・「氏名で引き継ぐ」は 4c。
- **データ形式（v2）**: `pf_roster[]` の各項目が `studentId` を持ち、`pf_records_年度`・`pf_fitness_年度` のキーが **`studentId`**。`id` は表示用の通し番号（位置+1）に格下げし、記録のキーには使わない。判別は**キーの形**（`srPfIsV2` / `srPfMode`: `none`/`v2`/`v1`）で行い、`v2` の条件は「名簿の全項目が studentId を持ち、記録のキーに数字だけのものが無い」。
- **起動時の移行 `migratePfStudentIdV1`**（`migrateStudentIdsV1` の直後）:
  - **計画 `planPfMigration`（純関数・書き込みなし）**: `pf_roster` がSPA名簿と**氏名・順序・id(位置+1)とも一致**（`srPfMatch`）するときだけ、id→その位置の児童の `studentId` へ変換。**変換しない（何も書かない）**: 一致しない各理由／記録に名簿に無い id（手入力・過去の削除の取り残し）／記録が壊れている・名簿に無い文字列のキー／名簿の studentId がSPAの位置の児童と食い違う／同じ児童の数字キーと studentId キーが両方ある（衝突）／SPAの児童に studentId が無い。不変条件 **M1〜M6**（件数の保存則・名簿の studentId の一意性と実在・位置の児童の氏名/studentId の一致・記録のキーが名簿内・名簿の他項目不変・記録の値が1バイトも不変）。
  - **実行（名簿変更・復元と同じ仕組み）**: 容量確認（実測。足りなければ**何も書かずに見送り**、旧方式のまま動く）→ 移行前に**端末の全キーのハッシュ**を取る → スナップショット（`kind:'pf-migration'`）とジャーナル → pf の4キーを検証付き書き込み → 検証（書いたキーの一致・**pf 以外の全キーが移行前と1バイトも変わらないこと**・**新しいキーが作られていないこと**・M1〜M6）→ 完了の印 `migration_pfStudentId_v1`（記録数・比較した他のキー数）→ スナップショット・ジャーナルを掃除（それ以前の取り消し点があれば元に戻す）。**失敗したらスナップショットへ書き戻し**（記録は移行前のまま）、次の起動で再試行する。途中で強制終了しても、次の起動の `recoverRosterTxnOnStartup`（loadMaster より前）が元に戻し、同じ起動で移行がやり直される。成功時は「新体力テストの記録を、児童ごとに管理する新しい方式に更新しました（内容は変わっていません）」を通知。
  - **iPad 上の移行前後の全キー比較の見え方**: 診断カード（設定）の「記録の管理方式」に「児童ごと（新方式）（○月○日に更新：記録 N件。更新の前後で、ほかの全データ M件は変化なし）」と表示される（M は移行前後で比較したキーの数）。旧方式のままなら、理由（一致しない／更新できていない）が出る。
- **新方式での名簿変更 `planPfChangeV2`**: 記録のキーは studentId なので、**並べ替え・転入・改名では記録・集計のキーを1バイトも書き換えない**。削除された児童の記録・集計・pf_roster の項目だけを退避（`spa_student_archive`、`pf_*` として。旧方式の退避と同じ形）へ移す。`pf_roster` は新しいSPA名簿から作り直す（`id`=位置+1、`studentId`、age・note など pf 側の項目は引き継ぎ、転入生は age=10、SPAの児童ではない項目＝手入力の `pfm_` は末尾に残す）。不変条件 **P1〜P3・P6・P7**（件数の保存則・削除された児童の記録が残らない・`pf_roster` が新名簿と一致し削除された児童がいない・転入生に記録が付かない・生き残る児童の記録の値が不変）。新方式では「pf の名簿が一致しない」通知は出ない。
- **復元 `restoreArchivedStudent`（新方式）**: 一致判定なしで、同じ studentId の記録として戻す（`pf_roster` にも項目を戻す）。**すでにその児童の記録が入力されていれば上書きしない**。旧方式（一致する端末）は従来どおり位置で戻し、一致しなければ `pf-residual`（4c で復元可能にする）。
- **旧方式の端末（名簿が一致せず移行しない）**: 従来どおりに動かす（名簿変更は段階2cの位置での追従、`pf_loadFromSpa` は studentId を付けない、取り込みは氏名照合）。**新旧が混在**（名簿は studentId 付きだが数字キーの記録が残る）した場合は、pf に触れず通知する（`mixed-format`）。
- **取り込み**: `pf_roster` の項目が studentId を持てば、**studentId でSPAの児童を探す**（氏名は使わない。**同姓同名でも取り込める**）。studentId が無い項目は従来どおり氏名で照合し、同姓同名は中止。
- **内蔵版 pf 画面**: 記録・集計のキーを `pf_key(s)`（studentId、旧方式は id）にした（入力・合計・個人分析・統計・CSV）。画面の入力欄・分析の選択肢は文字列のキー。「名簿を読み込む」は、旧方式の記録が残っていなければ studentId を付け、pf 側で直した年齢と手入力の児童（`pfm_`）を引き継ぐ。手入力の名簿（`pf_applyManualRoster`）は旧方式の記録が無ければ `pfm_` の ID を振る。
- **診断カード（4a）の拡張**: 「記録の管理方式」（新方式／旧方式・移行の結果）を追加。記録の人数は新方式でも数える。
- **テスト**（`tests/pf-migration.test.js` 116件、すべてPASS）: 計画（一致・氏名が空の児童・冪等・移行しない13種）／起動時の移行（27名・氏名が空の1名・**移行前後の全キー比較**: 変わったのは pf の4キーだけ・pf 以外の全キーが1バイトも不変・期待値は**テスト側で独立に計算**・通知・冪等・IndexedDB(鏡)との一致・診断カード）／移行しない端末（5種で全キー不変）／**失敗注入**（容量超過・内容破損・強制終了を pf の各書き込み位置4通り、強制終了→再起動での復旧と再移行、容量確認の失敗・スナップショットの失敗で何も書かない、**移行中にほかのキー・新しいキーが作られたことを検出してロールバック**）／新方式の名簿変更 **6操作**（記録・集計が持ち主に付いたまま・記録キー不変・削除は退避・`pf_roster` の再構築と age/note・核の18ストアもずれ0・取り消しで全キー復元・復元・入力済みを上書きしない）／手入力の児童の保持／新旧混在／旧方式の端末の動作維持／取り込み（同姓同名・並べ替え後・旧方式の中止）／pf 画面の実操作（入力→保存・名簿の並べ替え(UI)後の表示・個人分析・名簿の読み込み・旧方式の記録が残る端末）／旧方式のバックアップの復元→再移行。既存 `roster-ui.test.js`（67件。新方式のキーであることの確認を1件追加）は pf の照合を新旧両方式に対応させ（起動時に移行されるため実機と同じ新方式で検証する。pf-residual の場面は、新方式のまま名簿だけ旧方式の形・逆順に壊して再現）、`backup-restore.test.js`（71件・対象は復元）は起動時の移行を無効にした。既存 `roster-pf.test.js`（71件・旧方式の動作の検証）は、起動時の移行を無効にするテスト専用フック `window.__spaSkipPfMigration`（再読み込みをまたいで旧方式を検証するため。画面や保存には現れない）を使うよう変更した。
- **変異テスト**（実装を一時的に壊して確認。確認後に戻した。15件すべて検出）: 位置の対応が1つずれる→2件失敗／一致しなくても位置で変換→4件／移行後の全キー比較を外す→1件／失敗時にロールバックしない→5件／削除した児童の記録を退避せず残す→3件／名簿の age・note を引き継がない→5件／復元で入力済みを上書き→1件／取り込みが氏名照合→2件／pf 画面のキーが常に旧 id→4件／記録の値を書き換え検証も外す→1件／新旧混在の保護を外す→1件／旧方式の端末でも studentId を付ける→1件／容量確認を外す→1件／起動時に移行を呼ばない→7件／新方式を判別できない→9件。
- **制約・リスク**: (1) **`pf.html`（単体ページ）は 4e まで旧方式のまま**。アプリ内のどこからもリンクされておらず使用の痕跡もないが、URL を直接開いて「名簿を読み込む」を押すと `pf_roster` が旧方式に戻り、新方式の記録が見えなくなる。開かないこと。4e で案内ページにする。(2) 旧方式の端末（名簿が一致しない）で pf の「名簿を読み込む」を押すと、従来どおり記録が別の児童に付く（10.5の既存の不具合）。次の起動で、その状態を忠実に新方式へ移行する（新たなずれは作らない）。診断カードで事前に分かる。4c の「氏名で引き継ぐ」で解消する。(3) 旧版（v1.52.x 以前）へ戻すと、新方式の pf は旧版から追従されない（データは消えない）。非推奨。(4) バックアップの形式は version 10 のまま（4d-2 で印を付ける）。旧方式のバックアップを復元すると、次の起動で自動的に再移行される（テスト済み）。
- **次**: 4c（`pf-residual` の単体復元・`pf-unlinked`・「氏名で引き継ぐ」）→ 4d-2 → 4e。

## 20. 4b の push 前の追加対応（v1.53.1）— 一時的なテスト失敗の原因調査・起動直後の競合・`pf.html` の暫定ガード

### 20.1 `student-id-migration.test.js` が全体実行で一度だけ失敗した件（原因: 通知が保存ボタンへのタップを受けていた）

- **調査の方法**: 起動時の処理は、テストのコードを変えずに調べられるよう、`localStorage`・IndexedDB への全書き込みと、クリック・通知の変化を、時刻と呼び出し元の関数名つきで記録する調査用の仕組み（一時的。リポジトリには入れない）を作り、失敗した瞬間に全ページの状態（localStorage・メモリキャッシュ・IndexedDB の食い違い、起動ごとの書き込みログ）を保存した。
- **再現**: 単独実行20回（調査用の記録を有効にして順番に）で**5回失敗**（すべて同じ検査「末尾に転入生を追加」）。CPU に負荷をかけて（負荷用の処理を8本走らせて）8回実行すると3回失敗。負荷が無い静かな状態では、同じ実行を何十回繰り返しても失敗しにくい（=負荷のときだけ起きる）。最初の全体実行での1件の失敗は、当時は検査名を記録していなかったため断定できないが、同じ検査で、負荷が高い状態（他のテスト・調査が同時に動いていた）と考えられる。
- **失敗時の記録が示したもの**: 失敗した3回とも、同じ順序で、最後のクリック（保存ボタンを押す操作）が **`#toast`（画面下の通知）の上に着地**しており（`click #toast@212,772 756x54 at 251,776`）、保存ボタン（位置 124,750・256×52。中心は 251,776）には届いていなかった。保存の処理は一度も呼ばれず、名簿は4名のまま。
- **原因**: 名簿の編集を始めると「名簿を変更する前に、バックアップを取っておくと安心です」の通知が約2.5秒、画面の下端中央に出る。通知は `position: fixed` で、タップを受ける設定（pointer-events の指定なし）だった。名簿が長くなって**保存ボタンが画面の下端（通知の帯）に来ると、通知が保存ボタンに重なり、そこをタップしても通知に当たって何も起きない**。テストは通知の表示中に押したために失敗した。**実機（iPad）でも起こり得た問題**（名簿を編集した直後の約2.5秒に、保存ボタンが通知の下に隠れていると、1回目のタップが空振りする）。起動時の競合ではない。
- **修正（別コミット）**: 通知は下の要素へのタップを受けない（`.toast { pointer-events: none }`）。通知の中の「元に戻す」ボタンだけがタップできる（`.toast-undo-btn { pointer-events: auto }`）。見た目・表示時間は変えていない。
- **テスト**（`tests/toast-passthrough.test.js` 8件）: 保存ボタンの真上に通知が重なる状態を**決定的に**作り（画面の高さを保存ボタンの位置に合わせる）、実際の座標をタップして保存されること、タップの対象が保存ボタンであること、通知が pointer-events: none・「元に戻す」が auto であること、「元に戻す」を実際のタップで押せること、通常の通知の位置のタップが下の要素に届くこと。**修正前は3回とも失敗**（決定的な再現）、修正後は全件PASS。変異テスト: 通知の設定を外す→4件失敗／「元に戻す」だけタップできる設定を外す→3件失敗。

### 20.2 起動時の処理の実行順・await の関係と、競合の調査

- **実行順（記録で確認）**: `DOMContentLoaded` の中で**すべて同期的に**、この順に実行される: ① 容量確認の一時キーの掃除 → 名簿変更の復旧（`recoverRosterTxnOnStartup`。loadMaster より前）→ 各種の旧データの移行 → `loadMaster` → **児童IDの付与**（`migrateStudentIdsV1`）→ **pf の新方式への移行**（`migratePfStudentIdV1`）→ 画面の更新・通知。この間、IndexedDB は未準備なので、検証付きの書き込みは localStorage とメモリキャッシュだけに書き、検証付きの削除は「準備後に鏡から消す」一覧 `__pendingIdbDeletes` に積む。② そのあと**非同期**: `initIDB`（DBを開く）→ `migrateFromLS`（localStorage の全キーを鏡へ put し、トランザクションの完了を待つ）→ `loadCache`（鏡を getAll してキャッシュへ読み込み、**IndexedDB の一回限りの掃除**を行い、完了の印を書く）→ `flushPendingIdbDeletes`（一覧のキーを鏡・キャッシュから消す）。実測では、①は約14ms、②は約8ms（`Phase C` 到達まで約22ms）。
- **競合の検討結果**: (a) 児童IDの付与・pf の移行・復旧は同じ同期処理の中で完結し、await をまたがない。(b) `migrateFromLS` は実行時点の localStorage を読み、IndexedDB のトランザクションは作成順に処理されるため、①と②の間や、`migrateFromLS` と `loadCache` の間に書かれた値も、`loadCache` の getAll に反映される（キャッシュが古い値で上書きされることはない）。(c) 一回限りの掃除は「localStorage に無い所有キー」だけを鏡から消し、判定時点の localStorage を読むため、直前の書き込みを消さない。(d) **見つかった競合（1件）**: `flushPendingIdbDeletes` が、IndexedDB の準備前に**同じキーが書き直されていても**、一覧のキーを鏡・キャッシュから消していた。localStorage には新しい値があるのに、キャッシュ（StorageManager から見える値）と鏡から消える。対象は起動時に検証付きで削除されるキー（名簿変更のスナップショット・ジャーナル・容量確認の一時キーなど）に限られ、通常の使用ではほぼ起きないが、IndexedDB の準備が遅い（負荷が高い）起動で、準備前に名簿変更などが書かれると起こり得る。
- **修正（別コミット）**: 一覧のキーのうち、localStorage に値が戻っているもの（書き直されたもの）は消さない。書き直されていないものは、これまでどおり鏡・キャッシュから消す。
- **テスト**（`tests/boot-race.test.js` 8件）: IndexedDB の準備完了を800ms遅らせる（遅い起動の再現）→ 準備前にスナップショットを書き直す → 準備後に localStorage・キャッシュ・鏡が一致すること。書き直されなかったキーはこれまでどおり消えること、無関係のキーは影響を受けないこと。**修正前は「キャッシュが null・鏡に無い」で決定的に失敗**、修正後は全件PASS。変異テスト: 書き直しの判定を外す→2件失敗／一覧を無視して常に消さない→1件失敗。

### 20.1a 修正後の検証（調査用の記録を有効にして実行）

- `student-id-migration.test.js` の**単独実行20回**: 20回すべてPASS（37件・0失敗）。修正前は同じ条件（同じ調査用の記録）で**20回中5回**失敗していた。
- **全テストの全体実行5回**: 5回とも 54ファイル・**1517件PASS・0FAIL**・すべての終了コードが0、`attendance-snapshot.js` も5回とも基準と一致。失敗時の状態を保存する仕組みは、1度も作動しなかった（失敗の記録ファイル0件）。
- 修正前の全体実行（調査の途中で中断）と、並列2本での負荷実行（10回×2）の結果: 並列実行では別の原因（同名の一時ファイルの衝突: `sid-restore-<時刻>.json` を2本が同じミリ秒で作る）による失敗も出たが、これは並列に走らせたための副作用でテストの不具合ではない（単独・順番の実行では起きない）。同じ検査の失敗は並列でも2件あり、原因は同じ通知の重なりだった。
- 調査用の仕組み（書き込みの記録・失敗時の状態保存）と一時ファイルは、リポジトリに入れていない。

### 20.3 `pf.html` の暫定ガード（4e の前倒しの最小版・別コミット）

- **目的**: 4b で新体力テストの記録が新方式（児童ID）になったため、旧方式のままの単体ページ `pf.html` を URL 直接などで開いて「名簿を読み込む」を押すと、`pf_roster` が旧方式に戻り、新方式の記録が見えなくなる（16.7・19章の制約）。これを、検出して案内だけを出すことで防ぐ。
- **動作**: `pf.html` の先頭のスクリプトが、**読み取りだけ**で新方式を検出する（いずれか: `pf_roster` に `studentId` を持つ項目／記録・集計に数字でないキー／移行の完了の印 `migration_pfStudentId_v1`）。検出したら、localStorage・IndexedDB に**一切書き込まず**、「新体力テストは、アプリの中で使ってください。アプリ内の新体力テストタブを使ってください」という案内（「記録は何も変えていません」の説明と、アプリを開くボタン）だけを表示し、`pf.html` 本体のスクリプトは最初の行で止める（入力欄・保存・名簿の読み込みは存在しない）。念のための二重の防御として、案内表示中は `localStorage` の書き込み・削除・クリアと IndexedDB を開く操作を無効にする。旧方式の端末・データなしの端末では、ガードは働かず、これまでどおり使える。
- **テスト**（`tests/pf-guard.test.js` 46件）: 新方式の6種類の状態（名簿に児童ID・記録も児童IDのキー／新旧混在／集計だけ／名簿に児童IDのみ／手入力の児童 `pfm_`／完了の印だけ）で、案内だけが表示されること、**書き込み・削除・DBを開く操作が0回**（書き換えできない監視で確認）、開いたあとで localStorage と IndexedDB の全キーが1バイトも変わらないこと、本体の書き込み関数（保存・名簿の読み込み・削除・転送など）や直接の書き込み・削除・DB操作を試みても全キーが変わらないこと。旧方式・データなし・壊れた名簿の端末では従来の画面で動くこと・従来どおり保存できること。変異テスト8件すべて検出（検出をやめる／記録のキーでの検出をやめる／本体を止めず無効化もしない／localStorage の無効化をしない／IndexedDB の防御をしない／完了の印での検出をやめる／旧方式でも案内を出す／案内を出さない）。
- **未対応（4e で行う）**: `pf.html` 本体を案内ページに置き換える整理（JSONバックアップ機能の扱いなど）。暫定ガードは、新方式のデータがある端末でだけ働く。

## 21. H4 案B 段階4c 実施記録（v1.54.0）— 旧方式の記録の「氏名で引き継ぐ」・`pf-residual` の単体復元・`pf-unlinked`

- **設計の確認**（16.3・16.4・16.12・16.13 と現行コードの突き合わせ。判断を仰ぐ項目なし）: 引き継ぎは 16.3 の2（プレビュー→ボタンで確定・1回の操作=1トランザクション・取り消し可・引き継げない分は `pf-unlinked` として退避）、復元は 16.4（名簿に同じ `studentId` の児童がいるときだけ・入力済みは上書きしない・戻せなかった分は退避に残す）のとおり。先生の端末は名簿が一致して自動移行済み（v1.53.1・iPad で確認済み）のため、4c の機能は今の端末では表示されない（一致しない端末・過去に退避した `pf-residual` がある端末のための備え）。
- **氏名で引き継ぐ（`planPfTakeover` / `applyPfTakeover` / `pfTakeoverState`）**
  - **対象**: 旧方式(v1)で、自動では新方式に移行されない（`planPfMigration` が skip）端末だけ。自動で移行できる端末・新方式・データなしの端末には案内を出さない。
  - **紐付け**: pf 名簿の各児童を、**氏名（前後の空白は無視）がSPA名簿にちょうど1人だけあり、かつ pf 名簿側にも1つだけ**のとき、その児童の `studentId` へ紐付ける。同姓同名（どちらの側にいても）・SPAにいない氏名は紐付けない。新旧混在で、名簿の項目がすでにSPAの `studentId` を持つときはそれを優先。手入力の児童（`pfm_`）は紐付けせず、そのまま残す。これは pf 画面で先生が見ている「id→氏名」の対応（pf_roster 内で自己整合）をそのまま引き継ぐ方式で、過去の「名簿を読み込む」で既にずれた記録（id が振り直されたあとに入力された分）は、名前からは判別できず救えない（19章の制約 (2) と同じ）。
  - **書き込み**: 記録・集計のキーを、紐付けた児童の `studentId` に付け替える。`pf_roster` は新しいSPA名簿から作り直す（順序・氏名・id=位置+1・`studentId`。age・note は氏名で引き継ぎ、pf に居なかったSPAの児童は age=10）。**紐付けられなかった児童の記録・集計・`pf_roster` の項目は消さず**、退避 `spa_student_archive` に `reason:'pf-unlinked'`（`studentId` なし・氏名つき／名簿に無い番号だけの記録は氏名なし）として残す。名簿（`spa_master`）・18ストア・別アプリのキーには触れない。
  - **不変条件 T1〜T5**: 件数の保存則（引き継ぎ後＋退避＝引き継ぎ前）／記録のキーが名簿内／`pf_roster` のSPAの児童が新名簿と一致／紐付けた児童の記録の値が1バイトも不変／退避した記録の値が元と同一。計画は書き込みゼロ（テストで確認）。同じ児童の数字キーと `studentId` キーが両方ある衝突・名簿の番号の重複・名簿や記録の破損・SPAの児童に `studentId` が無い・1人も紐付かない場合は、何も書かずに理由を返す。
  - **実行**: 名簿変更・復元と同じトランザクション（容量確認・スナップショット・ジャーナル `kind:'pf-takeover'`・検証・ロールバック・起動時の自動復旧）。**取り消せる**（引き継ぎ後に対象キーへ入力があれば取り消さない専用の文言）。再読み込み後に「✅ 新体力テストの記録を引き継ぎました［元に戻す］」。
  - **画面（新体力テスト → 設定タブ）**: 該当する端末だけ「🔁 旧方式の記録の引き継ぎ」のカード。最初は「🔍 引き継ぎの内容を確認する」だけ（何も書かない）。確認すると、引き継ぐ児童の氏名・引き継げない分（氏名つき／番号のみ・記録件数）を表示し、「✅ この内容で引き継ぐ」「やめる」（ともに44px以上）。確認ダイアログは使わない。氏名は先生向けの設定画面にだけ出る（保護者向けの出力には出ない）。診断カード（設定）の旧方式の説明にも「引き継ぎ」の案内を追加。
- **`pf-residual` の単体復元（`restoreArchivedResidual`）**: 旧方式で pf 名簿が不一致のまま児童を戻したときに退避へ残した pf の記録（`residual:true`）を、**名簿に同じ `studentId` の児童がいて、pf が新方式のとき**だけ、その児童の記録として戻す。**入力済みの項目は上書きしない**（記録は項目ごとに、まだ無いものだけ戻す。集計は入力済みなら戻さない。同じ値は残さない）。戻せなかった分は退避に `residual` のまま残し、残りが無ければエントリを消す。戻せるものが無ければ `nothing-to-restore`。`pf_roster` に項目が無ければ、名簿の順序の位置に（age・note も）加える。名簿にその児童がいなければ `student-not-in-roster`（「先に、その児童を名簿に戻してください」）、旧方式なら `pf-not-v2`。不変条件 Q1〜Q3（戻した記録が入っている・他の児童は不変・名簿の項目が入っている）。`kind:'pf-restore'`・取り消し可。
- **退避カード（設定）**: `pf-residual` の行に、条件が揃うときだけ「↩ 戻す」（44px以上）。揃わないときは「先にその児童を名簿に戻すと、記録を戻せます」／「新体力テストが新しい方式になると戻せます」と案内し、完全削除のみ。`pf-unlinked` の行は「（氏名）の新体力テストの記録（引き継げなかった分）」で完全削除のみ（2回タップ）。退避中の児童の数には数えず「その他の退避データ」に数える。名簿カードの取り消しボタンは種別ごとの表記（「新体力テストの引き継ぎを取り消す」など）。
- **テスト**（`tests/pf-takeover.test.js` 72件、すべてPASS）: 計画（全員一致・SPAにいない氏名・記録だけの番号・両側の同姓同名・空白・移行しない各理由・新旧混在・手入力の児童。期待値はテスト側で独立に計算。書き込み0回）／実行（**全キー比較**: 変わったのは pf の4キーと退避だけ・名簿と18ストアは1バイトも不変・退避の中身・診断・ジャーナルの種別・取り消しで全キー復元・入力後は取り消さない・引き継げない状態では何も書かない）／**失敗注入**（容量超過・内容破損・強制終了を各書き込み位置6通り。起動時の復旧で全キー同一）／画面の実操作（表示条件・確認だけでは書かない・やめる・確定→再読み込み後のトースト・退避カード・2回タップ削除・取り消しボタン・タップ領域）／`pf-residual` の復元（正常・age/note・他の児童不変・全キー比較・取り消し・入力済みを上書きしない・同じ値・戻せない各状態・失敗注入18通り・画面）。
- **変異テスト**（13件すべて検出）: 同姓同名でも紐付ける→2件失敗／退避せず捨てる→7件／空白を無視しない→1件／age・note を引き継がない→1件／復元で入力済みを上書き→2件／名簿にいなくても復元→1件／旧方式でも復元→1件／案内を自動移行できる端末にも出す→1件／案内を出さない→3件／「戻す」を常に出す→1件／pf-unlinked の表示をしない→1件／取り消しを通常扱い→2件／記録の値を書き換え検証も外す→2件。
- **テストが見つけた実装の不具合**: `window.rosterErrorMessage = function(r) { return rosterErrorMessage(r); }` が、グローバルの関数宣言自体を自分自身へ差し替え、無限再帰になった（関数宣言はグローバルでは `window` のプロパティ。ラッパーを作らず削除）。
- **登録漏れガード**: 新しい sessionStorage キー `spa_settings_flash_title`（復元後の設定画面のトーストの文言）を `student-remap.test.js` の「既知の非対象キー」に追加。
- **制約**: (1) 一致しない旧方式の端末が無いと、引き継ぎの画面は出ない（先生の端末は一致済み）。(2) 引き継ぎ後の取り消し点は名簿変更・復元と共用の1つ。(3) 引き継ぎ・復元の再読み込み後は既定の画面に戻る（引き継ぎ後は取り消しのトースト）。

### 21.1 `/code-review` の指摘への対応と `test_grades.js` の既知の失敗

- レビューで、`HANDOFF.md` の「次の一手」が iPad 確認済みの記述と矛盾、`UI_WORKLOG.md` の35回目が「未push」のまま、`HANDOFF.md` のテスト実行手順の `cd` が背景実行と混ざる、全体実行の説明が `*.test.js` だけで `test_grades.js` を含まず件数も README と食い違う、の4点を指摘された。いずれも資料の修正で対応した。
- **`test_grades.js`（成績入力形式統一・86件）は 2件失敗しており、v1.49.0 から失敗している**（今回の作業前から。4c・H4 とは無関係）。`*.test.js` の名前規則から外れているため、これまでの全体実行（`for f in *.test.js`）に含まれず見落としていた。過去のコミットで確認: 440ae33（v1.49.0 の直前）は 86/86 PASS、その次の 5ac43ca（「評価項目ごとに入力形式(得点/5段階)を選べるようにする」）以降は 84/86。失敗する検査: 「状態遷移: category復帰後、既存の得点(13)と満点(15)で割合が正しい(87%)」「別の課題に切り替えてから戻っても割合は正しい(87%)」（課題の観点を「主体性」へ変えて元に戻したあと、入力画面の割合表示 `#rec-pct-8` が空になる）。→ **22章で原因を調査し、アプリの不具合と古い期待の両方だったと判明・解消済み（v1.54.1）**。

## 22. テスト実行の漏れの修正と、`test_grades.js` の失敗の原因（v1.54.1）

### 22.1 テスト実行の漏れ

- **漏れていたもの**: 全体実行を `for f in *.test.js` としていたため、`*.test.js` の名前規則に合わない **`test_grades.js`（成績計算コア・89件）と `attendance-snapshot.js`（出欠の集計の基準照合）が実行に含まれていなかった**（後者は別に毎回実行していたが、前者は v1.49.0 以降、私の全体実行では実行されていなかった）。`tests/` 直下には他に実行対象のスクリプトはなく（`helpers/` は共有ヘルパー）、対象は計57ファイル（`*.test.js` 55 + 上記2）。
- **件数の読み取り漏れ**: 私の全体実行（`grep -c '^PASS'`）は、`PASS` 行を出さない形式のテスト（`person.test.js` は `ok ` 行と `pass: 34 / fail: 0`）の件数を数えられず、`person.test.js` を 0 件として集計していた（実際は34件。終了コードでは成功と判定されていたため、失敗の見逃しには至っていない）。また、`PASS`/`FAIL` の行の数え方では、`roster-shift.test.js` のように集計行だけが件数を表すテストの件数が実際と違って見えていた（実測は240件）。
- **README の表の食い違い**: 表は29行で、実在する57ファイルのうち27ファイルの行が無く、件数も3ファイルで実測とずれていた（pe-rubric 74→115、pe-score10 23→25、roster-ui 67→71。roster-shift の240は正しい）。合計も実測と一致していなかった。
- **修正**: **`tests/run-all.js`** を追加。`tests/` 直下の `*.js` を名前規則に頼らずすべて順番に実行し、ファイルごとの検査数・終了コード・失敗した検査を表示する（検査数は集計行の5形式＋行の数え上げで読み取り、読み取れなければ警告して失敗扱い）。README の表（`<!-- run-all:table -->` の間）と実測を照合し、食い違えば終了コード1（`--write-readme` で表を更新）。`--only`・`--list`・サーバー未起動の案内あり。`package.json` の `test` も `node run-all.js` に変更。README の表は実測から生成する形にし、全57ファイルを載せた。

### 22.2 `test_grades.js` の2件の失敗（v1.49.0・5ac43ca から）— アプリの不具合と、古い期待の両方

- **失敗していた検査**: 「状態遷移: category復帰後、既存の得点(13)と満点(15)で割合が正しい(87%)」「別の課題に切り替えてから戻っても割合は正しい(87%)」。課題の観点を 知識・技能→主体性→知識・技能 と往復すると、入力画面の割合が空になる。
- **切り分け**（過去のコミットを作業ツリーに取り出して実行）: 440ae33（v1.49.0 の直前）は 86/86、その次の 5ac43ca（評価項目ごとの入力形式の選択）から失敗。テストのコードは v1.49.0 で変わっておらず、アプリの挙動が変わった。
- **原因はアプリ側**: 入力形式（`inputMode`）の印が無い**旧課題（v1.49.0 以前に作成した課題）に得点が入力済み**のとき、観点を変えて保存すると、印の無い項目は観点から入力形式を判定する（`isABCTest`）ため、入力形式が観点に追随して切り替わる。(a) 数値(13点)が入った課題を観点「主体性」に変えて保存すると、5段階の入力画面に化ける（満点は20のまま残る）。(b) 「知識・技能」に戻すと、フォームは5段階（満点欄は入力不可）のまま保存され、**満点が0になる**（入力欄の max 属性が 0・割合が出ない）。(c) 逆に ABC（文字）の記録が入った旧「主体性」課題を「知識・技能」に変えると、数値欄（満点0）になる。実UIと同じ操作（観点セレクトの change を発火）でも再現した。入力形式の固定（得点入力済みは観点を変えても入力形式を変えない）の仕様に反する。
- **H7（9f85b81）との関係**: H7 は同じ系統の不具合（観点変更で `{category:'主体性', maxScore:0, inputMode:'score'}` のような矛盾データになる）を、**印（`inputMode`）が既にある項目**について直した（保存時に切り替わった結果を印として書く・得点入力済みなら観点変更でタブを自動で切り替えない）。**印の無い旧項目**は、保存しても印が書かれず、観点の変更で判定が切り替わるため、対象外だった（H7 のテストは印付きの項目と、得点未入力の旧項目だけを確認していた）。
- **修正（v1.54.1）**: 得点入力済み（ロック中）の課題を編集して保存するとき、観点・種別の変更で「印なしの判定」がデータから決まる入力形式と食い違う場合に限り、現在の入力形式を明示の印として固定する（`recAddTest`）。食い違わない編集では、これまでどおり印の無い項目に印を付けない（`item-input-mode.test.js` の既存の確認を維持。最初の修正は常に印を付けてしまい、この確認が失敗したため絞った）。
- **テスト**: `category-edit-inputmode.test.js` に、旧項目・得点入力済みの往復（数値: 主体性へ→戻す〔満点を直す〕、ABC: 知識・技能へ）の5件を追加（**修正前は5件失敗**、修正後は14件すべてPASS）。`test_grades.js` は、古くなっていた期待（「得点入力済みの課題でも、主体性に変えると得点欄が消えてABCボタンになる」）を、現行の仕様（得点入力済みは数値のまま・割合も保たれる）に更新し、代わりに**得点未入力の課題**で「主体性⇔知識・技能」の切り替え（得点欄⇔ABCボタン）を確認する検査を追加した。観点セレクトは実UIと同じく change を発火して操作する。根拠: 得点入力済みの課題は入力形式を変えない（v1.49.0・H7 の仕様。フォームに「⚠️ 得点が入力済みのため入力形式は変更できません」と表示される）ため、旧期待は仕様と矛盾しており、バグの結果（保存すると5段階に化ける）をたまたま期待どおりに見せていた。`test_grades.js` は 89/89。
- **先生が気づく症状**: 実際の成績入力の画面では、**「以前に作った課題（得点をすでに入力済み）の観点を、課題の編集で『主体性』に変えて保存すると、点数入力の画面が5段階のボタンに変わる。さらに『知識・技能』などに戻すと、満点が0になり、割合が出なくなる（点数を入力できなくなる）」**という形で現れた。「主体性」以外の観点どうしの変更（知識・技能⇔思考・判断・表現）や、得点をまだ入力していない課題、v1.49.0 以降に作った課題では起きない。既に起きている場合は、設定の診断（「入力形式と満点が矛盾する課題」の件数）で分かる。今回の修正は、これから起きるのを防ぐもので、**すでに矛盾したデータは自動では直さない**（検出のみ・課題の編集で満点を入れ直す）。

## 23. H7 の診断（入力形式と満点が矛盾する課題）の拡張（v1.54.2）— 読み取り専用・自動修正なし

- **場所**: データ出力（左の列の📤）の画面の一番下の「🔍 監査診断」（押して開く）>「診断を実行」の表の「入力形式と満点が矛盾する課題」の行（画面・コピー用テキスト・注意欄）。**設定画面にはない**（初版の記述「設定 > 診断を実行」は誤りで、v1.54.3 で訂正。この診断のカードは折りたたまれていて、横向きの iPad では画面の外にあるため、探す手順を先生向けまとめに書いた。設定画面へ移す案は未実施）。データは書き換えない（診断の実行・コピーの間、`localStorage` への書き込み・削除が0回であることをテストで確認）。
- **既存の2種類が、印のない旧課題でも正しく検出されるか**: 確認済み。検出は入力形式の判定（`getItemInputMode`。印が無ければ観点・種別から推定）で行うため、印のない旧課題は判定上の入力形式で検出される（知識・技能で満点0＝「得点方式なのに満点なし」、主体性で満点あり＝「5段階なのに満点あり」）。既存テストは「知識・技能で満点0」だけを確認していたので、後者もテストに加えた。
- **新しい種類 `abc5num`**: 判定上は5段階で満点が無い（0）のに、**5段階として有効な値以外**（数値の点数、`A/B+/B/B-/C` と旧記号 `◎○△` 以外の文字）が記録に入っている課題。**印のない旧課題が主だが、印付きでも同じ矛盾なので対象にした**（依頼は印のない旧課題だったが、画面操作で入力形式が固定されたあとの状態（印が付き満点0）を見逃さないため。実UIで確認: 下の (c)）。欠席・空欄の記録は数えない。旧課題で満点が残っている5段階の数値（v1.49.0〜v1.54.0 の不具合の結果）は、既存の「5段階なのに満点あり」として検出され、今回、無効な値の件数と成績への影響が加わる。
- **成績への影響（確認と表示）**: 5段階（判定）の課題に数値の点数が入っていると、成績処理統合（`grdItemScore10`。カルテ・データ出力の成績も `grdCalculate` の同じ計算）は次のとおり誤る（実測）: **主体性**は 5段階の換算（`abcTo10`）に数値が渡り、1〜3 以外は「換算できず成績に入らない」、1〜3 は誤って 3/7/10 点に換算される（例: 数値2→7点）／**知識・技能**（記述問題など5段階の種別）は満点0だと満点100点として換算され低く出る（満点があれば数値の点数のまま正しく計算される）／**思考・判断・表現**は満点0だと成績に入らない。診断は、同じ `grdItemScore10` の結果を「数値の点数として読んだ換算」と比べ、食い違う記録の**児童の数**（番号で数える。氏名は出さない）を課題ごとと合計で表示する。**成績の重みが0の課題は「いまは成績に入っていません」**と表示し、正しく計算されている場合は「成績の値は変わりません（入力画面の表示だけが5段階になっています）」と表示する。
- **直し方の検証（実際のクリック・入力）**と表示する一行の案内:

| 状態 | 画面操作で直せるか | 案内（診断に一行で表示） |
|---|---|---|
| (a) 得点方式で満点なし・記録が数値（印なしの旧課題・印付き） | **直せる**: 入力画面で課題を選び「この課題を編集」→ 満点を入力して保存（入力形式の固定でも満点欄は使える）。満点が入り、割合が出て、診断から消える | 「児童の記録＞授業課題の管理でこの課題を開き、満点を入れて保存してください」 |
| (b) 5段階で満点あり・記録が文字（印なし・印付き） | **直せる**: 課題を開いてそのまま保存 → 満点が0になり5段階のまま。診断から消える | 「…そのまま保存してください（満点欄が消えます）」 |
| (c) 5段階（判定）で**数値の記録**（満点あり・なし） | **直せない**: 記録があるため入力形式のタブは両方とも押せず（固定・理由が表示される）、観点を変えて保存しても5段階のまま。印が付き満点0になり、診断には `abc5num` として残る | 「画面の操作では直せない状態です（記録の型と入力形式が食い違い、入力形式が固定されています）。お知らせください」 |
| (d) 得点方式（判定）で**文字の記録**・満点なし | **直せない**: タブが押せず固定。満点を入れて保存しても得点方式のまま文字の記録と食い違い、**診断からは消えてしまう**（満点あり・得点方式は検出の条件を満たさないため） | 同上 |

  → **(c)(d) は画面操作だけでは直せない**（入力形式の固定は「記録の型が入力形式と同じ」ことを前提にした安全装置だが、すでに食い違っている状態では、正しい形式へ戻す操作までふさいでしまう）。報告して判断を仰ぐ（HANDOFF.md）。**(c) は成績に影響する**（上記）。案: 記録の型と入力形式が食い違っている課題に限り、入力形式のタブの固定を外して1タップで切り替えられるようにする／診断から「入力形式を直す」ボタンを出す（確認つき・取り消し可）。(d) は満点を入れたあとも診断に残るよう、「得点方式で文字の記録」も検出の条件に加える案。
- **テスト**（`tests/audit-inputmode-repair.test.js` 38件、すべてPASS）: 検出（旧課題16通りの状態・正常な旧5段階/旧振り返り/欠席のみ/空欄のみは出ない）／無効な値の件数／成績への影響（主体性・知識・技能の記述問題・思考の作文・満点あり/なし・数値1〜3・無効な文字・重み0・児童数の重複除去。期待は `grdItemScore10` の結果と独立に設計）／氏名が画面・診断結果・コピー用テキストのどこにも出ない／読み取り専用（前後の全データ一致・書き込み0回。診断は少し遅れて計算されるため、終わるまで監視）／直し方 (a)(a2)(b)(b2) を実際のクリック・入力で確認し診断から消えること／直せない (c)(d) を確認。既存の `audit-inputmode-conflict.test.js`（12件）も引き続きPASS。
- **変異テスト**（10件すべて検出）: 新しい種類を検出しない→12件失敗／無効な文字を数えない→5件／成績への影響を数えない→9件／重み0も数える→1件／正しい計算も影響として数える→1件／直し方の案内の取り違え（2種）→各1件／診断が書き込む→1件（最初は検出できず、診断が遅れて計算されることを考慮して監視を待つよう改善）／氏名を出す→2件／数値を無効と数えない→15件。
- **関連する変更**: 成績処理の `grdItemScore10`・`grdGetItemWeight` を `window` へ公開（診断が同じ換算を読み取り専用で使うため）。

### 23.1 診断の誤判定と検出漏れの修正（v1.54.3）— 先生の iPad の診断結果から

- **きっかけ**: 先生の iPad の診断（v1.54.2）に「作文_運動会なりきり」（国語・作文・思考・判断・表現・満点5・数値の記録27件）が1件だけ出て、「成績に正しく入っていない児童 1人」と表示された。同じ条件（旧作文・満点5・数値の記録・遅れて提出1件）を作って再現した。
- **誤判定の原因**: 影響の判定で、「数値の点数として読んだ換算」を診断が独自の式（点数÷満点×10）で計算していたため、成績計算が適用する**遅れて提出の係数（0.8倍）**を含んでおらず、遅れて提出の1人を「食い違い」と数えていた。実際の成績は、数値の記録が数値の点数のまま正しく計算されている（3点・遅れて提出→4.8点は正しい動作）。**先生の成績は正しく、診断の表示が誤りだった。**
- **修正**: 影響の判定を、**成績計算の共通関数 `grdItemScore10` だけ**で行う。「いまの入力形式で計算した値」と、「入力形式だけを反対にしたコピーで、同じ関数を呼んだ値（＝記録の型に合わせて読み直した値）」を比べ、食い違う記録の児童を数える（独自の再計算はしない。遅れて提出の係数など、成績計算の規則は共通関数がそのまま適用される）。記録の型に合わせて読めるのは、数値は満点があるとき、文字は有効な値（A/B+/B/B-/C と旧記号）のとき。
- **検出漏れ (d) の修正**: 得点方式（判定）で**文字の記録**が入っている課題は、満点があっても新しい種類 `scorestr`（「得点方式なのに文字(5段階)の記録が入っている」）で検出し続ける（満点を入れて保存したあとも、記録の食い違いが残る限り診断に出る）。成績への影響も同じ方法で判定する（主体性は文字を点数として読めず成績に正しく入らない／知識・技能・思考・判断・表現は文字を5段階として計算するので影響なし）。
- **テスト**（`audit-inputmode-repair.test.js` 42件）: 旧作文（満点5・数値・遅れて提出1件）は影響0人／`scorestr` の検出と影響（主体性1人・知識0人）／(d) 満点を入れて保存したあとも診断に残る。変異テスト9件すべて検出（独自の式での再計算→2件失敗、`scorestr` を検出しない→5件、ほか）。
- **診断の場所の記述の訂正**: 上記 23 章の場所の記述を訂正した（設定画面ではなく、データ出力の一番下）。

## 24. 記録と入力形式が食い違っている課題を、記録に合う形式へ戻す（v1.55.0）

- **きっかけ**: 先生の iPad の診断に、旧作文「作文_運動会なりきり」（満点5・数値の記録27件・判定上は5段階）が出た。入力画面が5段階のボタンだけになり、入力済みの数値（1〜5）が選択状態で見えない。入力形式の固定のため、画面の操作では点数入力に戻せなかった（23章の (c)(d)）。
- **方針（先生の指示）**: 点数のまま戻す（**記録は変換しない・成績の値は変えない**）／固定を外すのは**記録と入力形式が食い違っている課題に限る**／切り替えられるのは**記録に合う形式**の方向だけ／確認ダイアログは使わず、切り替え後のトーストで取り消し。
- **判定 `inputModeFixInfo(test, scores)`（診断と編集画面で共有する唯一の判定）**: 記録（欠席・空欄を除く）がすべて数値なら「得点」、すべて有効な5段階の値（A/B+/B/B-/C と旧記号）なら「5段階」。混在・有効でない文字（X など）があれば戻せない。戻し先がいまの入力形式と同じなら食い違いなし（何もしない）。得点へ戻す場合に満点が無い（0）ときは、戻すときに満点を入れる（`needsMax`）。得点/5段階を選べない種別（まとめ・単元テスト・実技記録・授業態度）は対象外。
- **画面**: 課題の編集画面（入力画面で課題を選び「この課題を編集」）に、食い違いのある課題だけ、枠つきで説明と「🔁 記録（数値の点数）に合わせて、得点入力に戻す」（または「🔁 記録（A・B・C…）に合わせて、5段階入力に戻す」）ボタン（44px以上）が出る。入力形式のタブは**固定のまま**（勝手に切り替えられない。切り替えられるのはこのボタンだけ）。満点が保存済みならそのまま使い、無ければ満点欄を使えるようにして、入れずに押すと「先に満点を入力してください」（何も変わらない）。
- **実行 `recFixInputMode(testId, 満点)`**: 書くのは**課題（`spa_tests`）だけ**（記録 `spa_scores` は書かず、前後で1バイトも変えない）。名簿変更・復元と同じ仕組み: 容量確認 → スナップショット（`spa_roster_snapshot`）→ ジャーナル（`kind:'input-mode-fix'`）→ 検証付き書き込み → 検証（変えるのは対象の課題の `inputMode`（と満点）だけ・ほかの課題は不変・記録は不変）→ 確定。失敗したらスナップショットへ書き戻し（課題は元のまま）、強制終了しても次の起動で自動復旧する。得点へ戻す: `inputMode='score'`（満点は保存済みまたは入力した値）／5段階へ戻す: `inputMode='abc5'`・満点0。
- **取り消し**: 戻した直後に「✅ 「課題名」を得点入力に戻しました（記録は変えていません）［元に戻す］」のトースト（8秒）。押すと再読み込みされ、課題・記録を含む全キーが戻す前と1バイトも変わらない状態に戻る。名簿カードの取り消しボタンは「直前の入力形式の変更を取り消す」。戻したあとに課題が変更されていたら取り消さない（「入力形式を直したあとに課題の変更があったため、取り消せません」）。取り消し点は名簿変更・復元と共用の1つ。
- **診断の案内**: 戻せる状態には「直し方: 児童の記録＞授業課題の管理でこの課題を開き、『記録に合わせて、得点入力に戻す（5段階入力に戻す）』を押してください。記録は変わらず、取り消せます」（満点が無ければ「先に満点を入れます」）。混在・有効でない文字の状態だけが「画面の操作では直せない状態です…お知らせください」になる。
- **成績の値の変化（テストで確認）**: 先生の条件（旧作文・満点5・数値27件・遅れて提出1件・思考・判断・表現）は、戻す前後で成績処理統合の全結果が**完全に一致**（遅れて提出の児童は戻したあとも0.8倍で4.8点）。文字の記録の思考・判断・表現も換算・評定は同じ（表示用の「満点」の欄だけ20→0）。**主体性で数値の記録の課題は例外**: 戻す前は5段階として読めず成績から外れていた（null）が、戻すと記録の数値のまま換算される（16/20→8点など）。診断が予告した「成績に正しく入っていない児童」の人数と、戻して成績が変わった児童の人数が一致することを確認した。これは誤りが直る方向の変化で、先生の作文課題（思考・判断・表現）は該当しない。
- **テスト**（`tests/input-mode-fix.test.js` 49件、すべてPASS）: 先生と同じ条件の作文（画面の実際のクリック: 編集を開く・ボタン・取り消しのトーストのタップ）／全キー比較（変わったのは課題とスナップショット・ジャーナルだけ）／成績の全結果の一致／入力画面（点数入力に戻り入力済みの点数が見える）／診断から消える／主体性・文字の記録・満点なし／混在・有効でない文字・方向の制限（食い違いのない課題は変えられない）・記録なし・存在しない課題／失敗注入（容量超過・内容破損・強制終了→起動時の自動復旧・容量確認の失敗・スナップショット不可。全キーが元と同一）／画面での失敗表示／戻したあとの課題変更で取り消しを拒否／確認ダイアログ0回。**変異テスト13件すべて検出**（混在でも戻せる・食い違いなしでも変える・満点の書き換え・スナップショットを残さない・ロールバックしない・ボタンを出さない/常に出す・満点なしで戻せる・満点を0にしない・取り消し種別・確認ダイアログ・診断の案内・記録の書き換え）。
- **残る課題**: 入力形式が食い違ったまま、記録が数値と文字の混在・有効でない文字の課題（診断に「画面の操作では直せない」と出る）は、記録の修正が必要で、今回の対象外。

