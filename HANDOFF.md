# classroom-app 引き継ぎメモ（2026-09-20 時点・公開済みは v1.61.1＝push 済み・iPad 未確認／v1.61.2 は commit 済み・未 push（先生の承認待ち））

次のセッションは、まずこのファイルと `CLAUDE.md`、`UI_WORKLOG.md`（末尾の最新セッション）、
`DATA_FLOW_AUDIT.md`（該当章）を読む。`push` は先生の承認が出るまでしない。

## 1. 現在の状態

- **公開予定: v1.61.2（commit 済み・未 push・iPad 未確認）＝iPad の画面の拡大（ズーム）を止める**: 3種類すべて。(1) ダブルタップ: `html, body { touch-action: manipulation; }` (2) 2本指のピンチ: viewport に `maximum-scale=1.0, user-scalable=no`＋gesturestart/gesturechange/gestureend を `document` に `{passive:false}` で登録して preventDefault（**タッチ端末のみ**）。**戻せなくなる状態を作らない**: すでに拡大された状態（`visualViewport.scale>1.001`）から始まったピンチはそのピンチの最後まで止めない・`visualViewport` が無い端末も止めない（万一拡大しても2本指で戻せる） (3) 入力欄を押したときの自動拡大: `input（checkbox 等を除く）・select・textarea { font-size:16px !important }` を全体で1か所（実測: 526個のうち445個が16px未満だった）。見た目の影響は、全画面・全サブタブ・まとめモーダルの前後比較で**新しいはみ出し・文字切れ 0 件**（高さ+2〜3px・選択欄の幅+4〜31px・プレースホルダーが大きく・`kanjiModalChars` だけ17.6→16px。一覧は DATA_FLOW_AUDIT.md 38.2）。**PDF・Excel・CSV・成績・データは変えていない**（PDF の HTML に入力欄は無い）。テスト: `zoom-disable.test.js`（28件。修正前のコードで10件失敗・変異21件すべて検出）。**iPad 実機でしか確認できないこと**: 実際のピンチ・ダブルタップ・入力欄を押したときの拡大（8章の確認リスト）。**リスク**: html・body の touch-action が内側のスクロール領域でのダブルタップにも効くかは Safari 次第（効かなければ `*` に広げる案）。元に戻す方法は 38.4。DATA_FLOW_AUDIT.md 38章。
- 公開済み（一つ前）: **v1.61.1（push 済み 2026-09-20・iPad 未確認）＝面談用レーダーPDFの「提出率 60%(3/6)」の切り捨て表示を評価点ベースに**: v1.61.0 の iPad 確認（5項目すべて OK）を受けた1項目。切り捨て表示が出ていたのは**レーダーPDFの2箇所だけ**（1ページ目「提出物：提出率 70%（5/8件）」・教科カード「提出率 60%(3/6)」）。カルテPDF・面談テキスト・カルテ画面・ダッシュボードは提出率を % だけで出しており、切り捨て表示は無かった。今は成績処理と同じ文字「**評価点 3.6/6（60%）**」（1ページ目は「提出物：評価点 5.6/8（70%）」）。文字は `grdSubmissionDetailText(credit, counted)` の1関数で、成績処理の提出物の項目の説明文と同じ。**成績の値・提出率の%・コメントの段階は変えていない**。テスト: `radar-submission-text.test.js`（11件。修正前のコードで5件失敗→通った・変異7件すべて検出）＋`output-term-source.test.js` の4検査を新しい表記に更新。DATA_FLOW_AUDIT.md 37章。
- 公開済み（一つ前）: **v1.61.0（push 済み 2026-09-20・iPad 確認済み 2026-09-20）＝M7 専科成績の学期化・M9 成績の明細CSV・小修正2件**: **iPad の確認結果（先生の報告・5項目すべて OK）**: バックアップ保存／版表示 v1.61.0・成績処理の1学期の評定が以前と同じ／専科入力の学期切り替え（1学期に既存値・2学期は空）／成績一覧の専科（1学期は同じ値・2学期は未入力・通年は学期名つき）／成績の明細CSVのファイル名に「1学期」。**報告のなかった項目（未確認・省略可）**: 明細CSVの中身（提出物の状態・重みなど）・データ出力の成績処理CSVの教科に専科が出ること・カルテPDF／面談テキスト／レーダーPDFの専科の表示。 先生の回答（M7: 通知表には専科の評定を学期ごとに載せる・既存の値はすべて1学期分／M9: 案1）を受けた実装。1項目1コミット（M7 ae91e25・M9 44c10ba・小修正2件 afd3c0b・4ec3ca5）＋docs 970741b＋`sw.js` fd627df（v1.61.0）。**成績の数字は変わらない**（1学期の各出力と専科以外の成績は、変更前のコードで採取した値に固定）。(M7) 専科の値を学期ごとに持つ。**保存: 1学期＝`data[教科]`（v1.60.4 までの値がそのまま1学期の値。書き換えなし・移行なし）・2学期＝`data['教科@2']`・3学期＝`data['教科@3']`**。専科入力タブに学期の切り替えと「いま入力中：〇学期」の帯・各行の学期の札（初期値は現在の学期）。各出力（成績処理の表・カルテPDF・面談テキスト・レーダーPDF・成績CSV×2・算出根拠Excel）は、それぞれの学期の出所に従い、その学期の専科の値だけを使う。**値のない学期は「未入力」**（他の学期で埋めない）・**通年は最新の入力済み学期の値を学期名つきで**（例: B（2学期）。項目ごと・平均は取らない）。退避した児童を戻す処理が `教科@2` を専科でない教科と誤読していたのを直した（付け替え・バックアップ・復元・整合性検査はほかに変更なし。版の印 version 11 は変えていない）。(M9) データ出力に「🔎 成績の明細（項目ごと）」の別ファイル（`成績の明細_1学期_国語_….csv`。先頭に対象学期の説明）。1行＝児童×項目（観点・入力形式・生の値・満点・10点換算・重み。提出物は状態（お直し済み／お直し前／遅れ…）・評価点・欠席除外）。値は成績処理と同じ関数から。明細から観点ごとの重み付き平均を組むと成績処理CSVの平均と一致する。既存の3つのCSVは1バイトも変えていない。(小修正) 提出物の説明文を「評価点 3.6/6（60%）」に（成績の値は同じ）／データ出力の成績CSVの教科の選択肢に、テストも提出物もない専科の教科も出す。**記録のみ**: 専科の教科のテストが面談テキストの個票に載る件（今回は変えない）・（レーダーPDFの「提出率 60%(3/6)」の切り捨て表示は v1.61.1 で直した。DATA_FLOW_AUDIT.md 37章）。DATA_FLOW_AUDIT.md 36章。
- 公開済み（一つ前）: **v1.60.4（push 済み 2026-09-20・iPad 確認済み 2026-09-20）**: **iPad の確認結果（先生の報告・すべて OK）**: 版表示 v1.60.4 OK／監査診断の「主体性（得点方式）の空欄・文字の記録」**0件**（課題0件・児童0人）／「提出記録の studentIndex 範囲超過」**0件**（児童数27）／総合判定 🟢。→ 先生の端末には、空文字・文字の記録も、範囲外の提出記録も無い。**報告のなかった項目（未確認・省略可）**: 欠席→解除で ✓ が付かないこと（確認リストの3）・成績処理の評定が同じか。以下は v1.60.4 の変更内容: 2026-09-20 の調査（3章の L5・L7 の調査結果）を受けた3項目。**成績の値は変えていない**（各テストに変更前の値を固定）。(L7 案C) 監査診断が、主体性（得点方式）の課題にある**空文字・数字でない文字の記録**を、課題ごと・児童数とあわせて数える（「情報（参考）」と、構造的整合性の表の新しい行「主体性（得点方式）の空欄・文字の記録」・コピー用テキスト。**総合判定は変えない**）。数字の文字列（"8"）は、成績が数値と同じになるので「成績に正しく入っていない」に数えないよう直した（過大の修正）。(L7 案B) `recToggleAbsent` の解除で、中身が空の記録（`score:''` で、id・児童・課題・作成日時以外の項目がない）は記録ごと消す（今までは `{score:''}` が残り、主体性の得点方式だと0点として成績に入り、「入力済み」の数にも入っていた）。点数・5段階・遅れの印・メモなど中身がある記録は今までどおり。**すでにある空の記録は書き換えない**（その児童の欠席の操作をしたときだけ）。(L5 案D) 監査診断が**提出記録の範囲外 studentIndex**（名簿の人数以上・負）を数える（表の新しい行「提出記録の studentIndex 範囲超過」・「情報（参考）」に件数・番号・「成績・提出率には入らない」の説明・コピー用テキスト。総合判定は変えない）。**実装していないもの**: 案A（読み方を直す。空文字・数字でない文字を未評価にする）と案E（課題別の人数を名簿の人数までに絞る）。DATA_FLOW_AUDIT.md 34章。
- 公開済み（一つ前）: **v1.60.3（push 済み 2026-09-20・iPad 確認済み 2026-09-20）**: **iPad の確認結果（先生の報告・すべて OK）**: 版表示 v1.60.3 OK／同僚用テンプレートを Numbers で開き、5段階の評価の列に B+ を入れると観点の % が 85、B で 70、B- で 50 になった。**報告のなかった項目（未確認）**: 成績処理の評定・観点別が以前と同じか（確認リスト 3。今回はテンプレートの式だけの変更で、成績処理は変えていない）・小さな項目 (ア)〜(エ)。以下は v1.60.3 の変更内容: 同僚用テンプレートの A/B/C 入力の列で、5段階の **B+・B- が空欄扱いだった**のを修正（B+=8.5・B-=5。アプリの換算 `abcTo10` と同じ。小文字・全角の＋／－・前後の空白も可。A/B/C と数値入力の換算は変えていない）。満点欄の表記は「A/B+/B/B-/C」、「使い方」に換算の行を追加。シートの式は小さな計算機（`tests/helpers/xl-eval.js`）で成績処理と全員一致を確認し、**iPad の Numbers でも B+・B・B- の値が想定どおりになることを確認済み（Excel では未確認）**。DATA_FLOW_AUDIT.md 33章。
- 公開済み（一つ前）: **v1.60.2（push 済み 2026-09-20・iPad 確認済み 2026-09-20）**: **iPad の確認結果（先生の報告の 0〜10 すべて OK）**: 更新前のバックアップ保存／版表示 v1.60.2／成績処理の1学期の評定が以前と一致（成績が変わっていない）／新体力テストが27名で今までどおり／バックアップの復元後の整合性確認で問題なし／監査診断／カルテの提出率／面談テキストに「対象学期」が印字され1学期分だけ／レーダーPDFの1・2ページ目に対象学期が印字／同僚用テンプレートの題名に「（1学期）」／日付をまたいでも復旧バーが出ず、忘れ物の日付が今日になる。**これで v1.58.0 以来未確認だった「バックアップの書き出し」と「復元時の整合性確認カード」も確認済み**（v1.58.0 の項目は完了）。v1.60.0（L10・M2〜M5）・v1.60.1（レーダーPDFが作れる）・4e（新体力テストが今までどおり）も、この確認で確認済み。**報告のなかった項目（未確認・省略可）**: 児童用カルテPDFの「通年（１年間）のきろくです」・算出根拠Excelのファイル名と表題の学期・診断のサマリーの数字（まとめ形式30件＝まとめテスト12＋単元テスト18／testType 分布のまとめテスト13件）・`pf.html` の案内ページ。以下は v1.60.2 の変更内容: v1.60.1 の iPad 確認（レーダーPDFは生成できたが、学期の表記がなく、ボタンは個人カルテの出力画面にあった）を受けた修正。(1) 出力の学期の出所を「ボタンがある画面の学期セレクタ」に統一（レーダーPDFは出力画面の学期。表・提出率・机間巡視・カード・検定の行が同じ学期）し、レーダーPDF・児童用カルテPDF（通年も）・面談テキスト・算出根拠Excel・成績CSVに対象学期を印字（DATA_FLOW_AUDIT.md 32.1 に一覧）。(2) 別モジュールから呼ばれるのに未公開の関数を eslint とブラウザで洗い出し、日付が変わったあとの再描画（`fgtShowSub`・`subRenderDiary`）を直した（復旧バーが出ていた）。(3) 成績画面以外の async ボタン（バックアップの保存・復元・座席表PDF・PDFの共通処理・名前を伏せて作り直す）が失敗したらトーストで通知。成績画面の値は変えていない。
- 公開済み（一つ前）: **v1.60.1（push 済み 2026-09-20・iPad 一部確認済み）**: **iPad の確認結果（2026-09-20・途中）**: 版表示 v1.60.1 OK／監査診断（L10）OK／面談用レーダーPDFは**生成できた（押しても無反応は解消）**。ただし2点の問題: (a) PDF のどこにも学期が書かれておらず、何学期分か分からない (b) ボタンは成績処理画面ではなく**個人カルテ（出力）の画面**にあった（M4 は成績処理画面の学期で計算していた）。→ **v1.60.2 で対応済み**（学期の出所を「ボタンがある画面の学期セレクタ」に統一・全出力に対象学期を印字）。**未確認**: 更新前のバックアップと成績CSV・成績が変わっていないこと・提出率（M2）・面談テキスト（M3）・同僚用テンプレート（M5）・算出根拠・診断の数字（30件と13件）。（先生の報告の番号は確認リストの番号と一部ずれていたため、内容で記録した）
  以下は v1.60.1 の変更内容: 面談用レーダーPDFのボタンが、体育の検定（なわとび・泳力）の記録があると無反応だった**既存の不具合**（v1.59.1 でも同じ。M4 の回帰ではない）を修正。原因は `kenteiStageToLabel` が `window` に公開されていなかったこと。あわせて、成績画面のボタンの処理が失敗したら、トーストで「失敗しました: 原因」と知らせるように。DATA_FLOW_AUDIT.md 31章。
- 公開済み（一つ前）: **v1.60.0**（push 済み 2026-09-20・iPad 未確認）: 先生の方針確定を受けた L10・M2〜M5。(L10) 診断の警告「問題定義のないもの N件」が出るように（設問なしのテストがあると総合判定が「🟠 注意項目あり」）。(M2) 提出率を成績の数え方（お直し済み1.0・お直し前0.8・遅れ上限0.8）にカルテ画面・カルテPDF・提出物の一括PDF・面談テキスト・ダッシュボード（児童別・クラス）で統一。(M3) 面談テキストの成績・提出物・テスト個票を、出力画面の学期に統一（指示の「成績画面の学期」との差は DATA_FLOW_AUDIT.md 30.1。先生が「出力画面の学期」でよいと回答済み）。(M4) レーダーPDFの教科カードのテスト一覧・平均を成績画面の学期で絞り、得点方式の主体性を含める。(M5) 同僚用テンプレートを成績画面の学期に絞り・得点方式の主体性を得点入力の列に、算出根拠の「1→4点」を「1→3点」に。**成績画面の評定・観点別の値は変えていない**。DATA_FLOW_AUDIT.md 30章。
- 公開済み（一つ前）: **v1.59.1**（L9: 監査診断の表示改善。単元テストを単一観点の判定から除外・観点語のない名前の文言変更・内訳表に課題名と種別の列（赤印なし）・サマリーを「まとめ形式 X件＝まとめテストA＋単元テストB」に。表示だけで成績は変わらない。DATA_FLOW_AUDIT.md 29章）。push 済み（2026-09-20）・iPad 未確認。
- 公開済み（一つ前）: **v1.59.0**（H4 4e: `pf.html` を案内ページに。スクリプトなし・データを読みも書きもしない。DATA_FLOW_AUDIT.md 28章）。push 済み（2026-09-20）・**iPad 未確認**。
- 公開済み（一つ前）: **v1.58.0**（H4 4d-2: バックアップの版の印 version 11・版の判別・復元後の整合性検査と設定の確認カード。DATA_FLOW_AUDIT.md 27章）。push 済み・**版表示と監査診断は iPad 確認済み（2026-09-20）**: 版表示 v1.58.0 OK／アプリ内監査診断は構造的整合性すべて0件・致命的問題なし（児童27名・テスト93件・得点2,378件・提出記録2,216件）。**未確認: 復元時の整合性確認カード・バックアップの書き出し**。
- 公開済み（一つ前）: **v1.57.1**（v1.56.0 遅れ係数を提出物チェックだけに／v1.57.0 取り消し点を種類ごとの枠に／v1.57.1 診断に遅れ係数の影響の表示）。push 済み（2026-09-20・先生はバックアップと変更前の1学期成績CSVを保存済み）。`git log origin/main..HEAD` で確認する。**iPad 確認済みは v1.57.1 まで（2026-09-20）**: 診断の「遅れの印がある数値の記録」は作文_運動会なりきり・1学期・1人のみ（想定どおり）／保存した1学期CSVとの比較も想定どおり／気になった点なし。未確認: 取り消し点の枠の分離（v1.57.0）の取り消し操作そのもの（名簿変更→入力形式の変更→別々に取り消し）。
- 端末は先生の iPad（PWA・DevTools なし。確認は必ず画面操作で案内する）。1クラス最大30人。
- 先生は 4b（v1.53.0）以降を入れる前にバックアップ済み。**iPad（v1.53.1）で確認済み（2026-09-20・問題なし）**:
  バージョン表示 v1.53.1／起動直後に「新しい方式に更新しました」の通知／診断カードが「児童ごと（新方式）」「一致しています」／
  新体力テストの記録が更新前と同じ児童に付いている／変更なしで保存すると「変更はありません」／気になった点なし。
  → pf の新方式への自動移行は実機で成功。以後、先生の端末は新方式（v2）。
- 先生の端末の実測（4a）: 名簿は pf と一致・27名・単体ページ pf.html の使用の痕跡なし・手入力の児童なし・記録は2026年度27名。

## 2. H4（児童ID化）の進捗 — 詳細は DATA_FLOW_AUDIT.md 7〜20章

| 段階 | 内容 | 版 | 章 |
|---|---|---|---|
| 1 | 児童ID（`studentId`）付与・起動時移行・退避コピー | v1.51.0 | 9 |
| 2a/2b | 付け替えの純関数・18ストア登録表／トランザクション・退避・復元・取り消し | v1.51.1/2 | 11, 13 |
| 2c | pf 連携（旧方式で一致するときだけ追従） | v1.51.3 | 14 |
| 3 | 名簿編集をリスト編集UI（下書き→保存）へ。退避カード | v1.52.0 | 15 |
| 4a | pf 連携状態の診断表示（読み取り専用） | v1.52.1 | 17 |
| 4d-1 | `importBackup` の安全化（復元前の退避・検証付き書き込み・取り消し） | v1.52.2 | 18 |
| 4b | pf の記録を studentId キーへ（起動時移行・名簿変更・復元・取り込み・pf画面） | v1.53.0 | 19 |
| 追加 | 通知がボタンを隠す不具合の修正／起動直後の競合の修正／`pf.html` 暫定ガード | v1.53.1 | 20 |
| 4c | 旧方式の記録の「氏名で引き継ぐ」・`pf-residual` の単体復元・`pf-unlinked` | v1.54.0 | 21 |
| 追加 | テスト実行の漏れ（`run-all.js`）・旧課題の観点変更の不具合の修正 | v1.54.1 | 22 |
| 追加 | H7 の診断の拡張（5段階で数値の記録・成績への影響・直し方の案内）／誤判定の修正 | v1.54.2〜3 | 23 |
| 追加 | 記録と入力形式が食い違う課題を、記録に合う形式へ戻す（取り消しつき） | v1.55.0 | 24 |
| 追加 | 診断の「直し方」の経路を画面の表示どおりに統一／まとめテストの単一観点の情報を見直し | v1.55.1 | 24.1 |
| 追加 | 遅れ係数0.8倍を「提出物チェックの提出記録」だけに（児童の記録には掛けない）(M8) | v1.56.0 | 25 |
| 追加 | 取り消し点を種類ごとの別の枠に分ける（互いに上書きしない）。入力形式の取り消しは編集画面へ | v1.57.0 | 26 |
| 追加 | 診断に「遅れ係数の変更で値が変わる記録」（課題名・学期・該当児童数・合計）を追加（読み取り専用） | v1.57.1 | 25.1 |
| 4d-2 | バックアップに `version: 11`・`studentIdSchema`・`pfSchema` の印／復元時に中身で新旧を判別して案内／復元後の整合性検査と設定の確認カード（読み取り専用） | v1.58.0 | 27 |
| 4e | `pf.html` を案内ページに置き換え（スクリプトなし。暫定ガード・旧方式の画面・JSON バックアップは廃止。`sw.js` のキャッシュ対象には残す） | v1.59.0 | 28 |

関連: H8（IndexedDB の鏡に氏名が残る）は 12 章で対応済み（v1.51.2・v1.51.3）。

### 残り（H4 の段階は 4e まで実装・公開済み。iPad での 4e の確認だけ残る）

- 4c の実装内容は DATA_FLOW_AUDIT.md 21章。先生の端末は名簿が一致して自動移行済みのため、4c の画面は今の端末には出ない。
- （公開済み v1.58.0）**4d-2**（16.6・27章）: 書き出しは `version: 11`・`studentIdSchema: 1`・`pfSchema`（端末の実際の方式）。復元は版の数字でなく中身で新旧を判別し、案内だけ出す（止めない）。復元後に `spaIntegrityCheck`（読み取り専用）で児童IDの欠落・重複、pf の新旧混在、退避の二重・破損を検査し、トーストと設定の「データの整合性の確認」カードで知らせる。自動では直さない。
- （公開済み v1.59.0）**4e**（16.7 案P1・決定 a・28章）: `pf.html` を、スクリプトなしの案内ページ（アプリを開く → 【記録】の「📝」→「🏃 新体力」）に置き換えた。旧データの読み書き処理（暫定ガード 20.3 も）は残していない。localStorage の `pf_*` のキーは削除しない。`sw.js` のキャッシュ対象には `./pf.html` を残す。JSON バックアップはアプリ本体のバックアップに一本化。

## 3. 未対応項目（DATA_FLOW_AUDIT.md 3章。行番号は調査時点）

- （解消済み・v1.60.0）**M2** 提出率を成績の数え方に全画面で統一／**M3** 面談テキストの学期を出力画面の学期に統一／**M4** レーダーPDFの教科カードを成績画面の学期で絞り、得点方式の主体性を含める／**M5** 同僚用テンプレートの学期・得点方式の主体性・算出根拠のラベル。DATA_FLOW_AUDIT.md 30章。
- （解消済み・v1.60.2）**日付が変わったあとの再描画の `ReferenceError`**（`fgtShowSub`・`subRenderDiary` が未公開）と、**成績画面以外の `async` ボタン処理の失敗が画面に出ない件**。DATA_FLOW_AUDIT.md 32.2・32.3。全体の `unhandledrejection` を画面に出す網は、意図しない通知を避けるため入れていない。
- **M2〜M5 の残り（今回の対象外・困りごとが出てから）**: (a) 面談テキストの生活記録・児童の記録・保護者対応は学期で絞らない（PDF は生活記録・保護者対応を絞る）。(b) レーダー教科カードの検定（実技記録）の行・漢字の行は学期に関係なく通年。(c)（解消済み・v1.60.3・iPad の Numbers で確認済み）同僚用テンプレートの A/B/C 入力の B+・B-。(d) 提出物入力画面の進捗の%は「何人が出したか」の入力進捗で、提出率とは別の数え方（○△×の人数）。(e) 「新体力テスト総合」（新体力テストの自動取り込みが作る課題）は `testType:'まとめテスト'` だが `type` を持たず、算出根拠の換算方法の列が「まとめテストの該当観点分」と出る（30.2）。
- **M7** 専科成績に学期がない — **解消済み（v1.61.0・DATA_FLOW_AUDIT.md 36.1。調査は 35.1〜35.2）**。保存の形・入力画面・各出力の表示・付け替えの直しは上の1章の v1.61.0 を参照。**残り（記録のみ・成績の値には関係しない）**: (a) 専科の教科にテストを入れると、成績には入らないが面談テキストの「テスト個票」には載る（先生の指示で今回は変えない）。(b) 「前の学期の値をコピー」ボタンは付けていない（2学期以降の入力の手間が気になったら）。(c) 学期の方式を3学期制→2学期制に変えたあとも `教科@3` の値は消さず、通年の表示に「3学期」として出る（入力画面には出ない）。(d)（解消済み・v1.61.0）データ出力の成績CSVの教科の選択肢に専科だけの教科が出ない件。
- **M8**（解消済み・v1.56.0・25章）: 遅れ係数0.8倍は提出物チェックの提出記録だけ。児童の記録には掛けない。
- **M9** テスト成績/提出物CSVが生データのみ — **解消済み（v1.61.0・DATA_FLOW_AUDIT.md 36.2。調査は 35.3〜35.4。案1）**: 既存の3つのCSVは変えず、データ出力に別ファイルの「成績の明細CSV」を足した（列・状態の言葉・照合の仕方は上の1章と 36.2）。**残り（記録のみ）**: 専科の教科は明細に含めない（項目を持たないため。専科の値は成績処理CSVで出せる）。提出物CSVのセルの文字を日本語にする案（35.4）は、`COUNTIF` で再集計している人が壊れるので入れていない（明細に日本語の状態を出している）。
- **L1〜L4・L6**（将来リスク。3章「低」）: 未使用キー `spa_grades`（L1）、授業態度記号の変換（L2）、専科の評定しきい値固定（L3）、課題保存時の `createdAt` 上書き（L4）、「入力済 x/N」の数え方（L6。v1.60.4 の案B で、欠席の解除では空の記録が残らなくなった。欠席・元からある空の記録を数える点は残る）。L5・L7 は下の2項目で整理済み。
- **L7（主体性（得点方式）の score が文字列）— 解消済み（v1.60.4）・iPad 確認済み（診断0件）。案A は「該当なし・当面不要」**。調査結果と数値は DATA_FLOW_AUDIT.md 34.1（3章の古い記述「入力形式ロックにより通常は到達しない」は不正確だった。**「欠席」→「欠席中」の解除で `score:''` が残り、主体性の得点方式では0点になる**。数字でない文字は NaN→観点C。`"8"` は成績が同じ）。v1.60.4 で、案C（診断に空文字・数字でない文字の件数を出す。`"8"` は数えない）と案B（欠席の解除で空の記録を残さない。既存の記録は書き換えない）を実装（34.2）。先生の端末は診断で **0件**。
  - **案A（未実装・当面不要）**: `grdItemScore10` の主体性・得点方式の分岐で、数値（と数字だけの文字列）以外を未評価（null）にする（知識・思考と同じ扱い）。**該当の児童だけ成績が変わる**（今の0点・観点Cが未評価になり、主体性・評定が上がる方向）。数値の記録の児童は変わらない。`calcWeightedScore` が NaN を除外する防御を足す案もある。**再開の条件**: 診断の「主体性（得点方式）の空欄・文字の記録」が1件以上になったとき（その件数と課題名を見て、先生が判断する）。入れるときは、`audit-attitude-scorestr.test.js` が成績を今の値（空文字0点・文字はNaN→C）で固定しているので、その期待を更新する。
- **L5（名簿縮小後の範囲外 studentIndex）— 解消済み（v1.60.4）・iPad 確認済み（診断0件）。案E は「該当なし・当面不要」**。調査結果は DATA_FLOW_AUDIT.md 34.1: 名簿を減らす操作は必ず `applyRosterChange` を通り、範囲外の記録は退避される（新しくは起こらない）。過去の版で取り残された記録があっても、成績・提出率・面談テキストは変わらず、ずれるのは課題別の人数の表示だけ。v1.60.4 で、案D（診断で提出記録の範囲外を数える）を実装（34.2）。先生の端末は診断で **0件**（児童数27）。
  - **案E（未実装・当面不要）**: 課題別の人数の表示を名簿の人数までに絞る（`subRenderAssignList` の `asSubs`（課題カード）と `subRenderStats` のバーが `n` で絞っていない箇所）。**成績・提出率は変わらない**（取り残しがあるときの課題別の人数の表示だけが変わる）。**再開の条件**: 診断の「提出記録の studentIndex 範囲超過」が1件以上になったとき、または課題別の人数が名簿より多く見えたとき。入れるときは、`audit-sub-index-over.test.js` が課題カードの○△×を今の表示で固定しているので、その期待を更新する。
- （解消済み）`tests/test_grades.js` の2件の失敗（v1.49.0〜）は v1.54.1 で解消（22章）。
- （実装済み・iPad確認済み）入力形式の固定で直せなかった状態は、v1.55.0 で「記録に合わせて…入力に戻す」ボタンにより直せる（24章）。直せないのは記録が数値と文字の混在・有効でない文字の課題だけ（診断に「画面の操作では直せない」と出る）。
- （解消済み）取り消し枠の共有: v1.57.0 で、取り消し点を種類ごとの別の枠に分けた（名簿変更・バックアップ復元・pf 引き継ぎ・pf 復元・入力形式の変更）。入力形式の変更の取り消しは課題の編集画面のボタン。DATA_FLOW_AUDIT.md 26章。
- （確認済み・対応不要）体育「まテ_1学期　思考」の answers 全0/1（2件・testId=1782173422503）: 先生が確認し、**本当に0点**（旧形式の○×の取り違えではない）。診断の情報「複数設問・複数点配点で answers が全0/1 のレコード 2件」は、この2件が出ている間は出続ける（正常。コードは変えない）。DATA_FLOW_AUDIT.md 24.2。
- **L8（案・実装はしない）診断の「情報（参考）」に確認済みの項目が毎回出る**: 先生が確認済みの項目（単一観点のまとめテスト8件＝課題名に観点語がないもの／体育「まテ_1学期　思考」の answers 全0/1 の2件）が診断を開くたびに表示される。テストごとに「確認済み」の印を付けて情報表示から外す機能の案:
  **印の保存先**は新キー `spa_audit_ack`（項目の種類→testId の一覧。例 `{matomeSingle:[id…], answersBinary:[id…]}`。`KEYS` と `NOT_STUDENT_KEYED` に登録。成績・診断の判定には使わない）。
  **解除方法**は、診断の情報欄の下に折りたたみ「確認済み N件（表示する）」を置き、各行に「確認済みを解除」ボタン（印を消すだけ。課題が消えたら印は無視）。
  **バックアップ**は通常のバックアップに含め（先生の判断の記録なので復元後も残す）、退勤モードの消去の対象にする（課題名は出さず testId だけ持つ）。
- **L9（v1.59.1 で実装・公開済み。29章）診断の表示が紛らわしい2点**（どちらも表示の文字だけの問題。データ・成績は正しい）。以下は調査時の原因と案。実装は案の (a)(b)(c) とサマリーの言い方の変更のとおり（(b) の文言は「全問が同じ観点です。観点ごとに分けて作ったテストならこのままで問題ありません」に確定）:
  1. 「全問が単一観点（観点不均衡の可能性）」が8件出る。**原因**: 観点の内訳（`analyzeMatomeBalance`）の対象は `type==='matome'` の全テスト＝「まとめテスト」と「単元テスト」の両方（どちらも設問ごとに観点・配点を持つ形式で、作成時に `isMatome` が両方に `type:'matome'` を付ける）。単元テストは1単元の知識・技能を問うので全問が「知」になるのが普通で、しかもメッセージは一律に「まとめテスト testId=…」と書く。v1.55.1 は「課題名が観点を示すもの」（例「まテ_1学期　思考」）だけを外したので、課題名に観点の語が無い8件が残る。**仮説（この端末のデータは見られない）**: 8件は単元テスト、または観点語の無い名前のまとめテスト。確認は診断の情報行の課題名か、下の2で「単元テスト: N件」が出るか。**修正案**: (a) `perTest` に `testType` を持たせ、単元テストは「全問が単一観点」の判定から外す。(b) まとめテストで課題名に観点語が無い場合の文言を「全問が同じ観点（知識・技能）です。観点ごとに分けて作ったテストなら、このままで問題ありません」に変え、「不均衡の可能性」を消す。(c) 内訳の表（各まとめテストの内訳）の赤い「❌全問単一観点」は、今は「知」100%のときだけ出る（思・主は出ない）ので、赤をやめて表に課題名・種別の列を足す。L8（確認済みの印）とは独立に入れられる。
  2. データ量サマリーの「まとめテスト: 30件」と testType 分布の「まとめテスト: 13件」が食い違う。**原因**: サマリーは `type==='matome'`（まとめテスト＋単元テスト）を数え、分布は `testType==='まとめテスト'` だけを数える。（v1.60.0 の調査で訂正: 差は単元テストではなく、新体力テストの自動取り込みが作る「新体力テスト総合」（testType はまとめテスト・type なし）。実数は「まとめテスト12＋単元テスト18」＝30件で、分布の「まとめテスト13件」に「新体力テスト総合」が1件入る。DATA_FLOW_AUDIT.md 30.2）**修正案**: サマリーの表示を「テスト総数: 93件（うち、まとめ形式: 30件＝まとめテスト12＋単元テスト18）」のように、`byType` から内訳を出して書く。コピー用テキスト（`d.matomeTestCount` の2箇所だけが使用）・「各まとめテストの内訳」の見出し・「まとめテストのうち問題定義のないもの」も「まとめ・単元テスト」に揃える。
- **L10（v1.60.0 で対応・先生の決定「直す」）** 以下は調査時の記録。診断の警告「まとめ形式のテスト…のうち問題定義のないもの N件」が出ない不具合だった。`collectAnomalies` が `structuralIntegrity.matomeNoQ` を読むように直した（DATA_FLOW_AUDIT.md 30.3）: `collectAnomalies` は `result.matomeBalance.matomeNoQ` を読むが、その値は `structuralIntegrity.matomeNoQ` にしか無く（`analyzeMatomeBalance` は返さない）、常に `undefined > 0` で偽。画面には「構造的整合性」の行（`si.matomeNoQ`）が出るので情報は見えており、先生の端末は0件。**直すなら**: `result.structuralIntegrity.matomeNoQ` を読む（1行）。ただし今は出ていない「注意」が出て、診断の総合判定（`overallStatus`）が「🟠 注意項目あり」になり得るので、直す前に先生に確認（表示だけの変更ではなくなる）。
  - **先生向けの説明（やさしい言葉）**: (1) 診断には「問題（設問）が決まっていないまとめテスト・単元テストがある」ときに黄色の注意を出す仕組みがあるが、場所の書き間違いで、これまで一度も出たことがない。(2) 放置しても、画面の構造の行（「問題定義なし」の件数）で同じ件数は見えるし、先生の端末は0件なので、今すぐ困ることはない（将来、設問なしのテストができても、黄色の注意と総合判定に出ないだけ）。(3) 直す案は、注意が出るようにする（1行の修正・成績は変わらない）。直すと、設問なしのテストがあるとき診断の総合判定が「注意」になる。
  - （決定済み）先生は「直す」を選んだ。設問なしのテストがあるとき、診断の総合判定は「🟠 注意項目あり」になる。
- **メモリ記載の予定**: Step4-D — `recNwUpdateCardUI` 汎用化時に `recNwCalcGrade` → `kenteiStageToLabel` へ置き換える。
- **H4 の既知の制約**（19章・18章）: 旧方式で一致しない端末で pf の「名簿を読み込む」を押すと、従来どおり記録が別の児童に付く
  （10.5。次の起動でその状態を忠実に新方式へ移行するだけで、新たなずれは作らない。4c で解消）。
  取り消し点は種類ごとの枠（名簿変更・バックアップ復元・pf 引き継ぎ・pf 復元・入力形式の変更。v1.57.0・26章。起動時の pf 移行は一時枠で取り消し点を残さない）。
  旧版（v1.52.x 以前）へ戻すのは非推奨。
- 診断ツールの `spa_migration_backup_studentId` は氏名を含むため内容を出さない（文字数のみ）。

## 4. 確定済みの設計判断（要点）

- フィールド名は **`studentId`**（`stu_`+8桁）。`students[].id` は増やさない（pf の `s.id` と衝突するため）。
- 保存形式は**添字キーのまま**（案B）。名簿変更時に `applyRosterChange` が18ストアを一括付け替え
  （登録表 `STUDENT_KEYED_STORES`・不変条件 I1〜I7）。案C（全面のID化）は必要性を再評価してから。
- **localStorage が真実、IndexedDB は鏡**。書き込みは `storageVerifiedWrite/Remove`（直接読み戻して検証）。
  `StorageManager.setImmediate` は容量超過を握りつぶすため、重要な書き込みには使わない。
- トランザクション: 容量の実測確認 → スナップショット（`spa_roster_snapshot`・1世代）→ ジャーナル（`spa_roster_txn`）→
  書き込み → 検証 → 確定。失敗はロールバック、強制終了は次の起動（`loadMaster` より前）で自動復旧。
  名簿変更・復元・pf 移行が同じ仕組み（`kind` で区別）。
- 名簿編集UI: 下書き→保存ボタン（1回の保存＝1トランザクション＝取り消し1回）。確認ダイアログは使わない。
  完全削除は**2回タップ**。名簿を変える直前にバックアップを促すトースト（操作は止めない）。
- 削除した児童のデータは消さず `spa_student_archive` に退避（復元・完全削除）。
- pf: 記録・集計のキーを **`studentId`**（`pf_key(s)`）。`id` は表示用の番号。手入力の児童は `pfm_` の ID（移行の対象外）。
  起動時の移行は、pf 名簿がSPA名簿と**氏名・順序・id とも一致するときだけ**位置で変換。一致しなければ触らない。
- バックアップ復元（18章）: 復元前に変更キーを退避。容量不足なら**警告して続行**（消す操作はしない）。
  バックアップに無い児童別キーは、名簿が違うときだけ消す（取り消しで戻る）。別アプリのキーは端末に無いときだけ書く。
- 保護者向けの出力（PDF・帳票）に内部の警告・診断を出さない。診断カード等は設定画面の中だけ。
- 通知（トースト）は下のボタンへのタップを受けない（`pointer-events: none`。「元に戻す」だけ auto）。

## 5. 開発ルール

- 大きなファイルは**差分編集**（Edit の置換や小さなスクリプト）。全文の書き直しはしない。差分の提示は `git diff` 標準形式（`-`/`+`）。
- 編集後は `node --check`（`index.html` はインラインスクリプトを取り出して検査）。テスト・スクリプトも同様。
- 変更のたびに **`sw.js` の `CACHE_VERSION` を上げる**（`pf.html` もキャッシュ対象）。版を変えないと iPad が更新されない。ただし docs だけの変更は上げない。
- 版番号の採番（履歴からの慣例。明文の規則は無かったのでここに書く）: 画面の動き・機能が変わる段階は**マイナー**を上げる（4b v1.53.0・4c v1.54.0・4d-2 v1.58.0・4e v1.59.0・画面の数字や出力が変わる M2〜M5 v1.60.0・出力の学期の出所と印字 v1.60.2）。不具合修正・診断の追加・表示の直しだけは**パッチ**（v1.53.1・v1.54.1〜3・v1.57.1）。表示される版は `sw.js` の `CACHE_VERSION` が出所（他に版の文字列は無い）。
- `git add` は**ファイル名を明示**（`-A`・`.` は使わない。作業ツリーには過去の未追跡ファイルが多数ある）。
  1段階1コミット以上。機能・テスト／ドキュメント／`sw.js` は別コミット。コミットの末尾に Co-Authored-By 行。
- **push は先生の承認後**。承認前は commit までで停止して報告する。実機の挙動が変わる段階は「バックアップ → 更新 → 確認」の順で案内。
- 報告は簡潔に。**末尾に必ず「先生向けまとめ」**（やさしい言葉で: 先生にとって何が変わるか／iPad での作業／決めること）。
- 調査だけの依頼ではコードを変えない。設計案 → 先生の承認 → 実装の順。判断を仰ぐ項目があれば実装前に止まる。
- テストは**失敗させてから直す**（fail-first）。実装を一時的に壊す**変異テスト**で検出できることも確認する。
- iPad に DevTools はない。確認手段は画面表示（設定の診断カードなど）とスクリーンショットで案内する。
- **入力欄（input・select・textarea）の文字は16px以上**（v1.61.2）: iOS は16px未満の入力欄を押すと画面を拡大する。全体の規則（`input…, select, textarea { font-size:16px !important }`）で保証しているので、新しい入力欄にも特別な指定は要らない（規則を弱める指定を足さない）。`zoom-disable.test.js` が全画面の入力欄を検査する。画面の拡大を止める仕組み（viewport の meta・gesture の処理・touch-action）は、**拡大中はピンチを止めない**設計（戻せなくなるのを避ける）。変えるときは 38.1 の設計と `zoom-disable.test.js` の「拡大中は止めない」の検査を守る。
- `classroom-app` の資料を Artifact で外部公開しない（オフライン・ローカル保存の原則）。チャットかリポジトリ内のファイルで出す。
- 出力（PDF・Excel・CSV）のテストは、`htmlPagesToPdf`・`universalShare` を差し替えるだけにしない。**本番のボタン → html2canvas・jsPDF → 結果ダイアログまで通す**ケースを持ち、題材には本番のデータの種類（検定・専科・新体力など）を含める（v1.60.1: 差し替えのテストが、検定の記録がある場合の `ReferenceError` を見逃した。`radar-pdf-button.test.js` が手本）。`async` のボタン処理は、失敗を画面に通知する（保存や書き出しの失敗は必ず通知する原則。共通の `uiGuard(名前, 関数)` を使う。v1.60.2）。**ボタンの場所と、その出力が使う学期の出所は、画面の実物（HTMLの `id` の位置）で確かめる**（v1.60.0 はレーダーPDFのボタンを成績処理画面にあると思い込んで、学期の出所を間違えた）。**別モジュールの関数を呼ぶときは `window` への公開を確かめる**（eslint `no-undef`＋ブラウザの `typeof` で洗い出せる。`on*` 属性の関数は `day-rollover-refresh.test.js` が確かめる）。
- テストは実際の操作順序・状態遷移（blur の順序、編集・切替・削除後の再描画）を、DOM 直接代入ではなく
  `page.click()`/`type()` と実際の関数呼び出しで再現する。プリセット改修時は「プリセットに戻す」系の経路も grep で監査する。

### テストの実行

```sh
# 別ターミナルで(リポジトリのルートで):
python3 -m http.server 8123
# もう一つのターミナルで:
cd tests && npm install                # 初回のみ
node <名前>.js                         # 単独
node run-all.js                        # 全体(82ファイル。名前規則に頼らず tests/ 直下の *.js をすべて。README の表とも照合)
node run-all.js --write-readme         # テストを追加・変更したら実行して README の表を更新
node run-all.js --only=pf-,roster-     # 一部だけ(名前の一部)。--list で対象の一覧
```

- 全体は `node run-all.js`（82ファイル・2519件・約30分。件数は実行結果と README の表で確認する）。新しいテストの出力形式が読み取れない場合は `run-all.js` の `parseCounts` に形式を追加する。Chrome は `/Applications/Google Chrome.app`。
- 環境変数: `ONLY=H1,H2…`（data-reflection）、`PFM_ONLY=1,4`（pf-migration の章指定）、
  `ROSTER_SHIFT_FORCE=1`（roster-shift の実適用を強制）、`PF_BASE=http://…/`（pf-landing の接続先）。
- **同じテストを並列に走らせない**（一時ファイル名の衝突）。テストの追加・`index.html`/`pf.html` の変更中に全体実行をしない。
- 落とし穴: 日本語入力欄の置き換えは `element.select()`（トリプルクリックは不可）。通知の確認は `#toast.show #toastMsg`
  （`document.body.textContent` はスクリプトの文字列を含み常に真）。再読み込み後の通知は数秒遅れて出るので、
  次の操作の前に `#toast` が消えるのを待つ。テストの初期データの児童IDは固定する（起動時の採番で毎回変わるため）。

## 6. テスト専用フック・内部フラグ

| 名前 | 場所 | 用途 |
|---|---|---|
| `window.__spaSkipPfMigration = true` | `index.html`（`migratePfStudentIdV1` の先頭） | 起動時の pf 新方式への移行を無効にする。**テスト専用**。`page.evaluateOnNewDocument` で再読み込みをまたいで設定する（`roster-pf`・`backup-restore` が使用）。戻すときは `removeScriptToEvaluateOnNewDocument` |
| `window.__spaWasWiped` | `index.html` | 内部フラグ（直前が退勤モード消去か）。`idbGhostPurgeDue` が参照。テストで書き換えない |
| `window.__pendingIdbDeletes` | `index.html` | 内部。IndexedDB の準備前に検証付きで削除したキーの一覧（準備後に鏡から消す）。テストの seed 前に `[]` へ戻すことがある |
| `window.__fault` / `window.__marker` / `__w` など | テスト側で定義 | 失敗注入・再読み込み検出・書き込み監視。アプリ本体には無い |

- 公開している内部関数（テストから呼ぶ）: `applyRosterChange`・`restoreArchivedStudent`・`undoRosterChange`・`brRestoreFromBackup`・
  `brPlanRestore`・`buildBackupObject`・`planPfMigration`・`migratePfStudentIdV1`・`pfDiagnose`・`grdImportFitnessTest`・
  `StudentRemap`（`planRosterChange` など）。失敗注入は `Storage.prototype.setItem/removeItem` の差し替えで行う。
- `tests/helpers/roster-ui.js`（名簿UIの操作）と `tests/helpers/roster-data.js`（18ストアの合成データ・持ち主の目印・6操作）が共通ヘルパー。

## 7. 主なキーと関数の地図

- キー: `spa_master`（名簿・classInfo）、`spa_student_archive`（退避）、`spa_roster_snapshot`/`spa_roster_txn`（取り消し点・
  ジャーナル。バックアップに含めない）、`pf_roster`・`pf_records_年度`・`pf_fitness_年度`・`pf_setting`（新体力テスト。`StorageManager` を通らず
  localStorage 直）、`migration_studentId_v1`・`migration_pfStudentId_v1`（完了の印。診断カードが表示）。
- 名簿系は `index.html` の MASTER モジュール（`applyRosterChange`・`srRunTransaction`・`planPfChange`/`planPfChangeV2`・
  `recoverRosterTxnOnStartup`）、復元は BACKUP モジュール（`brRestoreFromBackup`・`importBackup`）、pf 画面は末尾の `pf_*` 関数群。
- 起動順: `DOMContentLoaded` で同期的に 復旧 → 各種移行 → `loadMaster` → `migrateStudentIdsV1` → `migratePfStudentIdV1`、
  そのあと非同期で IndexedDB（`migrateFromLS` → `loadCache` → `flushPendingIdbDeletes`）。詳細は 20.2。

## 8. 次の一手（推奨。2026-09-20 時点の優先順位）

0. **v1.61.2（画面の拡大を止める）と v1.61.1（レーダーPDFの提出物の表記）の iPad 確認**: 下の「iPad 確認リスト（v1.61.2）」4項目を見てもらう（4 に v1.61.1 の分を含めた）。
0. （v1.61.1 の iPad 確認は、下のリスト4に統合。単独のリストは前回分として残す）
0. **（確認済み 2026-09-20）v1.61.0（M7・M9・小修正2件）の iPad 確認**: 下の「iPad 確認リスト（v1.61.0）」5項目すべて OK（専科の既存値が1学期に入っている・値のない学期が「未入力」・通年は学期名つき・明細CSVのファイル名に学期）。
1. **（確認済み 2026-09-20）v1.60.4（L7 案C・案B・L5 案D）の iPad 確認**: 版表示 v1.60.4 OK／診断の「主体性（得点方式）の空欄・文字の記録」0件・「提出記録の studentIndex 範囲超過」0件（児童数27）・総合判定 🟢（すべて OK）。欠席→解除と成績処理が同じかは報告なし。（v1.60.3 も確認済み: 版表示・Numbers の B+/B/B-。v1.60.2 まで（L10・M2〜M5・レーダーPDF・出力の学期・v1.58.0 の書き出しと復元後の確認）も確認済み。）
2. **（決定済み）M3 の学期の基準**: 指示は「成績画面で選択中の学期」だったが、面談テキストの成績は H1（v1.50.0）から**出力画面の学期**で計算しており、成績画面の学期にすると H1 を巻き戻してカルテPDFと食い違うため、出力画面の学期に統一した。先生に確認し、「出力画面の学期（推奨）」で確定（DATA_FLOW_AUDIT.md 30.1）。
3. **（v1.61.0 で対応済み・push 済み・iPad 未確認）M7・M9**（専科成績の学期・成績の明細CSV。DATA_FLOW_AUDIT.md 36章）。残りは **M2〜M5 の残り**（3章の (a)〜(e)）と、M7 の記録のみの点（3章）。
4. **L 項目**（L1〜L4・L6 と、L8＝診断の確認済み項目を情報表示から外す案。Step4-D もここ）。困りごとが出てから。**L5・L7 は v1.60.4 で対応済み・iPad 確認済み（診断0件）。案A（主体性の得点方式で空文字・文字を未評価にする）と案E（課題別の人数を名簿の人数までに絞る）は「該当なし・当面不要」**（3章に再開の条件を書いた）。
5. H4 の後片付け（急がない）: DATA_FLOW_AUDIT.md の 7.5（実施順）を実施済みに更新／`pf-residual`（4c の単体復元。アプリ本体の機能なので残す前提）の扱いの見直し。
- そのほか: 取り消しの実機確認（v1.57.0）を、先生が次に名簿や入力形式を直す機会に頼む。

### iPad 確認リスト（v1.61.2）

**先生への案内は、やさしい言葉で・1項目2行以内**（今回は画面の動きだけの変更で、成績の数字・保存したデータは変わらない。4項目）。

1. **【更新の前】設定の「バックアップ」を保存する。** 念のための保存です（今回は画面の動きだけの変更で、保存したデータは書き換わりません）。
2. **【更新のあと】版表示が「v1.61.2」になっている。成績処理の評定が、更新の前と同じ。** 成績の数字は変わっていないのが正解です。
3. **画面が拡大しないことを見る。** 2本の指で広げても、ダブルタップしても、点数や名前の入力欄を押しても拡大しない（座席表のドラッグと一覧のスクロールは、いつもどおり動く。もし拡大したら、2本指で縮めると戻ります）。
4. **【前の版（v1.61.1）の分】個人カルテ（出力）で「面談用レーダーPDF」を作って開く。** 提出物が「評価点 3.6/6（60%）」の形で出る（以前の「提出率 60%(3/6)」ではない）。

### 前回（v1.61.1）の iPad 確認リスト（結果は未報告。3 は今回の 4 に含めた）

**先生への案内は、やさしい言葉で・1項目2行以内**（今回は見た目の文字だけの変更で、成績の数字は変わらないため、3項目）。

1. **【更新の前】設定の「バックアップ」を保存する。** 念のための保存です（今回は文字の表示だけの変更で、保存したデータは書き換わりません）。
2. **【更新のあと】版表示が「v1.61.1」になっている。成績処理の評定・観点別が、更新の前と同じ。** 成績の数字は変わっていないのが正解です。
3. **個人カルテ（出力）で「面談用レーダーPDF」を作って開く。** 1ページ目の「提出物」の行が「評価点 5.6/8（70%）」のように、2ページ目の各教科のカードが「提出物：評価点 3.6/6（60%）」のように出る（以前の「提出率 60%(3/6)」ではない）。%の数字は前と同じ。

### 前回（v1.61.0）の iPad 確認リストと結果（v1.60.4 の結果は末尾に残す）

**結果（2026-09-20・先生の報告）**: 1〜5 **すべて OK**（NG なし）。明細CSVの中身と、専科の教科が成績処理CSVの選択肢に出ることは、報告なし（未確認・省略可）。

**先生への案内は、やさしい言葉で・1項目2行以内**（内容は下のとおり）。

1. **【更新の前】設定の「バックアップ」を保存する。** 専科の値を新しい形で保存するため、念のための保存です（保存したファイルは、更新して問題がなければ消して構いません）。
2. **【更新のあと】版表示が「v1.61.0」になっている。成績処理の1学期の評定・観点別が、更新の前と同じ。** 専科（音楽など）の1学期の値も、前と同じ。
3. **成績処理 →「🎓 専科入力」を開く。** 上に緑の帯「いま入力中：〇学期」と、学期の切り替えが出て（今の学期が選ばれている）、児童の名前の横にも学期の札が付く。前に入れた専科の値は「1学期」に切り替えると入っていて、「2学期」に切り替えると空になっている（まだ入れていないため）。
4. **成績処理 →「成績一覧」で専科の教科を選び、右上の学期を 1学期 → 2学期 → 通年 と切り替える。** 1学期は前と同じ値、値を入れていない学期は「未入力」、通年は「B（2学期）」のように学期つきで出る（同じ画面から出す PDF・CSV も同じ）。
5. **データ出力 →「🔎 成績の明細（項目ごと）」で 1学期・国語 を「📥 成績の明細 CSV 出力」する。** ファイル名に「1学期」が入り、開くと先頭に対象学期の説明があって、1行ごとに項目・満点・10点換算・重みが並んでいる。あわせて、下の「成績処理統合」の教科の選択肢に、専科の教科（音楽など）も出る。

### 前回（v1.60.4）の結果と、未確認のまま残っている小さな項目

**結果（2026-09-20・先生の報告）**: 版表示 v1.60.4 OK／診断の「主体性（得点方式）の空欄・文字の記録」0件（課題0件・児童0人）／「提出記録の studentIndex 範囲超過」0件（児童数27）／総合判定 🟢。**すべて OK**。確認リストの3（欠席→解除）と、成績処理の評定が同じかは報告なし（未確認・省略可）。

**未確認のまま残っている小さな項目（省略可）**:
- (オ)【v1.60.4】📝児童の記録で、まだ点数を入れていない課題を開き、ある児童の「欠席」を押し、続けて「欠席中」を押して戻す → その児童の行に ✓ が付かず、上の「〇 / 〇 人入力済み」も増えない（前は ✓ が付き、主体性の得点方式の課題だと0点で計算されていた。成績処理の評定・観点別は前と同じのが正解）。
- (ア) 👤個人カルテの出力画面で「子ども向け」のカルテPDFを1人分出すと、表紙に「１学期のきろくです」（通年のときは「通年（１年間）のきろくです」）と出る。
- (イ) 📊「Excel（根拠）」のファイル名に「1学期」が入り、「評定一覧」シートの一番上の題名に「（1学期）」と出る。
- (ウ) 🔍監査診断の「データ量サマリー」で、テスト総数が「まとめ形式 30件＝まとめテスト12＋単元テスト18」、下の「testType 分布」のまとめテストが13件（＝12件＋新体力テスト総合1件）。
- (エ) 昔のブックマークの「…/pf.html」を開くと、案内だけが出る。
