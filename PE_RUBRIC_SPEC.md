# 体育「実技ルーブリック採点」実装仕様書

対象リポジトリ: `yymameda/classroom-app`（`index.html` 単一ファイル / 現行 v1.14.2）
目的: 6種目の体育採点を **Excelを使わずアプリ内だけで完結**させる。

---

## 0. 開発ルール（厳守）

- `index.html` は約18,400行の単一ファイル。**必ず `str_replace` による差分編集**を行う。`create_file` での全文書き換えは禁止。
- 変更をプッシュするたびに `sw.js` の `CACHE_VERSION` をインクリメントする（v1.14.2 → v1.15.0 → …）。
- プッシュ前に必ず `node --check index.html` 相当（`<script>`部の構文確認）を通す。
- テストは `tests/` 配下、puppeteer-core を使用。実行手順:
  ```
  python3 -m http.server 8123   # リポジトリルートで
  cd tests && node <name>.test.js
  ```
  テストは `http://localhost:8123/index.html` を参照する。`tests/` に `index.html` や `sw.js` をコピーする必要はない。
- コミット対象は `package.json` / `package-lock.json` / テストスクリプト / `README.md` のみ。`tests/node_modules/` は gitignore 済み。
- トークン節約のため、既存コードの読み込みは必要な関数の周辺のみに絞る。
- 本リポジトリの成果物（仕様書・チェックリスト・検証スクリプト等）は Artifact 等による外部ホスティングを行わず、ローカルとリポジトリ内に留める。児童データを扱うアプリの設計原則（オフライン・ローカル保存のみ）と整合させるため。

### 既存コードの主な接続点（行番号は v1.14.2 時点。着手時に必ず `grep` で再確認すること）

| 箇所 | 概算行 | 役割 |
|---|---|---|
| `recPeFields` の単位 `<select>` | 2646-2654 | 実技記録の単位選択UI |
| `KenteiPresets` | 7276 | なわとび／泳力のプリセット定義 |
| `kenteiGetPresetId()` | 7389 | `peUnit` 文字列 → プリセットID |
| `kenteiGetConfig()` | 7398 | test → config |
| `recBuildNawatobiCard()` | 7509 | ○×タップUI（流用元） |
| `recRenderList()` | 6861 | 入力一覧のレンダリング分岐 |
| 統計表示 `isNawatobi` 分岐 | 7121-7180 | 課題別の統計 |
| `recAddTest()` | 8240 | 課題登録。`isPE ? 9999` の箇所を含む |
| `recOpenKenteiTableModal()` | 8540 | 換算表編集モーダル |
| `peRecordToScore10()` | 12698 | **10点換算の唯一の入口** |
| `grdExtKenteiLabel()` | 13199 | 成績カードのラベル生成 |
| 体育の成績カード | 13236 | `testType === '実技記録'` を拾う箇所 |
| 監査・根拠表示 | 14169 / 14447 / 16559 | `kenteiConfig` を参照している |

---

## 1. 設計方針

### 1.1 成績側はほぼ改修しない

`peRecordToScore10()` は検定カード型のとき、保存済みの `sr.score10` を最優先で返す設計になっている（12703行）。

```js
if (typeof sr.score10 === 'number') return sr.score10;
```

新モジュールも **入力画面側で10点満点まで計算し `score10` に保存する**方式に揃える。これにより成績算出・観点別評価・保護者面談PDFはすべて既存の配線に乗る。`peRecordToScore10()` への追加は先頭の分岐1本のみ。

```js
function peRecordToScore10(sr, test) {
    if (!sr) return null;
    // ★追加: 実技ルーブリック型
    if (test.peRubric) return typeof sr.score10 === 'number' ? sr.score10 : null;
    // === 検定カード型(なわとび・泳力など) ===
    var kConfig = kenteiGetConfig(test);
    ...
```

### 1.2 単位（`peUnit`）による型判別

既存の `検定:` プレフィックスと並行して `実技:` プレフィックスを導入する。

| `peUnit` の値 | 種目 |
|---|---|
| `実技:volley` | ソフトバレーボール |
| `実技:mat` | マット運動 |
| `実技:vault` | 跳び箱運動 |
| `実技:hurdle` | ハードル走 |
| `実技:bar` | 鉄棒運動 |
| `検定:swimming` | 水泳（**既存機能を流用。新規実装なし**） |

```js
function peRubricGetPresetId(peUnit) {
    if (!peUnit) return null;
    if (peUnit.indexOf('実技:') === 0) return peUnit.substring(3);
    return null;
}
```

`recAddTest()` では、`実技:` の場合も検定カードと同様に `test.category = '知識・技能'` を強制し、`test.peRubric = peRubricBuildFromPreset(pid)` を格納する。

### 1.3 得点計算の共通ルール

すべての種目で共通:

1. 各項目（グループ）が **0〜10点** を返す
2. 合成方式は `average`（平均）または `table`（換算表引き）
3. 加点があれば合計に加算
4. **上限10点でクリップ**
5. 小数第2位で丸めて `score10` に保存（例: 8.67 + 0.5 = 9.17）

```js
function peRubricCalcScore10(config, data) {
    // → { itemScores: [...], bonus: n, total: n }  totalは min(10, round2(...))
}
```

`sr.score` にも同じ値を入れておくこと（既存の一覧表示や統計が `sr.score` を参照しているため）。

---

## 2. データモデル

### 2.1 test 側

```js
test.peRubric = {
    presetId: 'mat',
    name: 'マット運動',
    emoji: '🤸',
    combine: 'average',          // 'average' | 'table'
    items: [ /* 2.2 参照 */ ],
    bonus: [ /* 2.3 参照 */ ],
    cap: 10,
    conversionTable: null        // combine==='table' のときのみ使用
};
```

`kenteiConfig` と同様に **課題ごとにスナップショット**する。プリセット定義を後から変えても既存課題の採点基準は変わらない（学校の実態に合わせて基準値を調整できる余地を残すため）。

### 2.2 item（評価項目）

```js
{
    id: 'g1',
    name: '①前転グループ',
    hint: '基本：前転／発展：開脚前転・倒立前転(補助あり)',
    mode: 'level' | 'trial' | 'number',
    levels: [ { label: '未達成（3回中0〜1回成功）', score: 0 }, ... ],  // mode==='level'
    blocks: [ { id:'yoko', name:'横', trials:3 }, ... ],                // mode==='trial'
    fields: [ { id:'t50', name:'春先50mタイム', unit:'秒', step:0.1 } ],// mode==='number'
    resolver: 'exampleResolver' // blocks/fields から level を決める関数名（2.4）。現在どのプリセットも未使用
}
```

### 2.3 bonus（加点）

```js
{ id: 'b1', name: '大きな前転', mode: 'trial', trials: 3, stableAt: 2, value: 0.5 }
```

「安定」= 指定試技数中 `stableAt` 回以上成功。加点は**合計に対して1回だけ**加算する種目（跳び箱・鉄棒）と、**加点ごとに独立して加算**する種目（マット）がある。プリセット側で `bonusMode: 'each' | 'any'` を持たせる。

- `each`（マット）: 該当する加点それぞれを加算（最大 +1.0）
- `any`（跳び箱・鉄棒）: いずれか1つでも満たせば1回だけ加算（+0.5）

### 2.4 resolver 一覧

プリセットが参照する判定関数。`PeRubricResolvers` オブジェクトに集約する。

| resolver | 入力 | 出力 |
|---|---|---|
| ~~`vaultLevel`~~ | ~~横/縦/発展 の各3試技○×~~ | ~~Lv 0〜3~~ |
| ~~`barBasic`~~ | ~~静止技5種の秒数 + 振動技4種の成功回数~~ | ~~Lv 0〜5~~ |
| ~~`barUp`~~ | ~~膝掛け振り上がり/補助逆上がり/逆上がり の成功回数~~ | ~~Lv 0〜4~~ |
| ~~`barTurn`~~ | ~~ぜんめんしじ⇔はいめんしじ/前方支持回転(補助あり)/(補助なし)~~ | ~~Lv 0〜4~~ |
| ~~`barDown`~~ | ~~まえまわりおり/ふとんほしおり/片足踏みこし下り/両ひざかけ振動おり~~ | ~~Lv 0〜3~~ |
| ~~`hurdleTime`~~ | ~~春先50mタイム・ハードルタイム~~ | ~~得点 1〜10~~ |
| ~~`volleyCount`~~ | ~~5方向の○×~~ | ~~得点 1〜10~~ |

> **v1.15.0改訂: `volleyCount` は未使用**。resolverは「trial/compositeの生データ→単一itemのレベル値」を作り`combine:'average'`で平均するための仕組みだが、ソフトバレーは`combine:'table'`で項目が1つしかなく、item単位のレベルを出して平均する工程自体が存在しない。`peRubricCalcScore10()`の`table`分岐が「唯一のitemの成功数を数えて`conversionTable`を引く」処理を汎用的に持てば済み、resolverを介すると同じ計算を2箇所に書くことになる。§4.1は`combine:'table'`で代替する。行自体は経緯を残すため削除せず取り消し線に留める。
>
> **改訂: `vaultLevel`／`barBasic`／`barUp`／`barTurn`／`barDown`／`hurdleTime` も未使用**。跳び箱・ハードル・鉄棒はいずれも当初`trial`/`number`/`composite`＋専用resolverで設計されていたが、実装時にマット運動と同じ`mode:'level'`（教員が到達レベルを1タップで選ぶ方式）に統一されたため、resolverが1件も必要にならなかった。詳細は§4.3/4.4/4.5の「設計変更の経緯」を参照。`volleyCount`と同じ理由で行は削除せず取り消し線に留める。

### 2.5 score 側

```js
{
    id, studentIndex, testId,
    rubricData: {
        items: { g1: 1, g2: 2, g3: 0, g4: 0 },       // mode により形が変わる（2.6）
        bonus: { b1: [1,1,0], b2: [0,0,0] }
    },
    score10: 7,
    score: 7,
    createdAt
}
```

### 2.6 `rubricData.items[itemId]` の形

| mode | 形 |
|---|---|
| `level` | 整数（`levels` のインデックス） |
| `trial` | `{ dir:[1,0,null,null,null] }`（blockごとに `trials` 数分の固定長配列。`0`=×, `1`=○, `null`=未実施） |
| `number` | `{ t50: 9.2, thurdle: 10.1 }` |

未入力は `null`（またはキー不在）とし、**未入力の項目は0点ではなく「未評価」**として扱う。全項目未入力なら `score10` を保存しない（＝成績に反映されない）。

> **v1.15.0実装確定: `undefined` ではなく `null` を使う**。`recSaveScores()` は保存時に `JSON.stringify()` を通すため、配列要素の `undefined` はどのみち `null` に変換される（`JSON.stringify([1,undefined,0])` → `"[1,null,0]"`）。メモリ上の値と保存後の値を一致させるため、未実施は最初から `null` で統一する。判定側（`peRubricTrialStatus()` の `v === 0 || v === 1`）は `null`・`undefined` のどちらも等しく「未入力」として弾くため、完了ゲートの正しさには影響しない。

> **v1.15.0改訂: 部分入力時の確定値算出条件**
> 上記の初版では「一部だけ入力されている場合は、入力済み項目のみで平均を取り、その旨をカードに表示する」としていたが、これは**完了ゲート**（下記）に置き換える。
>
> 理由: `combine:'table'` のソフトバレーでは、5方向中一部だけタップした状態の生カウントが、全数実施した場合の正しいカウントと換算表上で区別できない（例: 2方向だけタップして両方○ → 生カウント2 → 換算表の「2→5点」に一致するが、これは「5方向中2方向成功」という正しい記録と見分けがつかない）。`peRecordToScore10()` は `sr.score10` が数値かどうかしか見ておらず、`grdCalculate()`（知識・技能平均→3段階評価→通知表・保護者面談PDF）も完了/未完了を区別する手段を持たないため、**書き込み側で確実にゲートする**必要がある。
>
> **完了(complete)の定義**（modeごと）:
> - `level`: 値が1つでも入っていれば完了（1回のタップで確定するため常に真偽二値）
> - `trial`: そのitemが持つ**すべての**トライアル枠に明示的な `0`/`1` が入っていれば完了。未実施の枠は `null`（上記2.6実装確定を参照）とし、`0`（×＝明示的な失敗）と区別する。タップは「未実施→○→×→未実施」の**3状態サイクル**とする（既存 `recNwToggle()` の2状態トグルをそのまま流用しない。2状態だと「未実施」と「×」が区別できない）
> - `number`: そのitemが持つ**すべての**`fields`に有効な値が入っていれば完了
> - テスト全体としては、`test.peRubric.items` の**全item**が完了して初めて「全項目完了」
>
> **確定値の保存条件**: `peRubricCalcScore10()` は常に `{ itemScores, bonus, total, complete }` を返す。`complete` が false でも `total` は計算する（§3.3のリアルタイム表示用プレビュー）。**`sr.score10`／`sr.score` に書き込むのは `complete === true` のときのみ。** `complete === false` の間はこの2キーを保存しない。既存の値があれば、入力を取り消して未完了に戻した時点で削除する。一部入力時の平均値はUIのプレビュー表示にのみ使い、確定値としては一切保存・出力しない。
>
> **bonus は完了条件に含めない**（加点技は全員が実施するとは限らないため。未評価は加点なしとして扱う）。主要itemと違い、加点は「できる子だけが挑戦する」性質のものであり、これを完了条件に含めると挑戦しなかった児童が永久に `complete=false` のままになり、`score10` が保存されなくなる（＝知識・技能の母数から静かに外れる）。`complete` は `test.peRubric.items` の全item完了のみで判定し、`config.bonus` の各エントリは未評価なら単に加点0として扱う。

---

## 3. 入力UI仕様

### 3.1 全体

`recRenderList()` に `isPeRubric` 分岐を追加し、児童1人＝1カードで描画する（既存の `rec-nawatobi-card` のCSSを流用・拡張）。iPad横向き（Smart Keyboard Folio装着時）での操作を前提に、**タップ対象は最低44px四方**を確保する。

### 3.2 モード別UI

- **level** … 到達レベルのボタン列（プルダウンではなくボタン。タップ数を減らすため）。選択中は色付き。
- **trial** … 既存の `rec-nw-skill-btn` と同じタップトグル。3試技を横並び。○（成功）＝緑、×（失敗）＝グレー、未実施＝枠線のみ。ブロック右端に「安定」バッジを自動表示。
- **number** … 数値入力欄。`inputmode="decimal"` を必ず指定する（iPadで小数点キーを出すため）。入力に応じて判定結果をリアルタイム表示。
- **composite**（鉄棒）… 技を4グループに折りたたみ表示。静止秒数は数値入力、それ以外は 0〜3 のボタン。

### 3.3 リアルタイム表示

各項目の右に得点、カード下部に「グループ平均 → 加点 → 最終得点」を常時表示する。Excelの集計シートと同じ情報がその場で見えることが、この改修の主目的である。

> **v1.15.0改訂**: この表示値は §2.6 の `complete` に関わらず常に計算・表示するプレビューである。`complete === false`（未完了）の間は、確定時の緑チェック等とは明確に異なる見た目（グレー・破線などの「入力中」バッジ）を併記し、教員が「まだ確定していない数字」であると一目でわかるようにする。

### 3.4 基準値の編集

`recOpenKenteiTableModal()` と同じ要領で「採点基準の編集」モーダルを用意する。編集対象:

- 各 level の点数
- 「安定」と判定する成功回数（`stableAt`）
- 静止秒数の目安（鉄棒）
- タイム差の区分（ハードル）
- 加点の値

Excelの「設定」シート・「基準表」シートに相当する。**保存時は該当課題の全児童の `score10` を再計算する**（`recKenteiRecalcScores()` と同じパターン）。

> **v1.15.0改訂**: 再計算（`peRubricRecalcScores(test)`）でも §2.6 の完了ゲートを適用する。既存 `rubricData` が `complete===false` のレコードに対しては `score10` を書き込まない。基準編集をきっかけに未完了データが確定してしまう経路を作らない。

---

## 4. 種目別プリセット定義

> 以下の点数はすべて元Excelの「本校の実態に応じた目安」であり、指導要領に明記された基準ではない。3.4のUIで調整可能にすること。

### 4.1 ソフトバレーボール（`実技:volley`）

- 項目1つのみ。5方向（①真正面 ②右 ③左 ④前寄り ⑤後ろ寄り）を各1回、○×で入力。
- 成功数 → 得点（`combine: 'table'`）

| 成功数 | 5 | 4 | 3 | 2 | 1 | 0 |
|---|---|---|---|---|---|---|
| 得点 | 10 | 9 | 7 | 5 | 3 | 1 |

- 加点なし。
- 判定基準（UIに注記表示）: センターサークル内（線上含む）にノーバウンドで返球できれば成功。パスの種類は問わない。投げ手は児童から5m、山なりで胸〜顔の高さ（床から約1.5〜1.8m）。

### 4.2 マット運動（`実技:mat`）

- `combine: 'average'`、`bonusMode: 'each'`、4項目すべて `mode: 'level'`

| 項目 | 基本 | 発展 | levels |
|---|---|---|---|
| ①前転グループ | 前転 | 開脚前転／倒立前転(補助あり) | 0 / 6 / 10 |
| ②後転グループ | 後転 | 開脚後転／伸しつ後転 | 0 / 6 / 10 |
| ③倒立回転グループ | 側方倒立回転とび | （加点技へ切り出し） | **0 / 10 の2段階** |
| ④平均立ちグループ | 壁倒立 | 頭倒立 | 0 / 6 / 10 |

levels ラベル:
- `未達成（3回中0〜1回成功）` = 0
- `基本の技が安定してできる（3回中2回以上成功）` = 6
- `発展技が安定してできる（3回中2回以上成功）` = 10
- ③のみ `安定してできる（3回中2回以上成功）` = 10

加点（各 +0.5、`mode: 'level'` の2択でよい）:
- 大きな前転
- 前方倒立回転跳び

最終得点 = min(10, 平均 + 加点合計)

**検算**: ①6 ②10 ③10 ④0 → 平均6.5、加点①のみ → **7**

### 4.3 跳び箱運動（`実技:vault`）

- `combine: 'average'`、`bonusMode: 'any'`、3項目すべて `mode: 'trial'` + `resolver: 'vaultLevel'`
- 各項目に3ブロック（横／縦／発展技）、各3試技。安定 = 3回中2回以上（`stableAt: 2`）

| 項目 | 横・縦 | 発展技 |
|---|---|---|
| ①開脚跳び | 開脚跳び | 大きな開脚跳び |
| ②かかえ込み跳び | かかえ込み跳び | 屈伸跳び |
| ③台上前転 | 台上前転 | 伸膝台上前転 |

`vaultLevel` の判定:
```
発展技が安定           → Lv3 = 10
横・縦の両方が安定     → Lv2 = 8
横・縦のどちらか一方   → Lv1 = 6
どちらも未達           → Lv0 = 0
```

加点（④、いずれか安定で +0.5）: 首はね跳び／頭はね跳び（各3試技）

最終得点 = min(10, 平均(①②③) + 加点)

**検算**: ①8 ②8 ③10 → 平均8.67、加点あり → **9.17**

注記（UI表示）: 全ての技は4段以上で実施。

### 4.4 ハードル走（`実技:hurdle`）

- `combine: 'average'`、`bonusMode: 'each'`、`cap: 10`、`displayMode: 'modal'`、3項目すべて `mode: 'level'`、加点なし（`bonus: []`）

| 項目 | levels（点） |
|---|---|
| ①インターバルのリズム | 1 / 3 / 6 / 10 |
| ②踏み切りと走り越し | 1 / 3 / 6 / 10 |
| ③接近度（タイム差） | 3 / 6 / 10 |

**①インターバルのリズム**（決めた歩数（3歩または5歩）で走り越せているかを見る）
- `ハードルに向かって走っている` = 1
- `ハードルを走り越せる` = 3
- `決めた歩数で走り越せる` = 6
- `最後のハードルまで同じリズムを保てる` = 10

**②踏み切りと走り越し**（遠くから決めた足で踏み切り、低く走り越せているかを見る）
- `ハードルに向かって走っている` = 1
- `手前で止まらずに走り越せる` = 3
- `遠くから踏み切り、低く越えられる` = 6
- `上下動が小さく、着地後すぐ走り出せる` = 10

①②とも最下段は0点ではなく1点にしている。「ハードルに向かって走っている」だけでも取り組んだ事実は残るため、走ったのに0点になることを避ける趣旨。

**③接近度（タイム差）**（40mハードル走のタイム − 40m走のタイム。教員が計算し、levelを選ぶ。`mode:'number'`・`resolver`は使用しない）
- `記録が取れている` = 3
- `2.0秒以内` = 6
- `1.2秒以内` = 10

1.2秒の根拠: 40m走のタイム + 0.3秒×4台（ハードル4台分）。東京都教職員研修センターの第5学年学習指導案、新潟県の指導資料の双方で共通して使われている目安。

最終得点 = 平均(①②③)（加点なし）

**検算**: ①10 ②6 ③10 → 平均8.67 → **8.67**

#### 設計変更の経緯（旧: 指標1「そろった箇所数」+指標2「タイム差自動判定」の2軸構成からの変更）

旧設計（そろった箇所数による指標1、春先50mタイムとハードルタイムの差分を`mode:'number'`+`resolver:'hurdleTime'`で自動判定する指標2、の平均）は撤回し、上記に置き換える。

- 春先50mタイムを別課題から自動参照する仕組みは実装されておらず、旧343行目の「設計上の判断が必要な点」で選んだ「まずは手入力」も見送られた。教員が毎回2つの数値を入力する運用負荷が大きかった。
- タイム差の計算自体は教員が行い、結果をlevelボタンで選ぶ方式（`mode:'level'`）に簡略化した。`mode:'number'`・`resolver:'hurdleTime'`は未使用。
- 指標を1つ増やし（①②の2項目→①②③の3項目）、他種目（マット・跳び箱・鉄棒）と同じ「複数項目の平均」という構成に揃えた。

### 4.5 鉄棒運動（`実技:bar`）

- `combine: 'average'`、`bonusMode: 'each'`、4項目すべて `mode: 'level'`（マット運動と同型。resolver不要）

文部科学省「領域別系統表（1）知識及び技能 B器械運動系」の公式グループ区分に準拠する。
https://www.mext.go.jp/sports/content/202309293-spt_sseisaku02-000022053_322.pdf

| 項目 | 基本 | 発展 | levels |
|---|---|---|---|
| ①前転グループ | 前回り下り／かかえ込み前回り／転向前下り | 前方支持回転／片足踏み越し下り | 0 / 6 / 10 |
| ②前方足掛け回転グループ | 膝掛け振り上がり／前方片膝掛け回転 | 膝掛け上がり／前方もも掛け回転 | 0 / 6 / 10 |
| ③後転グループ | 補助逆上がり／かかえ込み後ろ回り | 逆上がり／後方支持回転 | 0 / 6 / 10 |
| ④後方足掛け回転グループ | 後方片膝掛け回転／両膝掛け倒立下り | 後方もも掛け回転／両膝掛け振動下り | 0 / 6 / 10 |

levels ラベル（4グループ共通）:
- `未達成（3回中0〜1回成功）` = 0
- `基本の技が安定してできる（3回中2回以上成功）` = 6
- `発展技が安定してできる（3回中2回以上成功）` = 10

秒数基準や「1回成功」のような中間段階は設けない。「3回中2回以上成功」の一本のみで判定し、教員による判定の揺れをなくす。

加点（`bonus` 配列1項目、`mode: 'level'` の2択、`bonusMode: 'each'`）:
- 加点技（★更なる発展技）: 前方伸膝支持回転／横とび越し下り／もも掛け上がり／後方伸膝支持回転 のいずれか1つが安定 → +0.5
- levels: `未達成` = 0 ／ `安定してできる（3回中2回以上成功）` = 0.5
- 4技を個別に判定するのではなく、1つの加点技として一括判定する（マット・跳び箱と同じ、`bonusMode: 'any'`は未使用のため避ける）
- ④後方足掛け回転グループには★更なる発展技が存在しない（対象技なし）

最終得点 = min(10, 平均(①②③④) + 加点)

**検算**: ①発展(10) ②基本(6) ③発展(10) ④基本(6)、加点あり(+0.5) → 平均8、加点0.5 → **8.5**

#### 設計変更の経緯（旧: 基礎感覚／上がり技／支持回転技／下り技グループ構成からの変更）

旧設計（静止秒数基準・多段階得点・`mode:'composite'`）は撤回し、上記に置き換える。

- 旧グループ区分（①基礎感覚／②上がり技／③支持回転技／④下り技）は学習指導要領解説の例示技の並べ方であり、系統表の公式グループ区分ではなかった。系統表の公式グループ（①前転／②前方足掛け回転／③後転／④後方足掛け回転）に合わせることで、マット運動プリセット（①前転／②後転／③倒立回転／④平均立ち）と設計思想が揃う。
- 秒数基準や「1回以上成功」という中間段階は教員による判定の揺れの原因になるため廃止し、「3回中2回以上成功」の一本に統一した。
- 基礎感覚グループ（だんごむし・ふとんほし等）は、系統表では低学年の「鉄棒を使った運動遊び」に位置づけられるため、5年生の評価対象から外した。
- この結果、実装方式は `mode:'composite'` ＋ 専用resolver4種（`barBasic`/`barUp`/`barTurn`/`barDown`）ではなく、マット運動と同じ `mode:'level'` の再利用で済む（新規resolver不要）。**§2.4のresolver一覧・§3.2/3.4のcompositeモード関連記述は、本改訂の対象外（別途要更新）。**

### 4.6 水泳（`検定:swimming` — 既存機能）

**新規実装は不要**。既存の泳力検定カードの `stage` と、Excelの到達段階が完全に一致している。`KenteiPresets.swimming.defaultConversionTable()` を下記に差し替えるだけでよい。

| stage | 級 | 10点換算 |
|---|---|---|
| 0 | － | 1 |
| 1〜6 | 18〜13級（ホップ） | 2 |
| 7〜12 | 12〜7級（ステップ） | 4 |
| 13・14 | 6・5級 | 7 |
| 15・16 | 4・3級 | 8 |
| 17・18 | 2・1級 | 9 |
| 19〜24 | マスター6〜1 | 10 |

Excelの「クロールと平泳ぎの高い方を採用」というルールは、検定カードが累積式（前グループを全て達成しないと次に進めない）であるため自動的に満たされる。

既存課題の `kenteiConfig` はスナップショット済みなので、この変更は過去データに影響しない。

---

## 5. 同時に修正すべき既存の不具合

### 5.1 【重大】実技記録の10点換算が常に0になる

`recAddTest()`（8256行付近）で実技記録の満点が 9999 に固定されている。

```js
var maxScore = isAttitude ? 10 : (isABC ? 0 : (isPE ? 9999 : ...));
```

一方 `peRecordToScore10()` の末尾は `score / maxScore * 10` で換算するため、単位「点」「秒」「回」「m」「cm」で記録した実技記録は **すべて0点として知識・技能の平均に混入する**。入力欄には値が見えているため気づきにくい。

修正: `peRecordToScore10()` に単位「点」の分岐を追加する。

```js
if (test.peUnit === '点') return Math.min(10, Math.round(sr.score * 10) / 10);
```

数値系（秒・回・m・cm）については、換算方法が未定義のままである。**当面は課題登録時に警告を出す**か、相対評価（学級内の順位に基づく正規化）を別途設計する。少なくとも現状の「黙って0点」は解消すること。

なお `test.peDirection` / `test.peTrials` は保存されているだけで換算に一切使われていない。整理の対象。

### 5.2 実技記録の統計表示が壊れる

7180行付近の点数分布で `var pct = test.maxScore/100;` を使っているため、実技記録（maxScore=9999）では分布が正しく出ない。実技ルーブリック型・実技記録では分布バーを非表示にするか、10点満点用の区分（0-2/3-4/5-6/7-8/9-10）に切り替える。

### 5.3 小数入力時のキーボード

非実技記録の得点入力欄は `inputmode="numeric"` のため、iPadで小数点キーが出ない。実技ルーブリック型の数値入力は必ず `inputmode="decimal"` を指定する。

### 5.4 成績カードのラベル

`grdExtKenteiLabel()`（13199行）は検定カードのみ対応。実技ルーブリック型では `「マット運動：7.0点」` の形式でラベルを返す分岐を追加する。13236行の体育の成績カード生成部も同様。

### 5.5 監査・根拠表示モジュール

14169 / 14447 / 16559 行付近が `kenteiConfig` を前提としている。実技ルーブリック型でも採点根拠（各項目の得点・加点・合成方法）を表示できるよう対応する。既存の信頼性監査の網から漏らさないこと。

---

## 6. 実装フェーズ（PR単位）

各フェーズ完了時に `CACHE_VERSION` を上げ、テストを通してからプッシュする。

| # | 内容 | 版 | 備考 |
|---|---|---|---|
| 0 | 5.1〜5.3 の不具合修正 + 水泳の換算表差し替え（4.6） | v1.14.3 | 完了 |
| — | `master.classInfo`/`master.students` 欠落バックアップ復元時のクラッシュ修正（`updateSettingsUI`/`updateDashboard`/`loadMaster`。PE機能とは無関係、先行対応） | v1.14.4〜v1.14.5 | 完了 |
| 1 | 共通基盤（データモデル・`peRubricCalcScore10()`・`peRecordToScore10()` 分岐・課題登録UI・基準編集モーダル・未確定スコアの教員向け一覧）＋ **ソフトバレー＋マット運動** | v1.15.0 | 最も単純な種目で基盤を作り切る。ソフトバレー単体だと `combine:'average'`/`bonusMode:'each'`/`level`モードが未検証のまま残るため、マットまで含めて共通基盤を実データで検証する |
| 2 | **ハードル走**（`実技:hurdle`、3項目すべて`mode:'level'`。number/`hurdleTime` resolverは不使用） | v1.21.0 | 接近度（タイム差）は教員が計算しlevelを選ぶ方式に簡略化。実装順としてはvaultより先行 |
| 3 | **跳び箱運動**（`実技:vault`、3項目すべて`mode:'level'`。`vaultLevel` resolverは不使用） | v1.21.1 | `trial`+resolverではなく、マットと同じ`level`モードで実装 |
| 4 | **鉄棒運動**（`mode:'level'`再利用、マットと同型） | 未定 | §4.5改訂によりcomposite/resolverが不要化。実装コストはマット並みに軽量化 |
| 5 | 5.4 / 5.5 の成績側・監査側の対応、全種目の通し確認 | 未定 | |

フェーズ1で基盤を作り切ることが重要。以降は原則「プリセット定義の追加」だけで済む形にしておくこと。resolver がプリセットごとに増えるのは許容するが、UI・保存・計算・再計算の経路は共通化する。

---

## 7. テスト計画

`tests/pe-rubric.test.js` を新規作成。puppeteer-core で実ブラウザを操作する。

### 7.1 検算テスト（必須）

各種目について、**元Excelの記入例と完全に一致すること**を確認する。これが受け入れの中核。

| 種目 | 入力 | 期待値 |
|---|---|---|
| ソフトバレー | ○○×○○（4成功） | 9 |
| マット | ①6 ②10 ③10 ④0、加点①のみ | 7 |
| 跳び箱 | ①8 ②8 ③10、加点あり | 9.17 |
| ハードル | ①10 ②6 ③10 | 8.67 |
| 鉄棒 | ①発展(10) ②基本(6) ③発展(10) ④基本(6)、加点あり | 8.5 |
| 水泳 | クロール25m完泳まで（stage 13） | 7 |

鉄棒の記入例を直接入力し、グループ判定が ①発展 ②基本 ③発展 ④基本 になることも確認する:
```
①前転グループ: 前回り下り2 / かかえ込み前回り0 / 転向前下り0 / 前方支持回転2 / 片足踏み越し下り0 / 前方伸膝支持回転2 / 横とび越し下り0
②前方足掛け回転グループ: 膝掛け振り上がり2 / 前方片膝掛け回転0 / 膝掛け上がり1 / 前方もも掛け回転0 / もも掛け上がり0
③後転グループ: 補助逆上がり2 / かかえ込み後ろ回り0 / 逆上がり3 / 後方支持回転0 / 後方伸膝支持回転0
④後方足掛け回転グループ: 後方片膝掛け回転2 / 両膝掛け倒立下り0 / 後方もも掛け回転1 / 両膝掛け振動下り0
```
（①は前回り下り2回成功で基本達成、あわせて前方支持回転2回成功で発展達成、前方伸膝支持回転2回成功で加点0.5も発生。②は膝掛け振り上がり2回成功で基本達成だが膝掛け上がりは1回のみで発展未達。③は補助逆上がり2回成功で基本達成、あわせて逆上がり3回成功で発展達成。④は後方片膝掛け回転2回成功で基本達成だが後方もも掛け回転は1回のみで発展未達）

### 7.2 境界値テスト

- ハードル③接近度は`level`選択のみのため数値境界のテストは不要。①②の最下段（1点）が0点に落ちないこと
- 「安定」判定の境界（3回中1回 vs 2回）
- 合計が10を超えるケース（加点でのクリップ）— 例: マット全項目10 + 加点2つ → 10 で頭打ち
- 鉄棒 ④（★更なる発展技を持たないグループ）が加点判定に一切寄与しないこと

### 7.3 未入力・部分入力・欠席の扱い（v1.15.0改訂）

- 全項目未入力 → `score10` を保存しない。成績の平均に混入しないこと
- **一部のみ入力（ソフトバレー: 5方向中2〜4タップ）→ §2.6の完了ゲートにより `complete=false` のため `score10` を保存しない。UIには入力中プレビューのみ表示し、確定バッジは出さないこと**
- **マット（`combine:'average'`、4item中の部分入力）**:
  - 4項目中1〜3項目のみ入力 → 各itemはそれぞれ`level`選択済みで確定していても、テスト全体としては未完了（`test.peRubric.items`の全item完了ではない）扱いとし、`score10`は保存しないこと
  - 4項目目を入力した時点で（全item完了）`score10`が保存されること
  - その後1項目を選択解除する等で未完了に戻すと、`score10`が削除されること
- **`complete=true` の状態からタップ/入力を取り消して `complete=false` に戻した場合、既存の `score10`/`score` キーが実際に削除されること**
- **欠席フラグ: ルーブリックカードには欠席ボタンを実装しない。** `recToggleAbsent()` は欠席化時にスコアレコード全体を `{absent:true, score:''}` で置換し、解除時はレコードを丸ごと削除する破壊的な実装であり、これをルーブリックカードに接続すると誤タップ1回で全タップ入力が失われる。既存の検定カード（なわとび・水泳）にも欠席ボタンは実装されていないため、実装しないことでむしろ既存挙動と整合する。**`recToggleAbsent()` の破壊的挙動そのものを直す件は本仕様書のスコープ外・別タスクとする（末尾「Phase 1 設計確定事項」参照）。**

### 7.4 回帰テスト

- 既存のなわとび・泳力検定カードの入力・換算が壊れていないこと
- 5.1 の修正後、単位「点」の実技記録が正しく換算されること
- 成績算出画面で体育の知識・技能に正しく反映されること
- 保護者面談PDF（レーダーチャート）が正しく描画されること
- **`peRubricRecalcScores()` を `complete=false` のレコードに実行しても `score10` が書き込まれないこと（基準編集経由で未完了データが確定してしまわないことの検証）**
- **`grdCalculate()` 経由で、`score10` が削除された（未完了に戻した）レコードが知識・技能の母数から正しく除外されること**

### 7.5 実機確認（自動テスト外）

- iPad Air M4 横向き・Smart Keyboard Folio 装着状態でのタップ精度
- 体育館・プールサイドでの片手操作（跳び箱の○×9個/技が現実的な速度で入力できるか）
- オフライン（PWAキャッシュ）での動作

---

## 8. 受け入れ基準

1. 6種目すべてがアプリ内で入力完結し、Excelを開く必要がないこと
2. 7.1 の検算がすべて一致すること
3. 採点基準がUIから編集でき、編集後に既入力分が自動再計算されること
4. 成績算出・観点別評価・保護者面談PDFに正しく反映されること
5. 既存機能に回帰がないこと
6. 各項目に「本校の実態に応じた目安であり、指導要領に明記された基準ではない」旨の注記が表示されること

---

## 9. 補足: 出典の扱い

各採点表には、技の分類が文部科学省「小学校学習指導要領解説 体育編」を参考にしたものである一方、**回数基準・秒数・点数配分は同解説に明記された基準ではなく本校の実態に応じた目安である**旨が明記されている。アプリ内でも同等の注記を表示し、基準値を編集可能に保つこと。研究授業や外部公開の場面で説明責任を果たすうえで重要な要件である。

---

## Phase 1 設計確定事項（v1.15.0・実装着手前の設計レビューで確定した内容の記録）

### rubricData の保存先
既存の `recScores`（同一レコード）に持たせる。別ストアは作らない。検定カードの `passData`/`stage`/`score10` と同じ配置。

### 基準編集後の再計算
`recKenteiRecalcScores(test)` と同型の `peRubricRecalcScores(test)` を新設。責務は基準編集モーダルの保存ハンドラが持つ（`test.peRubric` を更新 → `recSaveTests()` → `peRubricRecalcScores()` → モーダルを閉じる → 再描画、という `recSaveKenteiTable()` と同じ順序）。

### 種目追加時に触る箇所
- `PeRubricPresets` への1エントリ追加（items/bonus/combine/cap）
- 単位 `<select>` への `<option>` 追加（不可避、検定カードと同様）
- 新モード（`trial`/`level` はPhase1で作成済み。`number`/`composite`は新規）を初めて使う種目ではレンダラ・resolverの追加が必要。これは§6のフェーズ表が示す通り想定内。

### なぜPhase 1にマットを含めるか
ソフトバレー単体では `combine:'average'`・`bonusMode:'each'`・`level`モードのレンダラが一度も実行経路を通らず、「基盤を作り切る」という目的に対して未検証のまま次フェーズに進むことになるため。マットを含めることで、共通基盤の主要経路（level/trialレンダラ、average/table合成、bonus each、基準編集モーダル、完了判定ゲート）を実データで検証する。

### volleyCount resolver と マット加点のmode（§2実装時の確定）
- `volleyCount` resolverは実装しない（§2.4参照）。`combine:'table'`のtable分岐が唯一のitemの成功数を数えて`conversionTable`を引く処理を汎用的に持てば済むため。
- マットの加点（大きな前転／前方倒立回転跳び）は `mode:'level'`（0点/加点値の2択ボタン）を採用する（§2.3の`mode:'trial'`例は使わない）。理由: マットの主要4項目自体が既に「教員が直接判定して1タップで選ぶ」`level`モードで統一されており、加点だけ`trial`（3回individually タップ+stableAt判定）にするとカード内でUIパターンが混在する。`trial`ベースの加点は跳び箱・鉄棒の`bonusMode:'any'`（Phase 2以降）で導入する。bonus側にも`mode`フィールドを持たせ、種目ごとに切り替えられるようにする。

### 水泳をpeRubric基盤に乗せない理由
目的は「Excelを開かずアプリ内で完結すること」であり、`peRubric` 基盤を経由することが目的ではない。水泳は既にPhase 0（v1.14.3）で完結している（`KenteiPresets.swimming` + `recBuildNawatobiCard()` + `peRecordToScore10()` の検定カード分岐）。§4.6自身も「新規実装は不要」と明記している。

### 欠席ボタン
Phase 1のルーブリックカードには実装しない（§7.3参照）。

### 未完了生徒一覧（教員向け）
`score10` が未確定（`complete=false`、または全く未入力）のルーブリックテスト・該当生徒を教員が確認できる一覧を、既存の警告表示パターン（`recPeNoConvertHint` 等）に合わせて実装する。**通知表出力・保護者面談PDF・児童向け表示には一切出さない。教員向け画面（入力/成績算出まわり）に限定する。** 実装時に出力経路（PDF生成関数・帳票生成関数）を確認し、この一覧のレンダリング関数がそれらから呼ばれていないことを差分で示す。

### 別タスク（PE機能とは無関係・未着手の既知不具合）
- `recToggleAbsent()` の破壊的挙動（欠席化でレコード全体を置換、解除で削除。ルーブリックの `rubricData` に限らず既存の全カードで、欠席往復により入力途中のデータが失われる）
- `saveMasterRoster()` の既定値捏造（学年・組の入力欄が空欄のまま保存されると、`parseInt(...) || 4` / `parseInt(...) || 1` により「4年1組」が実際の学級と無関係に `master.classInfo` へ書き込まれ、`saveMaster()` で永続化される。修正には空欄バリデーションとUI変更が必要。`loadMaster()`（v1.14.5）はこの問題を再現していない — `classInfo` を空オブジェクトに正規化するのみで、既定値は代入しない）
- `tests/emptystate.test.js` のpuppeteer-core方式への書き換え（現状 `jsdom` 未インストールで実行不能。undo機構の配線・実データ往復5系統・`emptyState()` のHTMLエスケープ含む15ケースを他テストと同じ `page.evaluate()` 方式に移植する。PE機能とは無関係のため別タスクとする）

### コミット分割
0. `docs: PE_RUBRIC_SPEC.md 改訂(完了ゲート/未完了一覧/Phase1範囲拡大)`
1. `feat(pe-rubric): 共通基盤(データモデル・peRubricCalcScore10・peRecordToScore10分岐)`
2. `feat(pe-rubric): 入力UI(trial/levelモード共通レンダラ)`
3. `feat(pe-rubric): 基準編集モーダル + peRubricRecalcScores`
4. `feat(pe-rubric): ソフトバレーボール プリセット`
5. `feat(pe-rubric): マット運動 プリセット`（独立コミット。作業が長引いた場合にこれだけ落として v1.15.0 を出せる状態を保つ）
6. `feat(pe-rubric): 未確定スコアの教員向け一覧`（独立コミット）
7. `test: pe-rubric.test.js`
8. `chore: sw.js CACHE_VERSION → v1.15.0`
