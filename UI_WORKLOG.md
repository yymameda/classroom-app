# classroom-app UI統一 作業ログ
各セッションの最後に追記する。次セッション開始時はこのファイルを最初に読む。

## 2026-08-29 セッション1
- 完了: 全画面UI監査（UI_AUDIT.md 作成）
- 使ってみた結果: なし
- 次: 未定（UI_AUDIT.md の D・E を確認してから決定）

## 2026-08-29 セッション2
- 完了: 直書き色296件を var() に置換（commit 9fa8504、v1.29.0としてpush済み）
- 使ってみた結果: 未確認
- 次: --text-light のコントラスト改善（4.02:1 → 基準未達、文字色として197箇所で使用）

## 2026-08-29 セッション3
- 完了: --text-light を #64748B に変更（commit 0e519bc、v1.30.0）
- 使ってみた結果: 未確認
- 次: 【2】で報告した白以外の背景上の箇所、
      および --success / --accent の文字色用途を暗い版へ寄せる作業

## 2026-08-29 セッション4
- 完了: --text-light を #5A677D に変更（commit 2d13ed2、v1.31.0）
- 使ってみた結果: 未確認
- 次: --success / --accent の文字色用途を暗い版へ寄せる作業

## 2026-08-29 セッション5
- 完了: .rec-sc-pct の濃背景版を追加、観点別得点の #cce を
  var(--text-light) に変更（commit 03406f8、v1.32.0）
- 使ってみた結果: 未確認
- 次: --success / --accent の文字色用途を暗い版へ寄せる作業

## 2026-08-29 セッション6
- 完了: L2014/L12556/L967・L1062 のコントラスト修正
  （commit 6635677、v1.33.0）
- 使ってみた結果: 未確認
- 次: 白文字 on --success/--accent のバッジ類（比2.78〜3.15、約8箇所）。
      背景色を暗い版に変える方針だが、--success/--accent の値自体を
      変えると背景用途の全箇所に波及するため、
      D-1の使用状況を確認してから方針決定

## 2026-08-29 セッション7
- 完了: タップ領域20箇所を44px化、var(--tap-min)へ集約
  （commit e97deef、v1.34.0）
- 使ってみた結果: 未確認
- 次: L1696（ルーブリック入力欄34px）。隣接する22px丸バッジとの
      不揃いが拡大するため、バッジ側の調整とセットで対応する

## 2026-08-29 セッション9
- 完了: L10364/L13339/新体力テスト6箇所/L721/L12555 を44px化
  （commit e9c9358、v1.35.0）
- 使ってみた結果: 未確認
- 次: L7657（まとめテストバッジ22px）とL2197（.pf-sg-table入力欄28px）。
      どちらも児童ごとの一覧行のため、要素自体を大きくするのではなく
      タップ領域の取り方を変える案（カード全体をタップ可能にする等）を
      含めて検討する

## 2026-08-29 セッション10
- 完了: .pf-btn（7箇所）を44px化（commit 96c58fb、v1.36.0）
- 使ってみた結果: 未確認
- 次: チェックボックスのタップ領域（調査結果次第）、
      L1696、L7657、L2197

## 2026-08-29 セッション11
- 完了: L1598/L14703 のチェックボックスをlabelで囲みタップ領域を確保
  （commit 7e0b61c、v1.37.0）
- 使ってみた結果: 未確認
- 次: L1696、L7657、L2197

## 2026-08-29 セッション12
- 完了: 児童の記録(点数入力/ABC・ルーブリック行)と提出物チェック(リスト)で
  最下段が見切れる不具合を修正。#view-records/#view-submissionsをflex列にし、
  ヘッダー・サブナビ・フィルタ・進捗行を固定、リスト(#recList/#subListWrap)
  だけを内側スクロールに変更。#subListWrapのmax-height:calc(100vh-200px)は撤去。
  副作用防止で.rec-test-mgmt/.patrol-containerにも受け皿を追加
  （commit未、v1.38.0）
- 使ってみた結果: 実機確認は未実施（Chrome/puppeteerでの自動検証のみ。
  1180x820/820x1180・30人・複数教科フィルタ折り返し・ルーブリック参照パネル
  展開の負荷条件で最終行の見切れ解消と固定要素の非可動を確認）
- 次: 実機(iPad Air M4 11インチ、PWA)での見た目確認。連続入力/座席モードは
  今回スコープ外（外側.viewフォールバックのまま、退行なしをpuppeteerで確認済み）

## 2026-08-29 セッション13
- 完了: v1.38.0実機確認で見つかった2件を修正。(1)提出物チェック.sub-input-row/
  .sub-row-hd/.sub-status-btnsのpadding/gap詰め（○△はみ出し対策、Chrome実測では
  詰め前後とも水平オーバーフローは検出できず改善量は数値化不可、実機確認待ち）。
  (2)児童の記録「まとめて保存」を.rec-progress帯に統合(.rec-save-inline-btn)し
  縦1行を解消、44px高は維持（独立commit、後で戻せるようにした）。列数削減
  （横6→5列/縦4→3列）は未実装、次回すぐ適用できるようUI_AUDIT等に方針提示済み
- 使ってみた結果: 実機確認は未実施（Chrome/puppeteerでの自動検証のみ）
- 次: 実機でv1.39.0を確認。○△はみ出しが残るようなら列数削減を追加適用。
  「まとめて保存」統合ボタンが縦向きで押しにくければ独立commitのみrevertする

## 2026-08-29 セッション14
- 完了: v1.39.0実機確認で判明した「はみ出しは横でなく縦（ボタン枠がタイル下端を
  突き抜ける）」を受けて方針転換。flex-wrapは採用せず（A/B+/B/B-/Cの並び順維持が
  優先）、.rec-abc-btn/.sub-status-btnにmin-widthの床を追加し無制限縮小を止め、
  .rec-row.abc-row/.sub-input-rowのpaddingを増やして余白側で吸収する方針に変更
  （v1.40.0）。sub-input-list列数削減案は横方向対策のため保留のまま
- 使ってみた結果: 実機確認は未実施（Chrome/puppeteerでは変更前後とも
  オーバーフローを再現できないため、今回からChrome実測を判断根拠にしていない）
- 次: 実機でv1.40.0を確認。まだ突き抜けるならpadding/min-widthの値を再調整

## 2026-08-29 セッション15
- 完了: v1.40.0/v1.40.1で3回対処しても改善しなかった「タイルからボタンがはみ出す」
  不具合を、横方向でなく縦方向の内容量過多が原因と特定して対処（v1.41.0）。
  主因は.sub-input-rowのお直し完了チェックボックスをlabelで囲みタップ領域を
  確保した際(v1.37.0)のmin-height:44pxが.sub-row-hdの実高を28px相当から
  44pxへ押し上げていたこと。タップ領域は::before疑似要素の非対称オフセット
  (視覚サイズ20pxのまま、隣接する欠席ボタン/○△ボタンを避けて拡張)に切り出し、
  padding/gap/ボタン高も詰めた。.rec-row.abc-rowはpadding/gap/ボタン高に加え
  番号バッジ・欠席ボタンを24pxに縮小(abc-rowにスコープ、他画面へ波及なし)。
  内容量はabc-row 93px→78px(min-height86pxまで8px余裕)、sub-input-row
  121px→88px(横min-height92pxまで4px、縦100pxまで12px余裕)。副次的に、
  ランドスケープ限定の.sub-status-btn高さ上書き(48px)が新設のtile-vheight
  テストで発覚、44pxに統一(コミット未、v1.41.0)
- 使ってみた結果: 実機確認は未実施。tile-vheight-v1.41.0.test.jsを追加し、
  内容量の積み上げ計算とチェックボックス拡張ヒットゾーンの実クリック検証
  (欠席ボタン/番号バッジ/氏名/○△ボタンが個別に正しく反応すること)を含め
  既存32件+新規1件、計33ファイルのテストが全て通過を確認
- 次: 実機でv1.41.0を確認。.sub-row-num/.sub-absent-btnの24px化は
  sub-input-row側では見送っているため(番号バッジ/欠席ボタンの視認性優先)、
  横min-height92pxまでの4px余裕でも見切れが残るようなら次の削減候補とする。
  また、ランドスケープ限定メディアクエリ内の.sub-status-btn高さ上書き(48px)が
  ベースのCSS変更を無効化していた件を新規テストで検知できたため、同種の
  「メディアクエリ内上書きが基準の変更を無効化している」箇所が他にも
  残っていないか次回grepで洗い出す

## 2026-08-29 セッション16
- 完了: 児童の記録・点数入力で最下段の行が画面下端で切れる問題に対し、ルーブリック
  参照パネルの帯(見出し・トグル)を単独の1行から教科タブ行(.rec-filter-row)の
  右側空きスペースへ移動（v1.42.0）。サブナビ行は5ボタンで空き24pxしかなく不採用、
  教科タブ行は最悪条件(8教科・縦向き)でも225px空きがあり帯の自然幅132pxに対し
  93px余裕があるため採用。展開される中身(#recRubricBody)は課題選択行の下の
  元の位置に残し、帯だけを移動(recSetRubricVisible()で両者の表示/非表示を一元化)。
  帯の高さは37px→44pxに是正(タップ領域要件)。#recList実高は横494px/縦858px
  (各+45px)。ユーザー確認の結果、展開時にリストが縮む挙動自体は既存仕様のまま
  維持(帯の新設置による二重の押し下げが無いことのみ確認すれば良い、と明確化)
- 使ってみた結果: 実機確認は未実施。rubric-band-move-v1.42.0.test.jsを追加し、
  帯のタップ高・開閉状態の一致・ルーブリック無し課題での教科タブ行の崩れ無しを
  確認。既存34件+新規1件、計35ファイルのテストが全て通過
- 次: 実機でv1.42.0を確認。#recList実高+45pxで横向きは4行→5行相当の余地が
  出る計算だが、縦向きはChromeのgrid align-content:stretchの影響で正確な
  行数増加を測れていないため、実機での見切れ解消を優先して確認する

## 2026-08-29 セッション17
- 完了: 提出物チェックのサブタブ順序を変更（v1.43.0）。「提出課題の管理」を
  最後尾(5番目)から入力タブ群の直後(3番目)へ移動し、入力/入力（個人）/
  提出課題の管理/提出課題の統計/修養日誌の順に。児童の記録側(点数入力→
  授業課題の管理→統計→…)とタブの並びの考え方を揃える目的。DOMのボタン順序
  のみ変更しIDやsubShowSub()の切替ロジックは無変更、他画面からの直接遷移も
  すべてdata-sub属性ベースのため無影響を確認
- 使ってみた結果: 実機確認は未実施。5タブすべての切替とcontentId/アクティブ
  ボタンの対応を実クリックで確認。忘れ物チェック・成績処理統合は別の
  .sub-subnavブロック(別タブ構成)のため無影響。既存35件のテストが全て通過
  (順序に依存するテストは無く更新不要と確認)
- 次: 特になし

## 2026-08-29 セッション18
- 完了: 残課題3（ヘッダー色の統一）。青グラデーション以外で上書きされていた
  4画面（漢字チェック#2E7D32/#388E3C、新体力テスト#1a6b4a/#2a8a5a、
  成績処理統合#1a1a2e/#16213e、データ出力#1565C0/#1976D2）を基本の
  `linear-gradient(135deg, var(--primary), var(--primary-light))`へ統一。
  調査の結果、緑2画面はhex値が別物で共有変数も無く、ナビグループとも
  対応しておらず、コメント・commit履歴にも色選択の理由の記載が無かった
  ため「意図された色分けではない」と判断（ユーザー承認済み）。
  2コミットに分割（v1.44.0として1回push）:
  (1) 漢字チェック(L3515,L3520)・データ出力(L3675)・新体力テスト
      (L3764本体＋付帯.pf-subnav-btn.active/:hoverのL2237-2238、
      #1a6b4aをvar(--primary)へ)
  (2) 成績処理統合(L3411本体＋付帯.sub-subnav背景L3413・
      タブボタン色L3414-3419・grdTermSel配色L3420、他画面の学期
      セレクタと同一スタイルに統一)。ヘッダーだけ変えると直下の
      紺タブ帯が浮くため付帯箇所を含め1コミットにまとめた
- 使ってみた結果: 実機確認は未実施。puppeteerで14画面全ヘッダーの
  computed背景がrgb(45,125,154)→rgb(58,156,191)に統一されたことを
  1180x820/820x1180の両方で確認（表示上の見た目一致の根拠としてでは
  なく、値が意図通り変わったことの機械的確認として使用）。成績処理統合・
  新体力テストのタブactive/inactive判別（背景色・文字色の対比）、
  grdTermSelがsubTermSelと同一の見た目（背景/文字色/枠線）になった
  ことも確認。既存34件のテストが全て通過。node --checkでJS構文異常なし
- 次: 未対応の既知課題に追記した「ランドスケープ拡大の無効化」
  （#view-grades/#view-submissionsのIDセレクタ上書き、漢字チェックの
  h2インライン固定）は今回のgradient削除では解消しないことを確認済み。
  対応するか判断が必要

## 2026-08-29 セッション19
- 完了:「ランドスケープ拡大の無効化」を解消（v1.45.0）。
  `#view-submissions .view-header`/`#view-grades .view-header`の
  padding:6px 16px上書きを削除、漢字チェックの2ヘッダー（一覧/児童詳細）
  からheight:50px・padding:0 12px・h2のfont-size:1rem固定をすべて削除し、
  基本の`.view-header`／ランドスケープ拡大メディアクエリ（L2024-2029付近、
  padding:18px 24px、h2 1.25rem）に委ねた。提出物チェックの
  「修養日誌1画面表示のため」の余白削減コメントは、30人+実測で
  余白367〜735px確保できることを確認した上で撤去（削るほどの
  必要性が無かったと判断、ユーザー承認済み）
- 想定外の副作用と対処: ヘッダーを66px→80px級に戻したことで、
  提出物チェックの「入力（個人）」タブでperson.test.jsがタイムアウト
  FAILするようになった。原因は`.view-header`と`.sub-subnav`が両方
  `position:sticky;top:0`のため、入力（個人）/統計/修養日誌/課題管理
  タブ（outer `.view`をスクロールするフォールバック方式）でスクロール
  すると両者が同じ位置で重なり合い、ヘッダーの拡大でこの重なりの
  死角（覆われて見えない/タップできない帯）が広がっていたこと
  （元々存在した設計上の欠陥で、今回のヘッダー拡大が顕在化させた）。
  「入力」タブは#sub-sub-inputが内部スクロールのためouter側は元々
  スクロールせずstickyが無関係、と確認した上で
  `#view-submissions .view-header, #view-submissions .sub-subnav`を
  `position: static`に変更して解消。成績処理統合も同じsticky構造だが、
  30人データで計算式/成績一覧/専科入力タブのoverflowが0（スクロール
  自体発生しない）ことを実測確認したため今回は変更なし（次項に注記）
- 使ってみた結果: 実機確認は未実施。puppeteerで全14画面のヘッダー高さ・
  paddingを横1180x820/縦820x1180で実測し、h2のみの画面（横66px/縦60px）
  とヘッダー内ボタン持ちの画面（提出物チェック・漢字チェック・個人カルテ、
  横80px/縦76px）の2グループに統一されたことを確認（出席簿・児童の記録・
  新体力テストは元々別レイアウトのためスコープ外、変更なし）。漢字チェック
  はh2が1.25rem化してもボタン（＋漢字を追加/一括PDF出力/←一覧へ戻る）が
  ヘッダー幅に収まりはみ出さないことを一覧・児童詳細の両ヘッダーで確認。
  既存34件のテストが全て通過（person.test.jsの回帰含め解消を確認）
- 次: 特になし（残課題3・4は完了）

## 2026-08-29 セッション20
- 完了: 実機確認で「色は統一されたがヘッダーの高さが揃って見えない」との
  指摘を受け、原因調査（残課題2として新規着手）。全14画面のヘッダー内の
  実際の中身を列挙し、(a)h2のみ (b)h2＋操作ボタン (c)機能ボタン混在
  (d)タブ・日付操作がヘッダー内、の4パターンに分類。青地が途中で
  切れて見える原因を実測で特定：`.view-header`は横方向flexで、
  子要素は`flex-grow`が無い限り横幅いっぱいに広がらない。児童の記録
  (`.rec-subnav`)・出席簿(`.att-top-bar`)はタブ/ツールバー用の白背景divを
  ヘッダーの唯一の子として横並びで置いているため、その内容量ぶんの幅
  しか占有せず、残りは親の青が露出する（実測: 児童の記録は
  ヘッダー幅1088pxに対し白い部分675px/青の露出413px、出席簿は
  631px/457px）。新体力テストも同じく「タブがヘッダー内」の(d)分類だが、
  `.pf-subnav`は縦方向flex(`flex-direction:column`)の子要素のため
  デフォルトの`align-items:stretch`で自動的に横幅いっぱいに伸び、
  実測でも1088px/1088pxと一致し問題なし。このためユーザー指摘のうち
  新体力テストは実害なし、児童の記録・出席簿のみが実際の不具合と判明。
  対応範囲は今回、低リスクな2件（新体力テスト・漢字チェック）に絞り、
  出席簿（新規に下段バーの設計が必要）と児童の記録（#recListの高さ
  計算に直結し本日4セッション分の調整が水泡に帰すリスク）は次回以降の
  独立セッションへ分離（ユーザー承認済み）
- 完了: 新体力テストの`.pf-subnav`を`.view-header`の外へ移動（見た目は
  現状維持、構造のみ他画面と統一）。ラッパーだったcolumn-flexの
  `.view-header`とその内側のタイトル行divを削除し、他画面と同じ
  素の`.view-header`（h2＋hdr-actions）＋兄弟要素の`.pf-subnav`という
  構成に変更（v1.46.0、独立commit 179a48c/次項とは別）
- 完了: 漢字チェックの「＋漢字を追加」ボタンをタイトル直後から
  `hdr-actions`内（一括PDF出力ボタンの前）へ移動。スタイル・位置以外の
  変更なし（v1.46.0、独立commit）
- 使ってみた結果: 実機確認は未実施。puppeteerで新体力テストの
  `.pf-subnav`が`.view-header`の外（`subnavInsideHeader:false`）に
  出て幅がヘッダーと一致すること、5タブ全ての切替（active状態・
  パネル表示）が正常に動くこと、外に出したことでview全体のoverflowが
  発生しないことを確認。漢字チェックは一覧ヘッダーの2ボタンが両方とも
  ヘッダー枠内に収まりはみ出さないこと（横1180x820/縦820x1180の両方）
  を確認。既存34件のテストが全て通過
- 次: 出席簿・児童の記録のヘッダー構造統一（未対応の既知課題を参照）

## 2026-08-29 セッション21
- 完了: 提出物チェック「入力」タブ(リストモード)で、教科フィルターバー
  (`#subFilterBar`)が単独で1行を専有し縦を圧迫していた問題を解消
  （v1.47.0）。実測で横向き27人・1教科の場合、統合前はリスト必要高
  488pxに対し確保高484pxで4pxオーバー（実機の見切れ報告と一致）、
  統合後は確保高537pxで49px余裕が生まれることを確認した上で実装。
  `#subFilterBar`を単独行から進捗帯(`#subProgressWrap`)の左側へ統合し、
  空いた1行ぶんを`#subListWrap`(flex:1)へ還元。教科タブ＋ラベル＋件数を
  新設の`#subProgressLeft`(flex:1)でグループ化し、`全員○`ボタンだけ
  右端に残す構成（教科タブ→提出率・件数→全員○ボタンの順）。共有クラス
  `.rec-filter-bar`/`.rec-filter-btn`/`.sub-input-progress`自体は変更
  せず、`#subFilterBar`のインラインstyleに`padding:0;border-bottom:none;`
  を追加して共有クラス由来の行区切り用padding(4px 8px)だけを打ち消した
  （実装時にこれを見落として進捗帯が54px→63pxに増えるミスを一度発生
  させ、テストで検知して修正した）
- 使ってみた結果: 実機確認は未実施。sub-progress-filter-merge-v1.47.0.test.js
  を新規追加し、27人/30人×1教科/8教科×横1180x820/縦820x1180の
  8パターンで①最下段クリップ無し(scrollH<=clientH)②進捗帯高さ54px維持
  ③教科タブ・全員○ボタンのタップ領域44px以上④教科タブクリックでの
  絞り込み動作⑤8教科でも折り返さない、を検証（既存34件＋新規1件で
  56項目、全通過）。実データに近い条件（横1180x820・27人・1教科）でも
  最下段クリップが解消（カード最下端793px＜確保領域800px）を個別に再確認
- 次: 特になし

## 2026-09-17 セッション22
- 完了：項目ごとの入力形式選択（得点／5段階、test.inputMode）とテスト日付の動的化（v1.49.0）
- 使ってみた結果：iPad実機確認OK（既存の5点満点の振り返りは数値のまま、新規項目で得点・5段階を選択可、既存の記述問題は5段階のまま）
- 次：（未定）

## 2026-09-19 セッション23
- 完了：データ反映監査（DATA_FLOW_AUDIT.md）の高＋M1＋M6を修正（v1.50.0）。カルテ出力/面談テキスト/成績CSVの学期を出力側に統一・成績側キャッシュを画面切替で破棄・汎用実技記録を全観点で共通換算・観点/教科変更時の項目重み移送・ダッシュボードのテスト平均を成績処理と同じ集計に・期限日欠席かつ未提出を提出率の分母から除外（全経路で grdSubmissionJudge 共通化）。回帰テスト tests/data-reflection.test.js 45件追加
- 使ってみた結果：iPad実機確認は未実施（puppeteerのみ。全テスト825 PASS、既存のisabc系3件FAILは着手前から）。実技記録を知識・技能以外にしている課題、出席簿が欠席の日が期限の未提出課題がある児童は成績・提出率の値が変わるため要確認
- 次：iPad実機で成績処理・カルテ出力・提出物統計の値を確認。未対応はH4(児童ID未導入)・M2〜M5・M7〜M9・L1〜L7（DATA_FLOW_AUDIT.md参照）

## 2026-09-19 セッション24（v1.50.1）
- 完了：H6（名簿保存で視力・身長が消える）を修正、isabc系の既存失敗3件（テスト側の古さ）を修正、その過程で見つかった実装不具合H7（観点編集で入力形式と満点が矛盾、v1.49.0由来）を修正。全テスト867件PASS
- 使ってみた結果：iPad実機確認は未実施（puppeteerのみ）。名簿を変更せず保存しても視力・身長が残ること、既存課題の観点を変更して入力画面が5段階/得点に追随することを要確認
- 次：push後にH4案B段階1（児童IDの付与と起動時移行）へ。段階2以降は段階1のiPad確認後

## 2026-09-19 セッション25（v1.51.0 前半）
- 完了：H7の既存矛盾データ検出。入力形式と満点が矛盾する課題（得点なのに満点なし／5段階なのに満点あり）を診断ツールに一覧表示（検出と表示のみ、自動修正なし）
- 使ってみた結果：iPad実機確認は未実施。データ出力(📤)の画面の一番下の「監査診断」>「診断を実行」で該当課題が出ること、課題管理で開いて保存し直せることを要確認
- 次：H4 段階1（児童IDの付与と起動時移行）

## 2026-09-19 セッション26（v1.51.0 後半）
- 完了：H4 案B 段階1。起動時に児童ID(studentId)を持たない児童にだけ付与（冪等）。付与前に spa_master を退避し、失敗時は書き戻し。既存データは1件も変わらない（全キー比較）。診断出力に退避コピーの氏名が出る不具合も修正。全テストPASS
- 使ってみた結果：iPad実機確認は未実施。起動して画面・成績・名簿がこれまでと同じであること、名簿保存とバックアップ復元後も動作が変わらないことを要確認
- 次：段階1のiPad確認後に段階2（対象ストアの登録表と remapStudentIndexes、スナップショット/検証/ロールバック）

## 2026-09-19 セッション27（v1.51.1）
- 完了：H4 案B 段階2a。児童データ付け替えの純関数(computeRosterRemap/planRosterChange)・不変条件I1〜I7・18ストアの登録表を実装(UI未接続・書き込みなし・既存の挙動は変わらない)。10.7の5点(退避は無期限・スナップショット1世代・取り消しは入力があれば不可でトースト・pfは通知のみ・自動再読み込みは事前にflush)を設計書に反映
- 使ってみた結果：画面から使われないため実機での見た目の変化はなし。iPad実機確認は未実施(段階1の確認待ち)。テストは 6操作×18ストアのずれ0(計画レベル)・ランダム60ケース・変異テストで確認
- 次：段階2b(検証付き書き込み・スナップショット・ジャーナル・起動時ロールバック・applyRosterChange・退避/復元)。段階1のiPad確認後に指示

## 2026-09-19 セッション28（v1.51.2 前半）
- 完了：H8 IndexedDBの鏡に削除したデータ(氏名)が残る問題を修正。退勤モード消去が新体力テスト(pf_roster=氏名・記録)と旧バックアップを消さなかった／pfの名簿リセット・記録削除が鏡に残り次回起動で復活しバックアップに入った／過去の残りの掃除。課題・記録の削除は残らないことも確認。全テストPASS
- 使ってみた結果：iPad実機確認は未実施。退勤モードで消去したあと、新体力テストの名簿・記録も消えていること、pfの「名簿リセット」「記録データ削除」のあとバックアップに残らないことを要確認
- 次：段階2bの続き

## 2026-09-19 セッション29（v1.51.2 後半）
- 完了：H4 案B 段階2b。検証付き書き込み(段階1のID付与も置換)・容量の事前確認・スナップショット/ジャーナル/退避/各ストア/名簿の順に適用する applyRosterChange・起動時の自動ロールバック・退避と復元・取り消しを実装(UI未接続で既存の名簿保存は変わらない)。書き込みの合間の中断23通り・容量超過/破損の注入23通りで全キーが変更前と一致。roster-shift の実適用(6操作×18ストア)もずれ0
- 使ってみた結果：画面から使われないため実機での見た目の変化はなし。iPad実機確認は未実施。変異テストでロールバック・中断復旧・検証の破壊を検出できることを確認
- 次：段階2c(pf連携)。段階1〜2bのiPad確認後に指示

## 2026-09-19 セッション30（v1.51.3）
- 完了：H8追補（実機のIndexedDBに残った削除済みデータを起動時に一回だけ掃除。所有しないキーは触らない）、名簿変更前の容量確認を実際の空きで判定する方式へ変更、H4 段階2c（新体力テスト連携: pf名簿が一致するときだけ記録・集計・名簿を追従、不一致は触らず通知のみ、pfも同じトランザクション）。テストの通知確認が常に通る不具合も修正。全テストPASS
- 使ってみた結果：iPad実機確認は未実施。画面からは使われないため見た目の変化はなし(段階3で接続)。初回起動で古い残りが自動掃除されること(操作不要)を要確認
- 次：段階3（名簿編集UIの接続・退避の一覧/復元/完全削除UI）。段階1〜2cのiPad確認後に指示

## 2026-09-19 セッション31（v1.52.0）
- 完了: H4 段階3（名簿編集UIの接続）。名簿のテキスト編集＋警告ダイアログを廃止し、リスト編集UI（並べ替え・途中削除・転入・改名・性別、下書き＋保存ボタン）に置き換え。保存は `applyRosterChange` の1本だけ。設定に「退避中の児童 N名」（一覧・復元・2回タップの完全削除）、取り消しボタン・取り消しトースト、バックアップ促しトースト。確認ダイアログなし。tests/roster-ui.test.js（66件）新規、roster-preserve・student-id-migration をUI操作に書き換え。全体1226件PASS。詳細は DATA_FLOW_AUDIT.md 15章
- 使ってみた結果: iPad実機で確認済み(push後)。バージョン表示がv1.52.0になった／名簿がリスト表示で内容が正しい／改名→保存→取り消しで元に戻る／気になった点なし。「変更なしで保存すると『変更はありません』と出る」も、後日の回答でOKを確認
- 次: 段階4（pf.html の studentId 化・pf-residual の復元・バックアップ v11）の設計案（コード変更なし）

## 2026-09-19 セッション32（v1.52.1）
- 完了: H4 段階4a（新体力テストの連携状態の診断表示。設定画面に確認用カード、読み取り専用）。名簿の一致/不一致(理由)・人数・pf_fitness の痕跡(単体ページ使用)・SPA名簿にいない児童の人数(氏名は出さない)・記録の年度別人数。tests/pf-diagnosis.test.js 37件。詳細は DATA_FLOW_AUDIT.md 17章
- 使ってみた結果: 未確認（iPadで設定画面のカードを見て、結果を報告してもらう。結果で16.11のa・bを決める）
- 次: 4d-1（importBackup の安全化）→ 4b → 4c → 4d-2 → 4e

## 2026-09-19 セッション33（v1.52.2）
- 完了: H4 段階4d-1（バックアップ復元の安全化。commitのみ・未push）。復元前の自動退避(スナップショット)・検証付き書き込み・失敗時のロールバック・起動時の自動復旧・復元の取り消し・容量不足時は警告して続行。バックアップに無いキーの扱いを明確化(名簿が違うときだけ児童別データを消す/別アプリのキーは端末に無いときだけ書く)。形式が違うファイルは「復元できません」で中止。tests/backup-restore.test.js 71件、変異テスト13件すべて検出。詳細は DATA_FLOW_AUDIT.md 18章
- 使ってみた結果: 未確認（iPadでの確認は未実施。push後に確認手順を案内する）
- 次: 4a の診断表示の結果(iPad)を見て16.11のa・bを決める → 4b → 4c → 4d-2 → 4e

## 2026-09-19 セッション34（v1.53.0）
- 完了: 4aの診断結果(iPad: 一致・27/27・単体ページなし・手入力なし)で16.11のa・bを確定(pf.htmlは案内ページ化・手動登録は移行の対象外)。4d-1をpush。H4 段階4b(commitのみ・未push): 新体力テストの記録を児童ID(studentId)で管理する新方式へ。起動時の自動移行(名簿が一致するときだけ・移行前後の全キー比較・失敗時は元に戻す)、名簿変更・復元・取り込み・pf画面を新方式に対応。tests/pf-migration.test.js 116件、変異テスト15件すべて検出。詳細は DATA_FLOW_AUDIT.md 16.13・19章
- 使ってみた結果: 未確認（iPadでの確認は未実施。先生はバックアップを取ってから更新する）
- 次: 4c(pf-residualの単体復元・pf-unlinked・氏名で引き継ぐ) → 4d-2 → 4e(pf.htmlを案内ページに)

## 2026-09-20 セッション35（v1.53.1）
- 完了: 4bのpush前の追加対応(push済み)。(1)全体実行で一度だけ失敗したテストの原因調査: 名簿の編集直後に出る「バックアップを促す」通知が、画面の下端にある保存ボタンに重なり、タップが通知に当たって保存されなかった(実機でも起こり得た)。通知が下のボタンへのタップを受けないよう修正。(2)起動直後の競合を1件発見して修正(IndexedDBの準備前に書き直されたキーが、準備後に消されていた)。(3)pf.htmlの暫定ガード: 新方式のデータがある端末で開くと、何も書き込まず「アプリ内の新体力テストタブを使ってください」だけを表示。詳細は DATA_FLOW_AUDIT.md 20章
- 使ってみた結果: **iPad実機(v1.53.1)で確認済み(2026-09-20、先生はバックアップ後に更新)**。バージョン表示がv1.53.1／起動直後に「新しい方式に更新しました」の通知が出た／診断カードが「児童ごと（新方式）」「一致しています」／新体力テストの記録が更新前と同じ児童に付いている／変更なしで保存すると「変更はありません」／気になった点なし。→ 4b(pfの新方式への自動移行)は実機で成功
- 次: 4c → 4d-2 → 4e

## 2026-09-20 セッション36（v1.54.0）
- 完了: v1.53.1のiPad確認結果(問題なし)を記録しpush。H4 段階4c(commitのみ・未push): 名簿が一致しない旧方式の端末向けに、新体力テストの設定タブに「旧方式の記録の引き継ぎ」(氏名で一意に一致した児童だけ引き継ぎ・確認→確定・取り消し可・引き継げない分は退避に保管)、退避した pf-residual の単体復元(入力済みは上書きしない)、pf-unlinked の表示。先生の端末は名簿が一致して自動移行済みのため、画面には表示されない。tests/pf-takeover.test.js 72件、変異テスト13件すべて検出。/code-review の指摘4点を資料で修正。詳細は DATA_FLOW_AUDIT.md 21章
- 発見: test_grades.js(全体実行の対象外だった)が v1.49.0 から2件失敗している(H4・4cと無関係。DATA_FLOW_AUDIT.md 21.1)。未対応
- 使ってみた結果: 未確認（4cは先生の端末では表示されない機能が中心。iPadでの確認は、先生の承認後にpushしてから案内する）
- 次: 4d-2 → 4e。test_grades.js の2件の調査

## 2026-09-20 セッション37（v1.54.1）
- 完了: 4cをpush(v1.54.0)。テスト実行の漏れを修正(全体実行が *.test.js だけで test_grades.js を含まなかった。tests/run-all.js を追加し、README の表を実測から生成・照合する形に)。test_grades.js の2件の失敗(v1.49.0から)を調査: アプリの不具合(得点入力済みの旧課題の観点を主体性に変えて戻すと満点が0になり割合が出ない)と古い期待の両方。アプリを修正しテストを更新。commitのみ・未push。詳細は DATA_FLOW_AUDIT.md 22章
- 使ってみた結果: 未確認（先生の承認後にpush → iPadで確認）。実際の成績入力の画面で先生が気づく症状: 「得点入力済みの旧課題の観点を『主体性』に変えると5段階ボタンに変わり、戻すと満点0で点数を入力できなくなる」（主体性以外の変更・得点未入力・v1.49.0以降の課題では起きない）。すでに起きていれば設定の診断「入力形式と満点が矛盾する課題」に出る
- 次: 4d-2 → 4e

## 2026-09-20 セッション38（v1.54.2）
- 完了: v1.54.1をpush。H7の診断(データ出力(📤)の画面の一番下の「🔍 監査診断」>「診断を実行」の「入力形式と満点が矛盾する課題」。設定画面ではない)を拡張(読み取り専用・自動修正なし): 新しい種類(5段階なのに数値の点数など5段階として読めない値)、成績への影響(影響を受ける児童数のみ・氏名なし・重み0は「入っていません」)、直し方の一行案内。直し方を実UI操作で確認: 満点なし・数値/満点あり・文字は画面で直せる。5段階で数値の記録/得点方式で文字の記録は入力形式の固定で直せない(判断待ち)。詳細は DATA_FLOW_AUDIT.md 23章
- 使ってみた結果: 未確認（iPadで診断を見てもらう。手順は先生向けまとめ）
- 次: 直せない状態(23章の(c)(d))への対応を先生と決める → 4d-2 → 4e

## 2026-09-20 セッション39（v1.54.3）
- 完了: 先生のiPadの診断結果(作文_運動会なりきり1件)を読み、診断の誤判定を修正(遅れて提出の0.8倍を考慮せず「成績に入っていない1人」と誤表示。成績は正しかった)。影響の判定を成績計算の共通関数(grdItemScore10)だけで行う形に。得点方式で文字の記録(満点あり)を検出し続ける(検出漏れ(d))。診断の場所の記述を訂正(データ出力の一番下。設定画面ではない)。詳細は DATA_FLOW_AUDIT.md 23.1。push済み
- 使ってみた結果: 未確認
- 次: 記録が数値なのに5段階と判定されている課題を点数入力に戻せるようにする(取り消しつき・成績は変えない。commitまで)

## 2026-09-20 セッション40（v1.55.0）
- 完了: 診断の誤判定の修正をpush(v1.54.3)。記録が数値なのに5段階と判定されている課題(先生の「作文_運動会なりきり」)を、記録に合う形式へ戻せるように(commitのみ・未push): 課題の編集画面に「記録に合わせて、得点入力に戻す」ボタン。記録は変換せず成績の値も変えない・食い違いのある課題だけ・記録に合う方向だけ・確認ダイアログなし・戻した後のトーストで取り消し・スナップショットで失敗時はロールバック。診断にも直し方の案内。詳細は DATA_FLOW_AUDIT.md 24章
- 使ってみた結果: **iPad（v1.55.0）で確認済み（2026-09-20）**: 「作文_運動会なりきり」を得点入力に戻せた・点数も表示された・診断は「🟢 致命的問題なし」・入力形式と満点が矛盾する課題は0件。ただし最初は手順どおりにたどり着けなかった（診断の案内「児童の記録＞授業課題の管理」と、報告の手順「児童の記録→課題を選ぶ→この課題を編集」が食い違っていた。次のセッションで案内を統一）
- 次: 診断の案内の統一 → 4d-2 → 4e

## 2026-09-20 セッション41（v1.55.1）
- 完了: 診断の「直し方」の経路を、画面の表示文字どおりに統一（📝児童の記録 → 📋授業課題の管理 → 「課題一覧」右の学期を「通年」→ その課題の行の「編集」。一覧は今の学期だけを出すので、古い学期の課題は「通年」にしないと見つからない）。ボタン名は編集画面と同じ定数から作る。案内の各語が実際の画面と一致すること・案内どおりにクリックして着くことをテストで確認。診断の情報「まとめテストが全問単一観点」を見直し: 先生は観点ごとに分けて作る（例「まテ_1学期　思考」）ので、課題名がちょうど1つの観点を示し全問がその観点なら表示しない。名前と観点が違う場合は「付け間違いの可能性」と添えて表示、名前に観点の語が無ければこれまでどおり。作成時の観点設定は保存されていないので、手がかりは課題名だけ。読み取り専用のまま。詳細は DATA_FLOW_AUDIT.md 24.1
- 使ってみた結果: 未確認（iPadで診断の案内どおりに操作できるか・「まテ_1学期　思考」が情報から消えているかを見てもらう）
- 次: 4d-2 → 4e（優先順位の案は最終報告）

## 2026-09-20 セッション42（v1.56.0）
- 完了: 先生の決定(1)体育「まテ_1学期　思考」の answers 全0/1（2件）は本当に0点＝対応不要として記録(24.2)。(3)遅れ係数0.8倍の適用範囲(M8): 0.8倍は「提出物チェックの提出記録」にだけ。児童の記録(全種別)には掛けない。遅れの印(表示・記録)は残し、成績の計算にだけ使わない。`grdItemScore10` の掛け算と定数を削除し、算出根拠シートの説明を更新。変わる値: 先生の作文「作文_運動会なりきり」の遅れの印1名 4.8→6.0点(確認できている分)。診断には出さず報告で示す。詳細は DATA_FLOW_AUDIT.md 25章
- 使ってみた結果: iPad（v1.57.1）の診断の表と保存した1学期CSVとの比較で、想定どおり確認済み（作文の遅れの印の1人だけが上がった。詳細はセッション44）
- 次: 取り消し枠の分離（種類ごとに別の取り消し点・入力形式の取り消しは課題の編集画面に）

## 2026-09-20 セッション43（v1.57.0）
- 完了: 取り消し点を種類ごと（名簿変更・バックアップ復元・pf 引き継ぎ・pf 復元・入力形式の変更）の別の枠に分け、互いに上書きしないように。入力形式の変更の取り消しボタンは課題の編集画面に（課題名・日時つき）。起動時の復旧・容量確認・検証付き書き込みは保つ。旧版の共用の枠に残った取り消し点は起動時に該当の枠へ移す。もう取り消せない枠の写しは次の変更の前に消して容量を空ける。詳細は DATA_FLOW_AUDIT.md 26章
- 使ってみた結果: v1.57.1 の確認で「気になった点なし」。ただし、取り消しの操作そのもの（名簿変更→入力形式の変更のあと、別々に取り消せるか）の実機確認は報告されていない（未確認。次に名簿や入力形式を直す機会に試してもらう）
- 次: 4d-2 → 4e（優先順位の案は報告済み）

## 2026-09-20 セッション44（v1.57.1）
- 完了: 診断に「遅れの印がある数値の記録（遅れ係数の変更で成績の値が変わる記録）」を追加（読み取り専用・氏名なし）。児童の記録の課題のうち、遅れの印がある数値の記録を、課題名・教科・学期・該当の児童数と合計で表示。提出物チェックの記録は含めない。詳細は DATA_FLOW_AUDIT.md 25.1。先生はバックアップと変更前の1学期成績CSVを保存済み。push の承認あり
- 使ってみた結果: **iPad（v1.57.1）で確認済み（2026-09-20）**: 診断の「遅れの印がある数値の記録」は「作文_運動会なりきり・1学期・1人」のみ（想定どおり）。変更前に保存した1学期の成績CSVとの比較も想定どおり（作文の1人だけが上がった）。気になった点なし。これで v1.56.0（遅れ係数）の実機の結果も確認できた
- 次: 4d-2 → M2〜M5（方針確認が先）→ 4e → M7・M9 → L 項目（HANDOFF.md §8）
- 追記の依頼(同日): HANDOFF.md の未対応項目に L8（診断の情報(参考)に確認済みの項目が毎回出る問題。「確認済み」の印を付けて情報表示から外す案）を追記。実装はしない

## 2026-09-20 セッション45（v1.58.0）
- 完了(push済み): H4 4d-2。バックアップに `version: 11`・`studentIdSchema: 1`・`pfSchema`（端末の実際の方式。新=2/旧=1/データなし=印なし）を追加。復元時に版の数字でなく中身（児童IDの有無・pfの方式）で新旧を判別し、「古い形式」「より新しい版」の案内だけ出す（復元は止めない）。復元後の再読み込みで整合性を検査（児童IDの欠落・重複／pfの新旧混在／退避の二重・破損。読み取り専用・氏名なし・自動では直さない）し、問題があればトーストで知らせる。設定に「🩺 データの整合性の確認（確認用）」カードを追加。保護者向け出力からは呼ばない。詳細は DATA_FLOW_AUDIT.md 27章。テスト `backup-integrity.test.js` 37件（変異12件を検出）。全体 64ファイル/2182件PASS
- 使ってみた結果: push 済み。**版表示と監査診断は iPad 確認済み（2026-09-20）**: 版表示 v1.58.0 OK／アプリ内監査診断は構造的整合性すべて0件・致命的問題なし（児童27名／テスト93件／得点2,378件／提出記録2,216件）。**未確認: 復元時の整合性確認カード・バックアップの書き出し**
- 次: 4e（pf.html を案内ページに）→ M2〜M5（方針確認が先）

## 2026-09-20 セッション46（v1.59.0）
- 完了(push済み): H4 4e。`pf.html`（単体ページ）を、スクリプトなしの案内ページ（アプリを開く → 【記録】の「📝」→「🏃 新体力」）に置き換え。暫定ガード・旧方式の画面・名簿の読み込み・JSONバックアップは廃止（アプリ本体のバックアップに一本化）。localStorage の `pf_*` のキーは削除していない。`sw.js` のキャッシュ対象には `./pf.html` を残し、`CACHE_VERSION` を v1.59.0 に。アプリ本体から `pf.html` へのリンクは無いことを確認。`pf-guard.test.js` を `pf-landing.test.js`（63件・変異8件を検出）に置き換え、`idb-residue.test.js` の5章を案内ページ向けに更新。全体 64ファイル/2200件PASS。詳細は DATA_FLOW_AUDIT.md 28章
- 調査(実装なし): 診断の紛らわしい2点（単一観点8件の誤検知に近い表示／まとめテスト30件と13件の食い違い）の原因と修正案を HANDOFF.md の L9 に記録。原因は、観点の内訳とサマリーの対象が「まとめテスト＋単元テスト」（`type==='matome'`）で、分布は「まとめテスト」だけを数えているため
- 使ってみた結果: **4e は iPad 未確認**。v1.58.0 の未確認2項目（復元時の確認カード・バックアップの書き出し）と一緒に、HANDOFF.md §8 の「次回の iPad 確認リスト」で確認してもらう
- 次: 4e の push（済み）→ iPad 確認 → M2〜M5（方針確認が先）→ L9 の実装（→ セッション47）

## 2026-09-20 セッション47（v1.59.1）
- 完了(push済み): v1.59.0（4e）を push（先生の指示）。L9（監査診断の表示改善。表示だけで成績・記録は変えない）: (a) 単元テスト（testType='単元テスト'）を「全問が単一観点」の判定から除外 (b) 観点語のない名前のまとめテストの文言を「全問が同じ観点です。観点ごとに分けて作ったテストならこのままで問題ありません」に (c) 内訳表に「課題名」「種別」の列を足し、赤い「❌全問単一観点」をやめた。サマリーのテスト総数を「N件（うち まとめ形式 X件＝まとめテストA＋単元テストB）」に（その他の種別があれば「＋その他C」）。5章の見出し・内訳の見出し・問題定義なしの行・コピー用テキストも「まとめ形式（まとめテスト・単元テスト）」に揃えた。`tests/audit-matome-perspective.test.js` を15→23件（変異9件を検出）。全体 64ファイル/2208件PASS。詳細は DATA_FLOW_AUDIT.md 29章
- 発見(未決事項・L10): 診断の警告「問題定義のないもの N件」は、読む場所を間違えていて既存のまま一度も出ない（画面の構造の行では見える）。直すと診断の総合判定が変わり得るので、先生に確認してから（HANDOFF.md L10。先生向けの説明つき）。v1.59.1 を push 済み
- 使ってみた結果: 未確認（v1.59.0＝4e の確認と一緒に、HANDOFF.md §8 の「次回の iPad 確認リスト」で確認してもらう）
- 次: iPad 確認（4e・L9） → M2〜M5（方針確認が先）・L10 の可否

## 2026-09-20 セッション48（v1.60.0）
- 完了(push済み 2026-09-20・7コミット 7d4466a〜4c59407): 先生の方針確定を受けて L10・M2〜M5 を1項目1コミットで実装（成績画面の評定・観点別の値は変えていない。各テストに変更前の値を固定）。(L10) 診断の警告「問題定義のないもの N件」が出るように（読む場所の修正。設問なしのテストがあると総合判定が「注意項目あり」）。(M2) 提出率を成績の数え方（お直し済み1.0・お直し前0.8・遅れ上限0.8）に、カルテ画面・カルテPDF・提出物の一括PDF・面談テキスト・ダッシュボード（児童別・クラス）で統一。(M3) 面談テキストの成績・提出物・テスト個票を出力画面の学期に統一。(M4) レーダーPDFの教科カードのテスト一覧・平均を成績画面の学期で絞り、得点方式の主体性を含める。(M5) 同僚用テンプレートを成績画面の学期に絞り・得点方式の主体性を得点入力の列に、算出根拠の「1→4点」を「1→3点」に。詳細は DATA_FLOW_AUDIT.md 30章
- 調査(コード変更なし): 診断の「まとめテスト30件／testType 分布13件＋18件」の食い違いは、新体力テストの自動取り込みが作る「新体力テスト総合」（testType はまとめテスト・type なし）が原因とみられる。内訳は「まとめテスト12＋単元テスト18」（HANDOFF の「13＋17」は誤りだったので訂正）。成績・帳票への影響なし（30.2）
- 指示との差(先生に報告): (1) M3 は「成績画面の学期」でなく「出力画面の学期」に統一（H1 で成績は既に出力画面の学期。成績画面の学期にすると H1 を巻き戻し、同じ画面のカルテPDFと食い違う）。先生に確認し「出力画面の学期（推奨）」で確定。(2) M2 に、ダッシュボードの児童別の提出率と `printAllKarte` の旧経路も含めた。(3) M5 の「1→4点」は同僚用ではなく算出根拠xlsxのラベル
- テスト: 新規5ファイル（`audit-matome-noq-warning`・`submission-rate-unify`・`conference-text-term`・`radar-card-term`・`template-xlsx-term`）。すべて fail-first で失敗を確認してから直し、変異テスト（L10=3・M2=8・M3=6・M4=5・M5=7件）で検出力を確認。`data-reflection.test.js` の M6（丁の提出率）の期待を方針どおり 100%→90% に更新。全体 69ファイル/2280件PASS（初回の全体実行では、面談テキストの検証が出力画面の学期の既定（現在の学期）に引きずられて1件失敗 → data-reflection の M6 に出力画面の学期「通年」の指定を足して解消。M3 で面談テキストの提出物が出力画面の学期で絞られるようになった正しい結果）
- 使ってみた結果: **iPad 未確認**（HANDOFF.md §8 の「次回の iPad 確認リスト」。更新前にバックアップと1学期の成績CSVを保存してから）
- 次: iPad 確認（v1.60.0・v1.59.x の未確認分）→ M7・M9 → L 項目（HANDOFF.md §8）

## 2026-09-20 セッション49（v1.60.1）
- 報告: iPad 実機（v1.60.0）で「面談用レーダーPDF」ボタンが無反応（成績処理・1学期）
- 調査: 実データに近い題材（27名・多教科・検定・専科・新体力）で再現し、v1.59.1 の別ポート起動（git worktree）と比較。**既存の不具合**（v1.59.1 でも同じ・M4 の回帰ではない）。原因は、教科カードの検定の行が呼ぶ `kenteiStageToLabel` が `window` に公開されていなかったこと（体育のなわとび・泳力の記録があると `ReferenceError`）。ボタンの処理は `async` で、例外はコンソールの Unhandled rejection だけだったため、画面には何も出なかった。詳細は DATA_FLOW_AUDIT.md 31章
- 完了(push済み 2026-09-20): `window.kenteiStageToLabel` を公開／成績画面の `btn` ヘルパーで処理の失敗（同期・`async`）をトースト「⚠️ 「ボタン名」に失敗しました: 原因」で通知。`sw.js` を v1.60.1 に。新規テスト `tests/radar-pdf-button.test.js`（9件・修正前の v1.60.0 と v1.59.1 で6件失敗を確認・変異4件を検出）。本番のボタンのクリック→html2canvas・jsPDF→結果ダイアログまで通し、コンソールエラー0件を確認。全体 70ファイル/2289件PASS
- 発見（未対応・HANDOFF.md 3章）: (1) 日付が変わったあとの再描画（`INIT`）が呼ぶ `fgtShowSub`・`subRenderDiary` も `window` に未公開。(2) 成績画面以外の `async` ボタン処理は失敗が画面に出ない可能性（未走査）
- 学び: v1.60.0 の `radar-card-term.test.js` は `htmlPagesToPdf` を差し替え、検定の記録も無かったので、この不具合を見逃した。出力のテストは本番の経路を通す（HANDOFF.md 5章に追記）
- 使ってみた結果: **iPad 一部確認済み（2026-09-20・途中）**: 版表示 v1.60.1 OK／監査診断 OK／面談用レーダーPDFは生成できた（無反応は解消）。**問題2点**: (a) PDF に学期がどこにも書かれておらず、何学期分か分からない (b) ボタンは成績処理画面ではなく個人カルテ（出力）の画面にあった。→ セッション50（v1.60.2）で対応。未確認: バックアップ・更新前の成績CSV・成績が変わっていないこと・提出率(M2)・面談テキスト(M3)・同僚用テンプレート(M5)・算出根拠・診断の数字
- 次: v1.60.2（学期の出所の統一・未公開参照の洗い出し・async ボタンの失敗通知）→ iPad 確認 → M7・M9 → L 項目

## 2026-09-20 セッション50（v1.60.2）
- 報告: iPad 実機（v1.60.1）の確認の途中結果（版表示・監査診断 OK／レーダーPDFは生成できた）。問題2点: (a) PDF に学期がどこにも書かれていない (b) ボタンは成績処理画面ではなく個人カルテ（出力）の画面にあった。→ 結果は先に HANDOFF・UI_WORKLOG に記録し、その後 v1.60.2 を実装（3項目・1項目1コミット）
- 完了(push済み 2026-09-20): (1) **出力の学期の出所を統一**（ボタンがある画面の学期セレクタに従う）。レーダーPDFは出力画面の学期（表・提出率・机間巡視・カード・検定の行を同じ学期に）。レーダーPDF・児童用カルテPDF（通年も）・面談テキスト・算出根拠Excel・成績CSVに対象学期を印字。出力ごとのボタンの場所と学期の出所の一覧は DATA_FLOW_AUDIT.md 32.1。(2) **別モジュールから呼ばれるのに未公開の関数の洗い出し**（eslint `no-undef`＋ブラウザの `typeof`。`on*` 属性も）。日付が変わったあとの再描画の `fgtShowSub`・`subRenderDiary` を公開し、復旧バーが出ていた不具合と、忘れ物の日付が更新されない不具合を修正。(3) **成績画面以外の async ボタン**（バックアップの保存・復元・座席表PDF・PDFの共通処理・名前を伏せて作り直す）の失敗をトーストで通知（共通の `uiGuard`）。詳細は DATA_FLOW_AUDIT.md 32章
- テスト: 新規3ファイル（`output-term-source` 18件・`day-rollover-refresh` 8件・`async-button-failure-notice` 9件）。すべて本番のボタン・イベントの経路で fail-first を確認（修正前に12/5/7件失敗）、変異テストで検出力を確認（12/6/8件を検出）。`radar-card-term`・`radar-pdf-button` は、ボタンの実際の場所（個人カルテ出力）と出力画面の学期セレクタを使うよう更新。成績画面の観点別・評定は各テストで変更前と完全一致。全体 73ファイル/2324件PASS
- 学び: v1.60.0 は、レーダーPDFのボタンの場所（個人カルテ出力）を確かめずに成績処理画面の学期を使い、テストも同じ思い込みで書いたため気づけなかった。出力の学期は「ボタンがある画面の学期セレクタ」、ボタンの場所は画面の実物で確かめる（HANDOFF.md 5章に追記）
- 使ってみた結果: **iPad 確認済み（2026-09-20・先生の報告の 0〜10 すべて OK）**: バックアップ保存／版表示 v1.60.2／成績処理1学期の評定が以前と一致／新体力27名／復元後の整合性確認で問題なし／監査診断／カルテ提出率／面談テキストの対象学期と1学期分のみ／レーダーPDF 1・2ページ目の対象学期／同僚用テンプレートの題名「（1学期）」／日付をまたいでも復旧バーが出ず忘れ物の日付が今日。**v1.58.0 以来未確認だった「バックアップの書き出し」「復元時の確認カード」も確認済み**。未確認（報告なし・省略可）: 児童用カルテPDFの通年表示・算出根拠Excelの学期表記・診断のサマリーの数字・pf.html の案内
- 次: v1.60.3（同僚用テンプレートの B+・B-）→ M7・M9 → L 項目

## 2026-09-20 セッション51（v1.60.3）
- 報告: iPad 実機（v1.60.2）の確認は 0〜10 すべて OK（セッション50の「使ってみた結果」に記録済み）。v1.58.0 以来未確認だった「バックアップの書き出し」「復元時の整合性確認カード」も確認済みとして HANDOFF.md を整理
- 完了(push済み 2026-09-20): 同僚用テンプレートの A/B/C 入力の列で、5段階の B+・B- が空欄扱いになる問題（HANDOFF 3章の残り (c)）を修正。換算の数字は `abcTo10` から作り（A=10・B+=8.5・B=7・B-=5・C=3）、小文字・全角の＋／－・前後の空白も可。A/B/C・数値入力・検定の換算、成績の値は変えていない。満点欄の表記を「A/B+/B/B-/C」に、「使い方」に換算の行を追加。詳細は DATA_FLOW_AUDIT.md 33章
- テスト: 新規 `tests/template-abc5-conversion.test.js`（16件・修正前に7件失敗・変異12件中11件を検出。残る1件は Excel の `=` が大文字小文字を区別しないための同値の変異）と、式を計算する小さな計算機 `tests/helpers/xl-eval.js`。出力したシートの式を計算して、6人分の入力で観点のABC・評定が成績処理と全員同じことを確認。実際の Excel／Numbers では未確認。`template-xlsx-term.test.js` は入力欄の表記の期待を更新。全体 74ファイル/2340件PASS
- 使ってみた結果: **iPad 確認済み（2026-09-20・先生の報告すべて OK）**: 版表示 v1.60.3 OK／同僚用テンプレートを Numbers で開き、B+ で観点の % が 85、B で 70、B- で 50（想定どおり）。**報告なし（未確認）**: 成績処理の評定・観点別が以前と同じか（HANDOFF.md §8 確認リスト 3）・小さな項目 (ア)〜(エ)。Excel では未確認
- 次: M7・M9 → L 項目

## 2026-09-20 セッション52（調査のみ・コード変更なし）
- 依頼: L7（主体性の得点が文字列で保存されることがある）と L5（名簿を減らしたあとの範囲外の児童番号）の調査。実装はしない
- 調査(puppeteer・実験用スクリプトはリポジトリに入れていない): (L7) 文字列が入る経路を特定。**「欠席」→「欠席中」（解除）の実際のボタン操作で `score:''` の記録が残り、主体性の得点方式だと0点として計算される**（評定が下がる例あり）。数字でない文字は NaN になり観点C・評定が下がる。`"8"`（数字の文字列）は成績は同じ。提出率は影響なし。診断は数字でない文字は数えられるが、空文字は数えられない。(L5) 名簿の縮小・戻す・完全削除は、実操作で範囲外の記録が0件・成績と提出率が変わらないことを確認。取り残しが実際にある場合は、成績・提出率は変わらず、課題別の人数の表示だけが「9/6」のようにずれる。詳細は HANDOFF.md 3章
- 修正案(先生の判断待ち): L7=案C 診断に件数（成績は変わらない）→案A 読み方を直す（空文字・数字でない文字の児童だけ成績が変わる）・案B 解除時に空の記録を削除（成績は変わらない）。L5=案D 診断に提出記録の範囲外（成績は変わらない）・案E 課題別の人数を絞る（成績は変わらない）
- 使ってみた結果: 調査のみのため iPad 作業なし（案Cの診断を入れたら、先生の端末の空文字の件数を診断で見てもらう）
- 次: 先生の判断（案C・案D を入れるか）→ M7・M9 → L 項目

## 2026-09-20 セッション53（v1.60.4）
- 依頼: 調査（セッション52）の結果を受けて、L7 案C・案B と L5 案D を実装（いずれも成績の数字を変えない）。L7 案A（読み方を直す）は実装しない（診断の件数を見てから先生が判断する）
- 完了(push済み 2026-09-20・1項目1コミット): (L7 案C) 監査診断が、主体性（得点方式）の課題の空文字・数字でない文字の記録を、課題ごと・児童数とあわせて数える（情報・構造的整合性の表・コピー用テキスト。総合判定は変えない）。数字の文字列（"8"）は「成績に正しく入っていない」に数えないよう修正。(L7 案B) `recToggleAbsent` の解除で、中身が空の記録（`score:''` で他の項目なし）は記録ごと消す（既存の空の記録は書き換えない）。(L5 案D) 監査診断が提出記録の範囲外 studentIndex を数える（情報・表・コピー用テキスト。総合判定は変えない）。`sw.js` を v1.60.4 に。詳細は DATA_FLOW_AUDIT.md 34章
- テスト: 新規3ファイル（`audit-attitude-scorestr` 18件・`absent-release-empty-record` 17件・`audit-sub-index-over` 16件）。すべて修正前の index.html で失敗（13/7/10件）してから直し、変異テストで検出力を確認（18/10/15件すべて検出）。案B は実際の「欠席」→「欠席中」のボタンを `page.click` で押す。成績画面の評定・観点別は各テストに変更前の値を固定（案A・案E が入ると失敗する）。`absent.test.js` の項目6（旧形式の欠席記録の解除後）は、「記録が残る」から「記録ごと消える」に期待を更新（意図した挙動の変更）。全体 77ファイル/2391件PASS（うち新規3ファイル51件）
- 学び: (1) 変異テストの見逃しは題材の偏りが原因だった（授業態度が入力形式の判定で先に除外されて効かない・遅れの印とメモを同じ記録に持たせて片方の判定が守られる・番号が一桁だけで文字列順と数字順が同じ・重複の記録がない）。1つの条件を1つの題材で固有に確かめる。(2) 変異テストの基準（壊しなし）が全件通っていることを、変異を回す前に必ず確かめる（1件落ちたまま回すと、検出数が水増しになる）。(3) `sed -i ''`（macOS）は複数行の置換に向かない。テストの一括置換は Python で行う。
- 使ってみた結果: **iPad 確認済み（2026-09-20・先生の報告すべて OK）**: 版表示 v1.60.4 OK／監査診断の「主体性（得点方式）の空欄・文字の記録」0件（課題0件・児童0人）／「提出記録の studentIndex 範囲超過」0件（児童数27）／総合判定 🟢。**報告なし（未確認・省略可）**: 欠席→解除で ✓ が付かないこと・成績処理の評定が同じか
- 整理（先生の判断）: 診断が0件だったので、**L7 案A と L5 案E は「該当なし・当面不要」**。HANDOFF.md 3章に、案の中身と再開の条件（診断が1件以上になったとき）を残して整理した
- 次: M7・M9 → L 項目（L1〜L4・L6・L8。困りごとが出てから）

## 2026-09-20 セッション54（v1.61.0）
- 依頼: 先生の方針確定（M7: 通知表には専科の評定を学期ごとに載せる・既存の値はすべて1学期分／M9: 案1）を受けて、M7・M9と小さな修正2件を実装。成績の数字は変えない（1学期の各出力と専科以外の成績は変更前と完全一致）
- 完了(push 済み 2026-09-20): (M7) 専科の値を学期ごとに持つ。1学期＝`data[教科]`（既存の値をそのまま・書き換えなし・移行なし）、2・3学期＝`data['教科@2']`・`data['教科@3']`。専科入力タブに学期の切り替え・「いま入力中：〇学期」の帯・各行の学期の札（初期値は現在の学期）。各出力（成績処理の表・カルテPDF・面談テキスト・レーダーPDF・成績CSV×2・算出根拠Excel）は、それぞれの学期の値だけを使い、値のない学期は「未入力」・通年は「B（2学期）」のように最新の入力済み学期の値を学期名つきで（平均は取らない）。退避した児童を戻す処理が `教科@2` を専科でない教科と誤読していたのを直した。(M9) データ出力に別ファイルの「成績の明細CSV」（1行＝児童×項目・観点・入力形式・生の値・満点・10点換算・重み・提出物の状態／評価点／欠席除外。ファイル名と先頭に対象学期）。既存の3つのCSVは変えていない。(小修正) 提出物の説明文を「評価点 3.6/6（60%）」に／データ出力の成績CSVの教科の選択肢に専科の教科も出す。1項目1コミット（M7 ae91e25・M9 44c10ba・小修正2件 afd3c0b・4ec3ca5）＋docs 970741b＋`sw.js` fd627df（v1.61.0）。詳細は DATA_FLOW_AUDIT.md 36章
- テスト: 新規3ファイル（`ext-grade-term` 51件・`export-grade-detail` 28件・`export-grade-subjects-ext` 8件）＋ pin 2ファイル（変更前のコードで採取した値）。`submission-rate-unify` は期待の更新と検査の追加（18→20件）。すべて修正前のコードで失敗してから直し（新機能の検査が失敗・1学期の値は変更前と一致）、変異テストで検出力を確認（M7 10件・M9 13件・小修正2 3件すべて検出。小修正1は修正前のコード＝変異）。全体 80ファイル/2480件PASS（FAIL 0。v1.60.4 の 77ファイル/2391件から、新規3ファイル87件＋`submission-rate-unify` の2件）
- 学び: (1) 「変更前と完全一致」を固定するには、テスト自身に採取モード（`CAPTURE=1`）を持たせ、変更前のコードで採取した値を pin の JSON に保存するのが確実。(2) 出力の表示関数（`grdExtShow`）は、専科でないとき・単一の学期の値のとき、今までの型のまま返す（評定の数字を文字にすると、`hy === '3'` の比較で専科でない教科の色まで変わる）。(3) `StorageManager.set` は約0.5秒遅れて localStorage に書く。保存を直接読むテストは、ボタンを押したあと待ってから読む。(4) テストの接続先が固定（8123）のファイルは、変更前のコードでの実行にコピー（接続先だけ置換）が要る。変異テストは作業ツリーの複製を別ポートで配信すると、実装と並行して回せる。(5) 全体テスト中は index.html を触らない（触ったら、走行を止めてやり直す）。上に貼りつく帯（`position:sticky`）は、スクロールする外側の `.view` の中で見出し・サブナビと重なるので、「常に見える」表示には各行の札を使う。
- 使ってみた結果: **iPad 確認済み（2026-09-20・先生の報告5項目すべて OK）**: バックアップ保存／版表示 v1.61.0・1学期の評定が以前と同じ／専科入力の学期切り替え（1学期に既存値・2学期は空）／成績一覧の専科（1学期は同じ値・2学期は未入力・通年は学期名つき）／成績の明細CSVのファイル名に1学期。報告なし（未確認・省略可）: 明細CSVの中身・成績処理CSVの教科に専科が出ること。iPad 確認リストは HANDOFF.md 8章の「iPad 確認リスト（v1.61.0）」5項目（先頭はバックアップ保存）
- 次: M2〜M5 の残りのうち「提出率 60%(3/6)」の表記（次のセッション55で対応）→ L 項目（困りごとが出てから）

## 未対応の既知課題
- 【セッション21で追記・設計基準】ヘッダー構造統一の基準（後述）に加え、
  「単独の1行を専有させず、空きのある隣接行に寄せる。どちらの行が
  主役になるかは画面ごとの空き具合で決まる（児童の記録＝教科タブ行に
  ルーブリック帯を寄せる／提出物チェック＝進捗帯に教科タブを寄せる）」
  を設計基準として記録
- 【セッション20で追記】ヘッダー構造の統一（残課題2）。全14画面を
  (a)h2のみ (b)h2＋操作ボタン (c)機能ボタン混在 (d)タブ・日付操作が
  ヘッダー内、に分類済み。今回(c)(d)のうち漢字チェック・新体力テストの
  みに対応。出席簿（h2なし、日付バッジ・前後ボタン・3タブ・PDFボタンが
  `.att-top-bar`としてヘッダー直下にまとまっている）と児童の記録
  （h2なし、`.rec-subnav`のみがヘッダーの中身）は未対応。設計基準は
  「青ヘッダーに入れるのはタイトル（左）・学期セレクタ・一括操作ボタン
  （最右）のみ。タブ・日付操作・フィルタはヘッダーの外（下の白い
  サブナビ行）に置く」（口頭決定のみでファイル未記載だったため今回
  明文化）。児童の記録は`#recList`の高さ計算に直結するため、着手時は
  ヘッダー変更前後の`#recList`実高を測りながら実機確認とセットで進める
  こと。2026-08-29のセッション12・14・15・16で「最下段が見切れる」
  不具合を繰り返し修正してきた領域であり、ヘッダーを66〜80px新設で
  追加するとその成果を打ち消す高いリスクがある
- 【セッション19で判明】`.view-header`と`.sub-subnav`を両方
  `position:sticky;top:0`にする実装は、outer `.view`側がスクロールする
  画面・タブでは常に重なり合う（サブナビ側が常にヘッダーの下に隠れる）
  設計上の欠陥。現状は提出物チェックのみ`position:static`で個別に
  無効化して対処（本セッション参照）。成績処理統合は現状データ量では
  スクロールが発生せず未対応のままだが、将来教科・課題数が増えて
  `#view-grades`がスクロールするようになった場合は同じ死角バグが
  顕在化しうる。恒久対応するなら`.sub-subnav`のtop値をヘッダー実高分
  オフセットする（現状は個別画面でstaticにする対症療法）
- 【セッション18で判明・実測】`.hdr-btn`（半透明白背景のヘッダー内ボタン、
  PDF出力・CSV・保存・モード切替等）の白文字コントラストが全画面で
  4.5:1未満（小さい文字の基準）。ヘッダー色に関わらず元々未達で、
  今回の色統一によって悪化したものではないためスコープ外とし記録のみ:
  基本(青)明側3A9CBF=2.44:1、漢字明側388E3C=2.97:1、
  新体力明側2a8a5a=3.10:1、データ出力明側1976D2=3.31:1、
  成績処理(紺)明側16213e=8.39:1（唯一の合格。色統一によりこの
  唯一の合格例も基本の青パターンに揃うため、対応するなら
  `.hdr-btn`自体の背景を不透明側へ寄せる等、ヘッダー色とは別軸の
  対応が必要）。なお、UI_AUDIT.mdに「Table B」という節は存在しない
  ことをセッション18で確認済み（このファイル旧記載の誤り）。
  上記コントラスト実測がその代わりの記録となる
- コントラスト4.5:1未満の残り38行は未対応。うち var(--danger) on white（3.82）が多数を占めるが、
  エラー表示用途で文脈からも判別可能なため優先度は低い
- 色の表記を変更するとスナップショットテスト（attendance-snapshot.js）が必ずFAILする。UI統一作業では色変更のたびに基準更新が必要。検証は「旧基準のhexをvar()へ順方向変換して新生成HTMLと比較」する方向で行う（逆方向だと元々var()だった箇所まで巻き込んで誤差が出る）
- ctx.strokeStyle の #fff（L15314, L21017）はCanvas描画のためvar()化できず、将来 --white を変更した際に取り残される
- 直書き色のうち文字色として使われているものは、D-3調査で色ごとの一覧は取ったが、背景とのコントラスト比までは全件検証していない。#cce のように常時判読困難な箇所が他にも残っている可能性がある
- L890（.rec-matome-modal-close、まとめテスト/実技ルーブリック入力モーダルの×ボタン）は44px化するとpe-rubric.test.jsの「モーダルスクロール解消(1180x820)」テストがFAILする（overflow=5）。#recPeRubricModalはv1.15.4でiPad Air横向き実寸ちょうどに余白を使い切る設計のため、共有クラス側を伸ばすと即座に破綻する。36pxのまま据え置き。44px化するなら#recPeRubricModal側だけ別クラスで除外するなど、モーダル全体の再設計とセットで行うこと
- セッション9時点でのbutton/input/select/textarea 44px未満の残存: L890(36px、モーダル×ボタン、テスト制約により据え置き。理由はこのファイル内の別項目参照)、L1696(34px、ルーブリック入力欄、隣接バッジとセットで対応予定)、L7657(22px、まとめテストバッジ、今回対象外)、L2197(28px、.pf-sg-table入力欄、今回対象外)。
- .sub-input-row のチェックボックス（L1598 .sub-correction-check）は縦方向のみmin-height:var(--tap-min)化済み（セッション11）。横方向のタップ領域は20pxのまま。カード幅が190〜200pxしかなく、横44px化すると氏名表示が76px→60pxに縮むため見送った。カードのレイアウト自体を見直す機会があれば再検討する
