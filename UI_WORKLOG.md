# classroom-app UI統一 作業ログ
各セッションの最後に追記する。次セッション開始時はこのファイルを最初に読む。

## 2026-08-29 セッション1
- 完了: 全画面UI監査（UI_AUDIT.md 作成）
- 使ってみた結果: なし
- 次: 未定（UI_AUDIT.md の D・E を確認してから決定）

## 2026-08-29 セッション2
- 完了: 直書き色296件を var() に置換（commit 9fa8504、v1.29.0としてpush済み）
- 使ってみた結果: 未確認
- 次: --text-light のコントラスト改善（4.02:1 → 基準未達、文字色として197箇所で使用）

## 2026-08-29 セッション3
- 完了: --text-light を #64748B に変更（commit 0e519bc、v1.30.0）
- 使ってみた結果: 未確認
- 次: 【2】で報告した白以外の背景上の箇所、
      および --success / --accent の文字色用途を暗い版へ寄せる作業

## 2026-08-29 セッション4
- 完了: --text-light を #5A677D に変更（commit 2d13ed2、v1.31.0）
- 使ってみた結果: 未確認
- 次: --success / --accent の文字色用途を暗い版へ寄せる作業

## 2026-08-29 セッション5
- 完了: .rec-sc-pct の濃背景版を追加、観点別得点の #cce を
  var(--text-light) に変更（commit 03406f8、v1.32.0）
- 使ってみた結果: 未確認
- 次: --success / --accent の文字色用途を暗い版へ寄せる作業

## 2026-08-29 セッション6
- 完了: L2014/L12556/L967・L1062 のコントラスト修正
  （commit 6635677、v1.33.0）
- 使ってみた結果: 未確認
- 次: 白文字 on --success/--accent のバッジ類（比2.78〜3.15、約8箇所）。
      背景色を暗い版に変える方針だが、--success/--accent の値自体を
      変えると背景用途の全箇所に波及するため、
      D-1の使用状況を確認してから方針決定

## 2026-08-29 セッション7
- 完了: タップ領域20箇所を44px化、var(--tap-min)へ集約
  （commit e97deef、v1.34.0）
- 使ってみた結果: 未確認
- 次: L1696（ルーブリック入力欄34px）。隣接する22px丸バッジとの
      不揃いが拡大するため、バッジ側の調整とセットで対応する

## 2026-08-29 セッション9
- 完了: L10364/L13339/新体力テスト6箇所/L721/L12555 を44px化
  （commit e9c9358、v1.35.0）
- 使ってみた結果: 未確認
- 次: L7657（まとめテストバッジ22px）とL2197（.pf-sg-table入力欄28px）。
      どちらも児童ごとの一覧行のため、要素自体を大きくするのではなく
      タップ領域の取り方を変える案（カード全体をタップ可能にする等）を
      含めて検討する

## 2026-08-29 セッション10
- 完了: .pf-btn（7箇所）を44px化（commit 96c58fb、v1.36.0）
- 使ってみた結果: 未確認
- 次: チェックボックスのタップ領域（調査結果次第）、
      L1696、L7657、L2197

## 2026-08-29 セッション11
- 完了: L1598/L14703 のチェックボックスをlabelで囲みタップ領域を確保
  （commit 7e0b61c、v1.37.0）
- 使ってみた結果: 未確認
- 次: L1696、L7657、L2197

## 2026-08-29 セッション12
- 完了: 児童の記録(点数入力/ABC・ルーブリック行)と提出物チェック(リスト)で
  最下段が見切れる不具合を修正。#view-records/#view-submissionsをflex列にし、
  ヘッダー・サブナビ・フィルタ・進捗行を固定、リスト(#recList/#subListWrap)
  だけを内側スクロールに変更。#subListWrapのmax-height:calc(100vh-200px)は撤去。
  副作用防止で.rec-test-mgmt/.patrol-containerにも受け皿を追加
  （commit未、v1.38.0）
- 使ってみた結果: 実機確認は未実施（Chrome/puppeteerでの自動検証のみ。
  1180x820/820x1180・30人・複数教科フィルタ折り返し・ルーブリック参照パネル
  展開の負荷条件で最終行の見切れ解消と固定要素の非可動を確認）
- 次: 実機(iPad Air M4 11インチ、PWA)での見た目確認。連続入力/座席モードは
  今回スコープ外（外側.viewフォールバックのまま、退行なしをpuppeteerで確認済み）

## 2026-08-29 セッション13
- 完了: v1.38.0実機確認で見つかった2件を修正。(1)提出物チェック.sub-input-row/
  .sub-row-hd/.sub-status-btnsのpadding/gap詰め（○△はみ出し対策、Chrome実測では
  詰め前後とも水平オーバーフローは検出できず改善量は数値化不可、実機確認待ち）。
  (2)児童の記録「まとめて保存」を.rec-progress帯に統合(.rec-save-inline-btn)し
  縦1行を解消、44px高は維持（独立commit、後で戻せるようにした）。列数削減
  （横6→5列/縦4→3列）は未実装、次回すぐ適用できるようUI_AUDIT等に方針提示済み
- 使ってみた結果: 実機確認は未実施（Chrome/puppeteerでの自動検証のみ）
- 次: 実機でv1.39.0を確認。○△はみ出しが残るようなら列数削減を追加適用。
  「まとめて保存」統合ボタンが縦向きで押しにくければ独立commitのみrevertする

## 2026-08-29 セッション14
- 完了: v1.39.0実機確認で判明した「はみ出しは横でなく縦（ボタン枠がタイル下端を
  突き抜ける）」を受けて方針転換。flex-wrapは採用せず（A/B+/B/B-/Cの並び順維持が
  優先）、.rec-abc-btn/.sub-status-btnにmin-widthの床を追加し無制限縮小を止め、
  .rec-row.abc-row/.sub-input-rowのpaddingを増やして余白側で吸収する方針に変更
  （v1.40.0）。sub-input-list列数削減案は横方向対策のため保留のまま
- 使ってみた結果: 実機確認は未実施（Chrome/puppeteerでは変更前後とも
  オーバーフローを再現できないため、今回からChrome実測を判断根拠にしていない）
- 次: 実機でv1.40.0を確認。まだ突き抜けるならpadding/min-widthの値を再調整

## 2026-08-29 セッション15
- 完了: v1.40.0/v1.40.1で3回対処しても改善しなかった「タイルからボタンがはみ出す」
  不具合を、横方向でなく縦方向の内容量過多が原因と特定して対処（v1.41.0）。
  主因は.sub-input-rowのお直し完了チェックボックスをlabelで囲みタップ領域を
  確保した際(v1.37.0)のmin-height:44pxが.sub-row-hdの実高を28px相当から
  44pxへ押し上げていたこと。タップ領域は::before疑似要素の非対称オフセット
  (視覚サイズ20pxのまま、隣接する欠席ボタン/○△ボタンを避けて拡張)に切り出し、
  padding/gap/ボタン高も詰めた。.rec-row.abc-rowはpadding/gap/ボタン高に加え
  番号バッジ・欠席ボタンを24pxに縮小(abc-rowにスコープ、他画面へ波及なし)。
  内容量はabc-row 93px→78px(min-height86pxまで8px余裕)、sub-input-row
  121px→88px(横min-height92pxまで4px、縦100pxまで12px余裕)。副次的に、
  ランドスケープ限定の.sub-status-btn高さ上書き(48px)が新設のtile-vheight
  テストで発覚、44pxに統一(コミット未、v1.41.0)
- 使ってみた結果: 実機確認は未実施。tile-vheight-v1.41.0.test.jsを追加し、
  内容量の積み上げ計算とチェックボックス拡張ヒットゾーンの実クリック検証
  (欠席ボタン/番号バッジ/氏名/○△ボタンが個別に正しく反応すること)を含め
  既存32件+新規1件、計33ファイルのテストが全て通過を確認
- 次: 実機でv1.41.0を確認。.sub-row-num/.sub-absent-btnの24px化は
  sub-input-row側では見送っているため(番号バッジ/欠席ボタンの視認性優先)、
  横min-height92pxまでの4px余裕でも見切れが残るようなら次の削減候補とする。
  また、ランドスケープ限定メディアクエリ内の.sub-status-btn高さ上書き(48px)が
  ベースのCSS変更を無効化していた件を新規テストで検知できたため、同種の
  「メディアクエリ内上書きが基準の変更を無効化している」箇所が他にも
  残っていないか次回grepで洗い出す

## 2026-08-29 セッション16
- 完了: 児童の記録・点数入力で最下段の行が画面下端で切れる問題に対し、ルーブリック
  参照パネルの帯(見出し・トグル)を単独の1行から教科タブ行(.rec-filter-row)の
  右側空きスペースへ移動（v1.42.0）。サブナビ行は5ボタンで空き24pxしかなく不採用、
  教科タブ行は最悪条件(8教科・縦向き)でも225px空きがあり帯の自然幅132pxに対し
  93px余裕があるため採用。展開される中身(#recRubricBody)は課題選択行の下の
  元の位置に残し、帯だけを移動(recSetRubricVisible()で両者の表示/非表示を一元化)。
  帯の高さは37px→44pxに是正(タップ領域要件)。#recList実高は横494px/縦858px
  (各+45px)。ユーザー確認の結果、展開時にリストが縮む挙動自体は既存仕様のまま
  維持(帯の新設置による二重の押し下げが無いことのみ確認すれば良い、と明確化)
- 使ってみた結果: 実機確認は未実施。rubric-band-move-v1.42.0.test.jsを追加し、
  帯のタップ高・開閉状態の一致・ルーブリック無し課題での教科タブ行の崩れ無しを
  確認。既存34件+新規1件、計35ファイルのテストが全て通過
- 次: 実機でv1.42.0を確認。#recList実高+45pxで横向きは4行→5行相当の余地が
  出る計算だが、縦向きはChromeのgrid align-content:stretchの影響で正確な
  行数増加を測れていないため、実機での見切れ解消を優先して確認する

## 2026-08-29 セッション17
- 完了: 提出物チェックのサブタブ順序を変更（v1.43.0）。「提出課題の管理」を
  最後尾(5番目)から入力タブ群の直後(3番目)へ移動し、入力/入力（個人）/
  提出課題の管理/提出課題の統計/修養日誌の順に。児童の記録側(点数入力→
  授業課題の管理→統計→…)とタブの並びの考え方を揃える目的。DOMのボタン順序
  のみ変更しIDやsubShowSub()の切替ロジックは無変更、他画面からの直接遷移も
  すべてdata-sub属性ベースのため無影響を確認
- 使ってみた結果: 実機確認は未実施。5タブすべての切替とcontentId/アクティブ
  ボタンの対応を実クリックで確認。忘れ物チェック・成績処理統合は別の
  .sub-subnavブロック(別タブ構成)のため無影響。既存35件のテストが全て通過
  (順序に依存するテストは無く更新不要と確認)
- 次: 特になし

## 2026-08-29 セッション18
- 完了: 残課題3（ヘッダー色の統一）。青グラデーション以外で上書きされていた
  4画面（漢字チェック#2E7D32/#388E3C、新体力テスト#1a6b4a/#2a8a5a、
  成績処理統合#1a1a2e/#16213e、データ出力#1565C0/#1976D2）を基本の
  `linear-gradient(135deg, var(--primary), var(--primary-light))`へ統一。
  調査の結果、緑2画面はhex値が別物で共有変数も無く、ナビグループとも
  対応しておらず、コメント・commit履歴にも色選択の理由の記載が無かった
  ため「意図された色分けではない」と判断（ユーザー承認済み）。
  2コミットに分割（v1.44.0として1回push）:
  (1) 漢字チェック(L3515,L3520)・データ出力(L3675)・新体力テスト
      (L3764本体＋付帯.pf-subnav-btn.active/:hoverのL2237-2238、
      #1a6b4aをvar(--primary)へ)
  (2) 成績処理統合(L3411本体＋付帯.sub-subnav背景L3413・
      タブボタン色L3414-3419・grdTermSel配色L3420、他画面の学期
      セレクタと同一スタイルに統一)。ヘッダーだけ変えると直下の
      紺タブ帯が浮くため付帯箇所を含め1コミットにまとめた
- 使ってみた結果: 実機確認は未実施。puppeteerで14画面全ヘッダーの
  computed背景がrgb(45,125,154)→rgb(58,156,191)に統一されたことを
  1180x820/820x1180の両方で確認（表示上の見た目一致の根拠としてでは
  なく、値が意図通り変わったことの機械的確認として使用）。成績処理統合・
  新体力テストのタブactive/inactive判別（背景色・文字色の対比）、
  grdTermSelがsubTermSelと同一の見た目（背景/文字色/枠線）になった
  ことも確認。既存34件のテストが全て通過。node --checkでJS構文異常なし
- 次: 未対応の既知課題に追記した「ランドスケープ拡大の無効化」
  （#view-grades/#view-submissionsのIDセレクタ上書き、漢字チェックの
  h2インライン固定）は今回のgradient削除では解消しないことを確認済み。
  対応するか判断が必要

## 2026-08-29 セッション19
- 完了:「ランドスケープ拡大の無効化」を解消（v1.45.0）。
  `#view-submissions .view-header`/`#view-grades .view-header`の
  padding:6px 16px上書きを削除、漢字チェックの2ヘッダー（一覧/児童詳細）
  からheight:50px・padding:0 12px・h2のfont-size:1rem固定をすべて削除し、
  基本の`.view-header`／ランドスケープ拡大メディアクエリ（L2024-2029付近、
  padding:18px 24px、h2 1.25rem）に委ねた。提出物チェックの
  「修養日誌1画面表示のため」の余白削減コメントは、30人+実測で
  余白367〜735px確保できることを確認した上で撤去（削るほどの
  必要性が無かったと判断、ユーザー承認済み）
- 想定外の副作用と対処: ヘッダーを66px→80px級に戻したことで、
  提出物チェックの「入力（個人）」タブでperson.test.jsがタイムアウト
  FAILするようになった。原因は`.view-header`と`.sub-subnav`が両方
  `position:sticky;top:0`のため、入力（個人）/統計/修養日誌/課題管理
  タブ（outer `.view`をスクロールするフォールバック方式）でスクロール
  すると両者が同じ位置で重なり合い、ヘッダーの拡大でこの重なりの
  死角（覆われて見えない/タップできない帯）が広がっていたこと
  （元々存在した設計上の欠陥で、今回のヘッダー拡大が顕在化させた）。
  「入力」タブは#sub-sub-inputが内部スクロールのためouter側は元々
  スクロールせずstickyが無関係、と確認した上で
  `#view-submissions .view-header, #view-submissions .sub-subnav`を
  `position: static`に変更して解消。成績処理統合も同じsticky構造だが、
  30人データで計算式/成績一覧/専科入力タブのoverflowが0（スクロール
  自体発生しない）ことを実測確認したため今回は変更なし（次項に注記）
- 使ってみた結果: 実機確認は未実施。puppeteerで全14画面のヘッダー高さ・
  paddingを横1180x820/縦820x1180で実測し、h2のみの画面（横66px/縦60px）
  とヘッダー内ボタン持ちの画面（提出物チェック・漢字チェック・個人カルテ、
  横80px/縦76px）の2グループに統一されたことを確認（出席簿・児童の記録・
  新体力テストは元々別レイアウトのためスコープ外、変更なし）。漢字チェック
  はh2が1.25rem化してもボタン（＋漢字を追加/一括PDF出力/←一覧へ戻る）が
  ヘッダー幅に収まりはみ出さないことを一覧・児童詳細の両ヘッダーで確認。
  既存34件のテストが全て通過（person.test.jsの回帰含め解消を確認）
- 次: 特になし（残課題3・4は完了）

## 2026-08-29 セッション20
- 完了: 実機確認で「色は統一されたがヘッダーの高さが揃って見えない」との
  指摘を受け、原因調査（残課題2として新規着手）。全14画面のヘッダー内の
  実際の中身を列挙し、(a)h2のみ (b)h2＋操作ボタン (c)機能ボタン混在
  (d)タブ・日付操作がヘッダー内、の4パターンに分類。青地が途中で
  切れて見える原因を実測で特定：`.view-header`は横方向flexで、
  子要素は`flex-grow`が無い限り横幅いっぱいに広がらない。児童の記録
  (`.rec-subnav`)・出席簿(`.att-top-bar`)はタブ/ツールバー用の白背景divを
  ヘッダーの唯一の子として横並びで置いているため、その内容量ぶんの幅
  しか占有せず、残りは親の青が露出する（実測: 児童の記録は
  ヘッダー幅1088pxに対し白い部分675px/青の露出413px、出席簿は
  631px/457px）。新体力テストも同じく「タブがヘッダー内」の(d)分類だが、
  `.pf-subnav`は縦方向flex(`flex-direction:column`)の子要素のため
  デフォルトの`align-items:stretch`で自動的に横幅いっぱいに伸び、
  実測でも1088px/1088pxと一致し問題なし。このためユーザー指摘のうち
  新体力テストは実害なし、児童の記録・出席簿のみが実際の不具合と判明。
  対応範囲は今回、低リスクな2件（新体力テスト・漢字チェック）に絞り、
  出席簿（新規に下段バーの設計が必要）と児童の記録（#recListの高さ
  計算に直結し本日4セッション分の調整が水泡に帰すリスク）は次回以降の
  独立セッションへ分離（ユーザー承認済み）
- 完了: 新体力テストの`.pf-subnav`を`.view-header`の外へ移動（見た目は
  現状維持、構造のみ他画面と統一）。ラッパーだったcolumn-flexの
  `.view-header`とその内側のタイトル行divを削除し、他画面と同じ
  素の`.view-header`（h2＋hdr-actions）＋兄弟要素の`.pf-subnav`という
  構成に変更（v1.46.0、独立commit 179a48c/次項とは別）
- 完了: 漢字チェックの「＋漢字を追加」ボタンをタイトル直後から
  `hdr-actions`内（一括PDF出力ボタンの前）へ移動。スタイル・位置以外の
  変更なし（v1.46.0、独立commit）
- 使ってみた結果: 実機確認は未実施。puppeteerで新体力テストの
  `.pf-subnav`が`.view-header`の外（`subnavInsideHeader:false`）に
  出て幅がヘッダーと一致すること、5タブ全ての切替（active状態・
  パネル表示）が正常に動くこと、外に出したことでview全体のoverflowが
  発生しないことを確認。漢字チェックは一覧ヘッダーの2ボタンが両方とも
  ヘッダー枠内に収まりはみ出さないこと（横1180x820/縦820x1180の両方）
  を確認。既存34件のテストが全て通過
- 次: 出席簿・児童の記録のヘッダー構造統一（未対応の既知課題を参照）

## 2026-08-29 セッション21
- 完了: 提出物チェック「入力」タブ(リストモード)で、教科フィルターバー
  (`#subFilterBar`)が単独で1行を専有し縦を圧迫していた問題を解消
  （v1.47.0）。実測で横向き27人・1教科の場合、統合前はリスト必要高
  488pxに対し確保高484pxで4pxオーバー（実機の見切れ報告と一致）、
  統合後は確保高537pxで49px余裕が生まれることを確認した上で実装。
  `#subFilterBar`を単独行から進捗帯(`#subProgressWrap`)の左側へ統合し、
  空いた1行ぶんを`#subListWrap`(flex:1)へ還元。教科タブ＋ラベル＋件数を
  新設の`#subProgressLeft`(flex:1)でグループ化し、`全員○`ボタンだけ
  右端に残す構成（教科タブ→提出率・件数→全員○ボタンの順）。共有クラス
  `.rec-filter-bar`/`.rec-filter-btn`/`.sub-input-progress`自体は変更
  せず、`#subFilterBar`のインラインstyleに`padding:0;border-bottom:none;`
  を追加して共有クラス由来の行区切り用padding(4px 8px)だけを打ち消した
  （実装時にこれを見落として進捗帯が54px→63pxに増えるミスを一度発生
  させ、テストで検知して修正した）
- 使ってみた結果: 実機確認は未実施。sub-progress-filter-merge-v1.47.0.test.js
  を新規追加し、27人/30人×1教科/8教科×横1180x820/縦820x1180の
  8パターンで①最下段クリップ無し(scrollH<=clientH)②進捗帯高さ54px維持
  ③教科タブ・全員○ボタンのタップ領域44px以上④教科タブクリックでの
  絞り込み動作⑤8教科でも折り返さない、を検証（既存34件＋新規1件で
  56項目、全通過）。実データに近い条件（横1180x820・27人・1教科）でも
  最下段クリップが解消（カード最下端793px＜確保領域800px）を個別に再確認
- 次: 特になし

## 2026-09-17 セッション22
- 完了：項目ごとの入力形式選択（得点／5段階、test.inputMode）とテスト日付の動的化（v1.49.0）
- 使ってみた結果：iPad実機確認OK（既存の5点満点の振り返りは数値のまま、新規項目で得点・5段階を選択可、既存の記述問題は5段階のまま）
- 次：（未定）

## 2026-09-19 セッション23
- 完了：データ反映監査（DATA_FLOW_AUDIT.md）の高＋M1＋M6を修正（v1.50.0）。カルテ出力/面談テキスト/成績CSVの学期を出力側に統一・成績側キャッシュを画面切替で破棄・汎用実技記録を全観点で共通換算・観点/教科変更時の項目重み移送・ダッシュボードのテスト平均を成績処理と同じ集計に・期限日欠席かつ未提出を提出率の分母から除外（全経路で grdSubmissionJudge 共通化）。回帰テスト tests/data-reflection.test.js 45件追加
- 使ってみた結果：iPad実機確認は未実施（puppeteerのみ。全テスト825 PASS、既存のisabc系3件FAILは着手前から）。実技記録を知識・技能以外にしている課題、出席簿が欠席の日が期限の未提出課題がある児童は成績・提出率の値が変わるため要確認
- 次：iPad実機で成績処理・カルテ出力・提出物統計の値を確認。未対応はH4(児童ID未導入)・M2〜M5・M7〜M9・L1〜L7（DATA_FLOW_AUDIT.md参照）

## 2026-09-19 セッション24（v1.50.1）
- 完了：H6（名簿保存で視力・身長が消える）を修正、isabc系の既存失敗3件（テスト側の古さ）を修正、その過程で見つかった実装不具合H7（観点編集で入力形式と満点が矛盾、v1.49.0由来）を修正。全テスト867件PASS
- 使ってみた結果：iPad実機確認は未実施（puppeteerのみ）。名簿を変更せず保存しても視力・身長が残ること、既存課題の観点を変更して入力画面が5段階/得点に追随することを要確認
- 次：push後にH4案B段階1（児童IDの付与と起動時移行）へ。段階2以降は段階1のiPad確認後

## 2026-09-19 セッション25（v1.51.0 前半）
- 完了：H7の既存矛盾データ検出。入力形式と満点が矛盾する課題（得点なのに満点なし／5段階なのに満点あり）を診断ツールに一覧表示（検出と表示のみ、自動修正なし）
- 使ってみた結果：iPad実機確認は未実施。設定の「診断を実行」で該当課題が出ること、課題管理で開いて保存し直せることを要確認
- 次：H4 段階1（児童IDの付与と起動時移行）

## 2026-09-19 セッション26（v1.51.0 後半）
- 完了：H4 案B 段階1。起動時に児童ID(studentId)を持たない児童にだけ付与（冪等）。付与前に spa_master を退避し、失敗時は書き戻し。既存データは1件も変わらない（全キー比較）。診断出力に退避コピーの氏名が出る不具合も修正。全テストPASS
- 使ってみた結果：iPad実機確認は未実施。起動して画面・成績・名簿がこれまでと同じであること、名簿保存とバックアップ復元後も動作が変わらないことを要確認
- 次：段階1のiPad確認後に段階2（対象ストアの登録表と remapStudentIndexes、スナップショット/検証/ロールバック）

## 2026-09-19 セッション27（v1.51.1）
- 完了：H4 案B 段階2a。児童データ付け替えの純関数(computeRosterRemap/planRosterChange)・不変条件I1〜I7・18ストアの登録表を実装(UI未接続・書き込みなし・既存の挙動は変わらない)。10.7の5点(退避は無期限・スナップショット1世代・取り消しは入力があれば不可でトースト・pfは通知のみ・自動再読み込みは事前にflush)を設計書に反映
- 使ってみた結果：画面から使われないため実機での見た目の変化はなし。iPad実機確認は未実施(段階1の確認待ち)。テストは 6操作×18ストアのずれ0(計画レベル)・ランダム60ケース・変異テストで確認
- 次：段階2b(検証付き書き込み・スナップショット・ジャーナル・起動時ロールバック・applyRosterChange・退避/復元)。段階1のiPad確認後に指示

## 2026-09-19 セッション28（v1.51.2 前半）
- 完了：H8 IndexedDBの鏡に削除したデータ(氏名)が残る問題を修正。退勤モード消去が新体力テスト(pf_roster=氏名・記録)と旧バックアップを消さなかった／pfの名簿リセット・記録削除が鏡に残り次回起動で復活しバックアップに入った／過去の残りの掃除。課題・記録の削除は残らないことも確認。全テストPASS
- 使ってみた結果：iPad実機確認は未実施。退勤モードで消去したあと、新体力テストの名簿・記録も消えていること、pfの「名簿リセット」「記録データ削除」のあとバックアップに残らないことを要確認
- 次：段階2bの続き

## 2026-09-19 セッション29（v1.51.2 後半）
- 完了：H4 案B 段階2b。検証付き書き込み(段階1のID付与も置換)・容量の事前確認・スナップショット/ジャーナル/退避/各ストア/名簿の順に適用する applyRosterChange・起動時の自動ロールバック・退避と復元・取り消しを実装(UI未接続で既存の名簿保存は変わらない)。書き込みの合間の中断23通り・容量超過/破損の注入23通りで全キーが変更前と一致。roster-shift の実適用(6操作×18ストア)もずれ0
- 使ってみた結果：画面から使われないため実機での見た目の変化はなし。iPad実機確認は未実施。変異テストでロールバック・中断復旧・検証の破壊を検出できることを確認
- 次：段階2c(pf連携)。段階1〜2bのiPad確認後に指示

## 2026-09-19 セッション30（v1.51.3）
- 完了：H8追補（実機のIndexedDBに残った削除済みデータを起動時に一回だけ掃除。所有しないキーは触らない）、名簿変更前の容量確認を実際の空きで判定する方式へ変更、H4 段階2c（新体力テスト連携: pf名簿が一致するときだけ記録・集計・名簿を追従、不一致は触らず通知のみ、pfも同じトランザクション）。テストの通知確認が常に通る不具合も修正。全テストPASS
- 使ってみた結果：iPad実機確認は未実施。画面からは使われないため見た目の変化はなし(段階3で接続)。初回起動で古い残りが自動掃除されること(操作不要)を要確認
- 次：段階3（名簿編集UIの接続・退避の一覧/復元/完全削除UI）。段階1〜2cのiPad確認後に指示

## 2026-09-19 セッション31（v1.52.0）
- 完了: H4 段階3（名簿編集UIの接続）。名簿のテキスト編集＋警告ダイアログを廃止し、リスト編集UI（並べ替え・途中削除・転入・改名・性別、下書き＋保存ボタン）に置き換え。保存は `applyRosterChange` の1本だけ。設定に「退避中の児童 N名」（一覧・復元・2回タップの完全削除）、取り消しボタン・取り消しトースト、バックアップ促しトースト。確認ダイアログなし。tests/roster-ui.test.js（66件）新規、roster-preserve・student-id-migration をUI操作に書き換え。全体1226件PASS。詳細は DATA_FLOW_AUDIT.md 15章
- 使ってみた結果: iPad実機で確認済み(push後)。バージョン表示がv1.52.0になった／名簿がリスト表示で内容が正しい／改名→保存→取り消しで元に戻る／気になった点なし。「変更なしで保存すると『変更はありません』と出る」も、後日の回答でOKを確認
- 次: 段階4（pf.html の studentId 化・pf-residual の復元・バックアップ v11）の設計案（コード変更なし）

## 2026-09-19 セッション32（v1.52.1）
- 完了: H4 段階4a（新体力テストの連携状態の診断表示。設定画面に確認用カード、読み取り専用）。名簿の一致/不一致(理由)・人数・pf_fitness の痕跡(単体ページ使用)・SPA名簿にいない児童の人数(氏名は出さない)・記録の年度別人数。tests/pf-diagnosis.test.js 37件。詳細は DATA_FLOW_AUDIT.md 17章
- 使ってみた結果: 未確認（iPadで設定画面のカードを見て、結果を報告してもらう。結果で16.11のa・bを決める）
- 次: 4d-1（importBackup の安全化）→ 4b → 4c → 4d-2 → 4e

## 2026-09-19 セッション33（v1.52.2）
- 完了: H4 段階4d-1（バックアップ復元の安全化。commitのみ・未push）。復元前の自動退避(スナップショット)・検証付き書き込み・失敗時のロールバック・起動時の自動復旧・復元の取り消し・容量不足時は警告して続行。バックアップに無いキーの扱いを明確化(名簿が違うときだけ児童別データを消す/別アプリのキーは端末に無いときだけ書く)。形式が違うファイルは「復元できません」で中止。tests/backup-restore.test.js 71件、変異テスト13件すべて検出。詳細は DATA_FLOW_AUDIT.md 18章
- 使ってみた結果: 未確認（iPadでの確認は未実施。push後に確認手順を案内する）
- 次: 4a の診断表示の結果(iPad)を見て16.11のa・bを決める → 4b → 4c → 4d-2 → 4e

## 未対応の既知課題
- 【セッション21で追記・設計基準】ヘッダー構造統一の基準（後述）に加え、
  「単独の1行を専有させず、空きのある隣接行に寄せる。どちらの行が
  主役になるかは画面ごとの空き具合で決まる（児童の記録＝教科タブ行に
  ルーブリック帯を寄せる／提出物チェック＝進捗帯に教科タブを寄せる）」
  を設計基準として記録
- 【セッション20で追記】ヘッダー構造の統一（残課題2）。全14画面を
  (a)h2のみ (b)h2＋操作ボタン (c)機能ボタン混在 (d)タブ・日付操作が
  ヘッダー内、に分類済み。今回(c)(d)のうち漢字チェック・新体力テストの
  みに対応。出席簿（h2なし、日付バッジ・前後ボタン・3タブ・PDFボタンが
  `.att-top-bar`としてヘッダー直下にまとまっている）と児童の記録
  （h2なし、`.rec-subnav`のみがヘッダーの中身）は未対応。設計基準は
  「青ヘッダーに入れるのはタイトル（左）・学期セレクタ・一括操作ボタン
  （最右）のみ。タブ・日付操作・フィルタはヘッダーの外（下の白い
  サブナビ行）に置く」（口頭決定のみでファイル未記載だったため今回
  明文化）。児童の記録は`#recList`の高さ計算に直結するため、着手時は
  ヘッダー変更前後の`#recList`実高を測りながら実機確認とセットで進める
  こと。2026-08-29のセッション12・14・15・16で「最下段が見切れる」
  不具合を繰り返し修正してきた領域であり、ヘッダーを66〜80px新設で
  追加するとその成果を打ち消す高いリスクがある
- 【セッション19で判明】`.view-header`と`.sub-subnav`を両方
  `position:sticky;top:0`にする実装は、outer `.view`側がスクロールする
  画面・タブでは常に重なり合う（サブナビ側が常にヘッダーの下に隠れる）
  設計上の欠陥。現状は提出物チェックのみ`position:static`で個別に
  無効化して対処（本セッション参照）。成績処理統合は現状データ量では
  スクロールが発生せず未対応のままだが、将来教科・課題数が増えて
  `#view-grades`がスクロールするようになった場合は同じ死角バグが
  顕在化しうる。恒久対応するなら`.sub-subnav`のtop値をヘッダー実高分
  オフセットする（現状は個別画面でstaticにする対症療法）
- 【セッション18で判明・実測】`.hdr-btn`（半透明白背景のヘッダー内ボタン、
  PDF出力・CSV・保存・モード切替等）の白文字コントラストが全画面で
  4.5:1未満（小さい文字の基準）。ヘッダー色に関わらず元々未達で、
  今回の色統一によって悪化したものではないためスコープ外とし記録のみ:
  基本(青)明側3A9CBF=2.44:1、漢字明側388E3C=2.97:1、
  新体力明側2a8a5a=3.10:1、データ出力明側1976D2=3.31:1、
  成績処理(紺)明側16213e=8.39:1（唯一の合格。色統一によりこの
  唯一の合格例も基本の青パターンに揃うため、対応するなら
  `.hdr-btn`自体の背景を不透明側へ寄せる等、ヘッダー色とは別軸の
  対応が必要）。なお、UI_AUDIT.mdに「Table B」という節は存在しない
  ことをセッション18で確認済み（このファイル旧記載の誤り）。
  上記コントラスト実測がその代わりの記録となる
- コントラスト4.5:1未満の残り38行は未対応。うち var(--danger) on white（3.82）が多数を占めるが、
  エラー表示用途で文脈からも判別可能なため優先度は低い
- 色の表記を変更するとスナップショットテスト（attendance-snapshot.js）が必ずFAILする。UI統一作業では色変更のたびに基準更新が必要。検証は「旧基準のhexをvar()へ順方向変換して新生成HTMLと比較」する方向で行う（逆方向だと元々var()だった箇所まで巻き込んで誤差が出る）
- ctx.strokeStyle の #fff（L15314, L21017）はCanvas描画のためvar()化できず、将来 --white を変更した際に取り残される
- 直書き色のうち文字色として使われているものは、D-3調査で色ごとの一覧は取ったが、背景とのコントラスト比までは全件検証していない。#cce のように常時判読困難な箇所が他にも残っている可能性がある
- L890（.rec-matome-modal-close、まとめテスト/実技ルーブリック入力モーダルの×ボタン）は44px化するとpe-rubric.test.jsの「モーダルスクロール解消(1180x820)」テストがFAILする（overflow=5）。#recPeRubricModalはv1.15.4でiPad Air横向き実寸ちょうどに余白を使い切る設計のため、共有クラス側を伸ばすと即座に破綻する。36pxのまま据え置き。44px化するなら#recPeRubricModal側だけ別クラスで除外するなど、モーダル全体の再設計とセットで行うこと
- セッション9時点でのbutton/input/select/textarea 44px未満の残存: L890(36px、モーダル×ボタン、テスト制約により据え置き。理由はこのファイル内の別項目参照)、L1696(34px、ルーブリック入力欄、隣接バッジとセットで対応予定)、L7657(22px、まとめテストバッジ、今回対象外)、L2197(28px、.pf-sg-table入力欄、今回対象外)。
- .sub-input-row のチェックボックス（L1598 .sub-correction-check）は縦方向のみmin-height:var(--tap-min)化済み（セッション11）。横方向のタップ領域は20pxのまま。カード幅が190〜200pxしかなく、横44px化すると氏名表示が76px→60pxに縮むため見送った。カードのレイアウト自体を見直す機会があれば再検討する
